# Frozen-interface lock — manifest format + dir-merkle + checksum modes + excludes
# (Frozen interface #1: manifest format; #2 layout helpers will be added/locked at
# store-trait). Locked by gate `freeze-contract` @ 2026-05-31T02:22:13Z (human-approved).
#
# These snapdir-core source files DEFINE the frozen manifest line format, the directory
# merkle rule, the snapshot-id derivation, the md5/sha256/keyed hashers, and the
# %system%/%common% exclude sets. After the freeze, any change to line format, ordering,
# checksum algorithm, sharding, or the exclude sets needs a human-approved frozen-interface
# mutation (a `## Proposal` in a handoff → AskUserQuestion). The golden tests in
# crates/snapdir-core (golden_b3sum/golden_md5/golden_sha256/keyed_context) are the
# behavioral guard; this lock is the tripwire on the defining source.
#
# NOTE: later core gates (store-trait, cache-id) add NEW files (store.rs, cache.rs) —
# those do not affect this lock. A mismatch HERE means a frozen-format file changed →
# CRITICAL ALERT, escalate. Re-verify: shasum -a 256 -c .gatesmith/manifest-format.sha.lock
91d7f88d8c38a5a268c46035ee6f6c5c8099b825ecbd1c8dcb1dd10e4d770ce7  crates/snapdir-core/src/manifest.rs
ea166a73330458f20b0c70850696c577a67906f3b17ef361cf47308ba811512f  crates/snapdir-core/src/merkle.rs
b6f31f301aad5c875a8ed95362e3585851b0210678153a4fc1738807f8dc8857  crates/snapdir-core/src/excludes.rs
