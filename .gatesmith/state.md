# Project state

> Derived from `.gatesmith/gates.yaml` — re-projected by the PM at the end of every
> tick. Do not edit by hand; edit `gates.yaml` instead.

## 🔨 PHASE 32 IN PROGRESS — 18/27 — exact-mirror (`--delete`) + materialization modes for checkout/pull/sync

> Plan: `~/.claude/plans/cosmic-knitting-tide.md` (operator-approved 2026-06-19). 5 adversarial triples (prune-set/core → materialize-modes/stores → checkout-cli/cli → atomic-swap/stores → sync/stores) + 7 hardening/verify gates (durability keystone, safety, feasibility-matrix, bench, security-review ✋, docs, verify). HARD INVARIANTS: no shared-object corruption (objects 0444; writable/remote-link = hard error); no extra byte copies (atomic only when staging is zero-copy: reflink-on-CoW or symlink); hard-refuse dangerous dests (no --force escape); GC OUT OF SCOPE (sync prunes manifests only). ⏳ OPERATOR ADD (2026-06-19): a linked-mode checksum-reuse fast-path triple is being enqueued (see journal LINKED-FASTPATH) — re-snapshot of a `--linked` tree can recover BLAKE3 from the object path (no re-hash), but must guard non-default checksum algo / escaped+dangling symlinks / mutated 0444 object / mode+size fidelity.
> ✅ `mirror-atomic-swap-impl` (stores, code `b9bf8bf`) — `FileStore::fetch_files_atomic`: builds the complete tree into a same-FS sibling staging dir via `fetch_files_with_mode` (Auto reflink / Linked symlink), then `rename(dest→.old)`+`rename(staging→dest)`+best-effort old removal (rollback if swap rename fails); dest-absent = single rename. Held-fd survives (old inode renamed aside). Swap-or-nothing: dest touched only at final rename after staging fully succeeds; every error path cleans staging + `dest.old`. Zero-copy ONLY: Auto consults `cow_reflink_supported` → non-CoW = `StoreError::Backend` (no frozen-core variant); Linked never refused. Took over staged test → 7/7 (zero content change). ⚠️ CLI FOLLOW-UP: `checkout --delete` does not yet route through `fetch_files_atomic` (atomic-by-default-when-zero-copy is a CLI/router concern). Verify FULL PASS (FINAL_EXIT=0): build `--locked` + 7/7 + workspace 1016 + frozen lock 3/3 + fmt + clippy.
> ✅ `mirror-sync-spec-tests` (adversary, ledger-only) — BLACK-BOX spec staged at `.gatesmith/pending-tests/mirror_sync.rs` (13 `#[test]`s; lands in `crates/snapdir-stores/tests`). Assumed API: `sync_snapshot_mirror(...)`/`delete:bool` + `StreamStore::supports_mirror()->bool` (FileStore true) + `delete_manifest(id)` — **NO `delete_object`**. Pins: manifest-set mirror (prune dest manifests absent from source set, keep shared + synced id); **NEVER-DELETE-OBJECT** (3 ways: shared object survives+verifies, orphan survives, `.objects/` before⊆after); copy-in-before-delete; healthy mirror byte-identical to plain sync; unsupported-dest (`supports_mirror()==false` double) HARD ERROR changing nothing; `--dryrun` deletes nothing; idempotent. ⚠️ FLAGGED: unsupported-dest refusal is stores-level via `supports_mirror()` — if gated only at CLI it moves to a CLI test (not weakened); single-id-vs-manifest-set semantic took the conservative reading. Expected un-passable until impl.
> **▶ NEXT (id-asc): `mirror-sync-impl` (stores)** — add `delete_manifest` + `supports_mirror()->bool` to the StreamStore trait (default Unsupported/false; FileStore true; **NO `delete_object`**); `sync_snapshot` mirror path: copy-in → list source vs dest manifest ids → delete dest manifests absent from source set; refuse object/remote dests; take over the staged test → green. Then `mirror-sync-review`.
>
> 🏁 **LINKED-FASTPATH CLUSTER COMPLETE** (design ✋ → spec-tests → impl-core [reopened for strict-verify] → impl-cli → review). ✅ `mirror-linked-fastpath-review` (adversary, code `cabc8d9`) — **independently CONFIRMED** the 2 PM-authorized corrections are correct (traced: non-plain-BLAKE3 → `object_store_roots` empty → fast path dormant → normal hash; only plain-BLAKE3 + `SNAPDIR_VERIFY_COPIES=1` reaches the integrity compare, so md5/keyed has no address to verify → success+rehash, not error). Strengthened both + weakening audit clean (9/11 byte-identical). +4 cases (11→**15**): manifest-level no-read keystone + strict-verify error, mixed linked+real, `recover_object_key` `..` canonicalization. NO reopen. Verify FULL PASS (FINAL_EXIT=0; ~13min under external load ~200-237): 15/15 + workspace green + frozen 3/3.
> **▶ NEXT (id-asc): `mirror-sync-spec-tests` (adversary)** — opens the LAST triple. BLACK-BOX for `sync --delete` (manifest-set mirror): after sync, dest manifests NOT in the source set are deleted; **NO object ever deleted** (shared-pool safety); copy-in before any delete; `--delete` to s3/gcs/b2/ssh dest = clear HARD ERROR; healthy `--delete` round-trip byte-identical to plain sync. Then impl (stores: `delete_manifest`+`supports_mirror`) → review. After that: the 6 hardening/verify gates (durability, safety, feasibility-matrix, bench, security ✋, docs, verify).
>
> 🗒️ (history) `mirror-linked-fastpath-impl-cli` PASSED (cli, code `c5011f1`, fc=1→pass) — wired `object_store_roots` for id/manifest (cache-dir store + local `--store`, gated plain-BLAKE3) + `cache_dir()`/`$SNAPDIR_CACHE_DIR` bugfix + `verify_linked_objects` from `SNAPDIR_VERIFY_COPIES=1` (→ `LinkedObjectIntegrity` error names the file). Corrected the 2 over-reach assertions (wrong-algo/keyed → success+rehash, not error) per the locked design; other 9 untouched. 11/11; workspace 1031 green; frozen 3/3. ⚠️ The 2 test corrections are flagged for the review to audit/bless.
> **▶ NEXT (id-asc): `mirror-linked-fastpath-review` (adversary)** — weakening audit of `crates/snapdir-cli/tests/mirror_linked_fastpath.rs` vs staged; **specifically audit/bless the 2 PM-authorized over-reach corrections** (confirm they match the locked design — wrong-algo/keyed re-hashes, no error — and weren't a weakening to hide a bug); re-assert the keystone (default==strict on healthy store), no-content-read proof, strict-verify error, escaped/dangling, checksum-only; add impl-revealed cases. `cargo test --workspace` + frozen lock green. (Then the sync triple + 6 hardening/verify gates remain.)
>
> 🗒️ (history) `mirror-linked-fastpath-impl-core` RE-PASSED (core, code `4154b8e`, fc=1→pass) — added the strict-verify keystone on top of `2469a57`: opt-in `WalkOptions::verify_linked_objects` (default false) + typed `WalkError::LinkedObjectIntegrity`; flag on → re-hash content + compare to the recovered address, error naming the object on mismatch (`SNAPDIR_VERIFY_COPIES=1` semantics). Default byte-identical; wrong-algo/keyed/escaped inert (no spurious error — confirms the 2 impl-cli over-reach tests must be corrected to expect success). NO LANE-FENCE-EXC (the `..default()` from 2469a57 already in HEAD absorbs the new field; core-only diff). Verify FULL PASS (FINAL_EXIT=0; 1020 green, frozen 3/3). **impl-cli unblocked.**
> **▶ NEXT (id-asc): `mirror-linked-fastpath-impl-cli` (cli, fc=1)** — RE-RUN: wire `object_store_roots` for id/manifest (redo the `cache_dir()`/`$SNAPDIR_CACHE_DIR` bugfix) gated plain-BLAKE3 + local store; set `verify_linked_objects=true` (+populate roots) when `SNAPDIR_VERIFY_COPIES=1`; **correct the 2 over-reach assertions** (`wrong_checksum_bin`/`keyed_manifest_context`: expect SUCCESS + re-hashed manifest, NOT error — PM-authorized per the locked design, adversary review audits). Take over the staged CLI test → green 11. Then `mirror-linked-fastpath-review`.
>
> 🗒️ (history) `mirror-linked-fastpath-impl-cli` first attempt BLOCKED 8/11 (fc=1) + impl-core REOPENED — strict-verify gap fixed above; 2 over-reach tests pending correction at the impl-cli re-run. The cli teammate wired the hint correctly (incl. a real `cache_dir()`-ignores-`$SNAPDIR_CACHE_DIR` bugfix for id/manifest) — 8/11 green — but 3 tests need behavior `impl-core` didn't land. **PM adjudication:** (1) `strict_verify_id_errors_on_corrupted_object` = LEGIT core gap — the approved design keystone says `SNAPDIR_VERIFY_COPIES=1` re-hashes AND ERRORS; `2469a57` did recovery+dangling only → **reopen impl-core** to add an opt-in `verify_linked_objects` WalkOptions flag (recover address + hash content + typed-error on mismatch). (2)+(3) `wrong_checksum_bin`(md5) + `keyed_manifest_context` = OVER-REACH — design says wrong-algo merely re-hashes (no error); `manifest --checksum-bin md5` is pure hashing → different md5 manifest, exit 0; the "must ERROR" assertions contradict the locked design → corrected (success+rehash) when impl-cli re-runs, adversary review audits. Failed cli working-tree changes reverted (clean tree for the core reopen; pending CLI test restored).
> **▶ NEXT (id-asc, failed-first): `mirror-linked-fastpath-impl-core` (core, fc=1)** — RE-RUN: ADD the opt-in `verify_linked_objects` flag (default false; when set, eligible linked-into-store entry recovers H + hashes content → H', `H!=H'` → typed `WalkError` naming the file — the strict-verify keystone). Keep recovery+dangling (2469a57) + default byte-identical + frozen lock. Then `mirror-linked-fastpath-impl-cli` re-runs: wire the flag from `SNAPDIR_VERIFY_COPIES=1` + redo the cache_dir fix + correct the 2 over-reach assertions → green 11. Then review.
>
> 🗒️ (history) `mirror-linked-fastpath-impl-core` first pass was `2469a57` — CORE half, additive/opt-in/DORMANT. New pure `recover.rs::recover_object_key` inverts the frozen `.objects/<3>/<3>/<3>/<rest>` layout (path parsing, no I/O/panic). `HashFile::recovers_object_keys()` = type-driven eligibility (plain `Blake3Hasher` only; keyed/md5/sha256→false; seam is `HashFile` since `Hasher` is in frozen `merkle.rs`). `WalkOptions::object_store_roots` (default empty). walk.rs: eligible symlink-to-object → checksum recorded directly, NO content read; dangling → typed `WalkError::DanglingLinkedObject` (no panic); escaped/ineligible → normal hash. KEYSTONE: default byte-identical (snapshot_id unchanged, frozen 3/3, 1020 green). ⚠️ **LANE-FENCE-EXC** (journaled): one behavior-preserving `..WalkOptions::default()` at the sole exhaustive cli `WalkOptions` literal (forced by the new field; hint empty → dormant) — impl-cli replaces it with real wiring. Did NOT take over the CLI test. Verify FULL PASS (FINAL_EXIT=0).
> **▶ NEXT (id-asc): `mirror-linked-fastpath-impl-cli` (cli)** — wire the `object_store_roots` hint into the `id`/`manifest` walk (replace the dormant `..default()` at cli.rs:3432 with the real local-store-root) so a re-snapshot over a `--linked` tree takes the fast path; honor the eligibility (plain non-keyed BLAKE3, not `SNAPDIR_VERIFY_COPIES=1`; keep `manifest --checksum-bin` OFF the fast path). Take over the staged CLI test (`git mv → crates/snapdir-cli/tests/mirror_linked_fastpath.rs`) → green 11. Then `mirror-linked-fastpath-review`. (`mirror-sync-spec-tests` also ready, sorts after by id.)
>
> ✅ `mirror-linked-fastpath-spec-tests` (adversary, ledger-only) — BLACK-BOX CLI assert_cmd spec staged (11 `#[test]`s; lands in `crates/snapdir-cli/tests` via impl-cli). Pins: KEYSTONE default `id`/`manifest` over a `--linked` tree == `SNAPDIR_VERIFY_COPIES=1` (re-hash) byte-identical on a healthy store; no-content-read proof (garbage-inject the object keeping its address → default unchanged, strict errors); wrong-algo (`manifest --checksum-bin md5sum`) + keyed (`SNAPDIR_MANIFEST_CONTEXT`) disable→re-hash→error; escaped/non-object symlink hashed normally; dangling→typed error (no panic); checksum-only honesty (id differs from source). ⚠️ FLAGGED for impl: `snapdir id` has NO `--checksum-bin` (only `manifest` does, cli.rs:570) — eligibility must gate plain-non-keyed-BLAKE3 AND keep `manifest --checksum-bin` off the fast path. Expected un-passable until impl.
> **▶ NEXT (id-asc): `mirror-linked-fastpath-impl-core` (core)** — land the recover-from-object-path capability + the additive opt-in 'object-store roots' hint in snapdir-core walk/hash (dormant until cli wires it); verified by workspace build+tests green + frozen lock (does NOT take over the CLI test). Then `mirror-linked-fastpath-impl-cli` (wire id/manifest + take over the test) → review. (`mirror-sync-spec-tests` also ready, sorts after by id.)
>
> ✅ `mirror-linked-fastpath-design` (generic, HUMAN ✋ APPROVED 2026-06-19 'Approve + wire CLI now', ledger-only) — locked the linked-mode checksum-reuse fast path in `.gatesmith/reviews/mirror-linked-fastpath.md`: recover BLAKE3 from the symlink's object path (no content hash) when target=local-store object + plain non-keyed BLAKE3 + not `SNAPDIR_VERIFY_COPIES=1`; fallbacks re-hash (wrong-algo/escaped/strict) or typed-error (dangling, no panic); **CHECKSUM-ONLY** (does not reproduce the original id — walk records the symlink's own lstat mode+size); KEYSTONE recovered==hashed on a healthy store; additive opt-in core hint, no frozen-format change. **GATE-BUMP** (operator 'wire CLI now', 26→27): split impl into `mirror-linked-fastpath-impl-core` (core capability + hint) → `mirror-linked-fastpath-impl-cli` (wire into id/manifest, take over the CLI test) → review; spec-tests authored CLI-level (assert_cmd).
> **▶ NEXT (id-asc): `mirror-linked-fastpath-spec-tests` (adversary)** — opens the fastpath cluster. CLI-level black-box assert_cmd: `snapdir id`/`manifest` over a `--linked` checkout reuses the embedded checksum byte-identically; prove no-content-read (corrupt object bytes, keep address) on the default path while `SNAPDIR_VERIFY_COPIES=1` re-hashes+errors; wrong-algo/escaped→re-hash; dangling→typed error; checksum-only (no id-reproduction claim). (Also ready, sorts after by id: `mirror-sync-spec-tests`.)
>
> 🏁 **ATOMIC-SWAP TRIPLE COMPLETE** (spec→impl→review; review recovered from an fc=1 flaky-test fix). ✅ `mirror-atomic-swap-review` (adversary, code `9f3cdab`, fc=1→pass) — RE-RUN fixed the flaky R1/R3 (isolated their residue scans under private per-test parents so they no longer race concurrent sibling tests over the shared `coloc_parent()`; assertion intent unchanged). Weakening audit clean; final suite **11** (7 staged + R1–R4: successful-swap cleanup, Linked-never-refused-on-non-CoW, wholesale-replacement, same-FS-rename). Stable 11/11 across 8+3 runs. NO src change (impl `b9bf8bf` was correct). Verify FULL PASS (FINAL_EXIT=0): 11/11 ×3 + workspace green + frozen lock 3/3.
> **▶ NEXT (id-asc): `mirror-linked-fastpath-design` (generic, HUMAN ✋)** — reaches the head now that atomic-swap is done; depends on `mirror-materialize-modes-review` ✓. **PM will ESCALATE via AskUserQuestion** to lock the linked-mode checksum-reuse fast-path design (`.gatesmith/reviews/mirror-linked-fastpath.md`): eligibility predicate, the 4 failure-mode fallbacks, CHECKSUM-ONLY mode/size-fidelity decision, byte-identical-id keystone, strict-verify override, additive/opt-in core lane. Sub-phase B (sync triple) + the hardening/bench/security/docs gates follow.
>
> 🗒️ (history) `mirror-atomic-swap-review` first attempt FAILED fc=1 — flaky NEW R1 test (shared-parent scratch-scan race); fixed in the re-run above, not a src bug. The adversary's weakening audit was clean (+47/−14 = fmt reflow) and it added 4 impl-revealed cases (7→11), but its NEW R1 case `successful_swap_over_existing_dest_leaves_no_old_or_staging_sibling` is RACY: `swap_scratch_siblings` scans the SHARED `coloc_parent()` for generic `.snapdir-staging*`/`.snapdir-dest-old*` prefixes and, under cargo's parallel test execution, caught a CONCURRENT sibling test's transient scratch dir (TOCTOU — message re-scan showed `[]`). PM verdict: impl `b9bf8bf` cleanup is CORRECT (this test's own swap left nothing); R2/R3/R4 + the audit are sound. **NOT reopening `mirror-atomic-swap-impl`.** Working-tree test changes left uncommitted for the fix.
> **▶ NEXT (id-asc, failed-first): `mirror-atomic-swap-review` (adversary, fc=1)** — RE-RUN to FIX the racy R1: scope the scratch-sibling scan to THIS dest's own scratch suffix (the impl tags siblings `.snapdir-dest-old-<tmp>`/`.snapdir-staging-<tmp>` — match only this dest's), OR serialize the residue check via the existing `env_lock`, OR use a per-test unique parent instead of the shared `coloc_parent`. Keep R2/R3/R4 + the audit. Then `cargo test --workspace` + frozen lock green. (`mirror-linked-fastpath-design` ✋ queued; sorts after.)
>
> ✅ `mirror-atomic-swap-spec-tests` (adversary, ledger-only) — BLACK-BOX spec staged at `.gatesmith/pending-tests/mirror_atomic_swap.rs` (8 `#[test]`s). Assumed API: NEW `FileStore::fetch_files_atomic(&Manifest,&Path,MaterializeMode)->Result<(),StoreError>` (staging sibling + atomic rename swap; impl may re-point method NAME only); typed error tolerant of `Backend{message}`/non-`Io` Display naming unsupported/atomic/CoW (stays out of frozen core). Pins: zero-copy (reflink-CoW clonefile_hits advances + Linked symlinks any-FS, no dup); atomic-on-non-CoW (`SNAPDIR_CLONEFILE=0`) = `cow_reflink_supported` false → typed non-panic error, no partial apply, no leftover staging/`dest.old`; held-open fd survives swap (reads ORIGINAL bytes); swap-or-nothing via observable mid-stage failure (missing object) → dest byte-for-byte intact. MODE-AWARE: non-CoW refusal is Auto-only; Linked never refused. ⚠️ FLAGGED: `checkout --delete` switching to atomic-swap when zero-copy is a CLI/router FOLLOW-UP, not this stores triple. Expected un-passable until impl.
> **▶ NEXT (id-asc): `mirror-atomic-swap-impl` (stores)** — implement `fetch_files_atomic` (zero-copy staged swap: reflink/symlink into a sibling staging dir + `rename(dest→dest.old)`+`rename(staging→dest)`+cleanup; HARD-ERROR non-zero-copy atomic for Auto via `cow_reflink_supported`; Linked always allowed; swap-or-nothing). Take over the staged test → green; fmt + clippy `--all-features`; frozen lock. (`mirror-linked-fastpath-design` ✋ still queued, sorts after by id.)
>
> 🏁 **CHECKOUT-CLI TRIPLE COMPLETE** (spec→impl→review). ✅ `mirror-checkout-cli-review` (adversary, code `ee09890`) — weakening audit CLEAN (staged-vs-landed `c30b2e8` = fmt + 2 no-behavior clippy fixes; 15 intact); **trycmd blessed** (help-checkout/pull additive-only). +5 impl-revealed cases (15→**20**): local `--linked` happy path (symlinks→shared object, correct content, 0444 target); `restore_permissions`-skip-in-Linked CONFIRMED correct (object stays 0444, not re-chmod'd — re-chmod would corrupt the shared object); `--linked --delete`; dangerous-dest canonicalization aliases (`$HOME/`, `$HOME/.`, symlink-to-`$HOME` all refuse even with `--force`); `--exclude` regex semantics. NO real bug, NO reopen. Verify FULL PASS (FINAL_EXIT=0): 20/20 + workspace green + frozen lock 3/3.
> **▶ NEXT (id-asc): `mirror-atomic-swap-spec-tests` (adversary)** — opens the atomic-swap triple (stores). BLACK-BOX: on CoW/symlink the staged swap copies ZERO bytes (assert no byte-copy path); atomic where it would DUPLICATE bytes (plain copy non-CoW) = HARD ERROR; a held-open fd keeps reading old bytes across the swap; a simulated mid-stage failure leaves the original dest intact (swap-or-nothing). Will use the `cow_reflink_supported` probe from materialize-modes. (`mirror-linked-fastpath-design` — generic, HUMAN ✋ — still queued; sorts after by id, escalates for sign-off once it reaches the head.)
>
> ✅ `mirror-checkout-cli-impl` (cli, code `495d1c8`) — wired `--delete`+`--exclude`+`--linked` into checkout/pull. New `MirrorArgs` group (`--delete`, `--exclude`) on Checkout/Pull only (disjoint from `WalkArgs::exclude`). `--delete`: lstat dest-walk (paths+types, no hashing, symlinks recorded-not-followed) → `mirror::prune_set` (deepest-first) → remove each (symlinks unlinked w/o following out of dest). `--dryrun` lists + deletes-nothing. dest-absent == plain checkout. Dangerous-dest guard fires FIRST, unconditional (`--force` no bypass), CANONICALIZED vs `/`/`$HOME`/cache/default-cache/`--store`+`--objects-store`. `--exclude` = extended-regex (resolved glob-vs-regex). `--linked` → `fetch_files_with_mode(Linked)` local file:// (skips perm-restore on 0444 symlinks); remote `--linked` = hard error before materialize. 15/15; reuse-only (no new lib API). ⚠️ REVIEW FLAGS: (1) bless 2 regen'd trycmd (help-checkout/pull); (2) local `--linked` happy path not in the 15 (only remote-refusal) — review may add; (3) restore_permissions skipped in Linked mode — confirm. Verify FULL PASS (FINAL_EXIT=0; load normalized ~12 mid-run): build `--locked` + 15/15 + workspace green (1004 passed) + frozen lock 3/3 + fmt + clippy `--all-features`.
> **▶ NEXT (id-asc): `mirror-checkout-cli-review` (adversary)** — weakening audit of `crates/snapdir-cli/tests/mirror_checkout_cli.rs` vs staged (fmt + 2 clippy-fix delta — confirm all 15 intact); **bless the 2 regenerated trycmd** (help-checkout/help-pull); re-assert hard-refusal (no `--force` bypass) + symlink-escape + dryrun + exclude + remote `--linked` refusal; **add a local `--linked` checkout happy-path case** (not in the staged 15) and confirm `restore_permissions`-skip-in-Linked is intended. `cargo test --workspace` + frozen lock green. (Also pickable, sorts later by id: `mirror-linked-fastpath-design` — generic, HUMAN ✋ — escalates when it reaches the head, after the checkout-cli review + atomic-swap gates.)
>
> ✅ `mirror-checkout-cli-spec-tests` (adversary, ledger-only) — BLACK-BOX assert_cmd spec staged at `.gatesmith/pending-tests/mirror_checkout_cli.rs` (15 `#[test]`s). Assumed CLI surface: `--delete` (bool) + `--exclude <PATTERN>` on the transfer family; dest = trailing positional. Pins: extraneous file + nested deepest-first subtree pruned; idempotent; no-op when dest matches; `--dryrun` lists + deletes-nothing; dest-absent == plain checkout; `--exclude` protects matching / still prunes sibling; escaping symlink unlinked-not-followed (nothing outside dest root deleted); HARD-REFUSE (no `--force` bypass) for `/`, `$HOME`, `$SNAPDIR_CACHE_DIR`, default cache, AND store path (non-zero exit + surviving sentinel); CARRIED remote-source `--linked` refusal (s3/gs/b2/ssh/sftp → hard error, no partial dest). Suite SAFE (HOME/cache/store → tempdirs; `/` asserts exit only). ⚠️ FLAGGED for impl: glob-vs-regex (`--exclude`; authored regex, review adjusts if true glob); guard must canonicalize real paths + cover `--store` AND `--objects-store`. Expected un-passable until impl.
> **▶ NEXT (id-asc): `mirror-checkout-cli-impl` (cli)** — wire `--delete` + `--exclude` into `checkout`/`pull` (in-place prune via `snapdir_core::mirror::prune_set`); wire `--linked` → `fetch_files_with_mode(Linked)` + the remote-source hard refusal; canonicalizing dangerous-dest guards (no `--force` escape). Take over the staged test → green; fmt + clippy `--all-features`; frozen lock. NOTE: also pickable is `mirror-linked-fastpath-design` (generic, HUMAN ✋) — sorts after by id; will escalate when it reaches the head.
>
> 🏁 **MATERIALIZE-MODES TRIPLE COMPLETE** (spec→impl→review). ✅ `mirror-materialize-modes-review` (adversary, code `91eba16`) — weakening audit CLEAN (staged-vs-landed = cargo-fmt reflow only; exactly 8 real `#[test]`s intact, none ignored). +5 impl-revealed cases (8→**13**): 0444 re-hardening idempotency (second linked checkout, write still fails), single-hop canonical sharded target (no chain/`..`), `cow_reflink_supported` probe (returns bool, honors `SNAPDIR_CLONEFILE=0`, no residue, false on non-CoW), no-corruption depth (object still BLAKE3-verifies + clean re-fetch after a CoW-break edit). NO real bug, NO reopen. Verify FULL PASS (FINAL_EXIT=0, under external load ~100-140): 13/13 + workspace green + frozen lock 3/3. ⚠️ CLI FOLLOW-UP carried to `mirror-checkout-cli` review: genuine remote-source (S3/GCS/B2/SSH) `--linked` refusal at the router layer.
> **▶ NEXT (id-asc): `mirror-checkout-cli-spec-tests` (adversary)** — opens the checkout-cli triple. BLACK-BOX assert_cmd spec for `checkout`/`pull` `--delete` + `--exclude`: extraneous pruned; idempotent; `--dryrun` lists+deletes-nothing; dest-absent==plain checkout; HARD-REFUSE `/`|`$HOME`|cache|store dests (no `--force` bypass, nothing deleted); symlink-escape removed-not-followed; `--exclude` protects. **MUST ALSO pin the remote-source `--linked` refusal** carried from materialize-modes.
>
> ✅ `mirror-materialize-modes-impl` (stores, code `959ddd6`) — `MaterializeMode{Auto,Linked}` + `FileStore::fetch_files_with_mode` (inherent; Store trait is frozen core). Auto delegates to unchanged `fetch_files` (byte-for-byte). Linked: dirs real, files = `0444`-hardened atomically-created (temp-symlink+rename) symlinks into local `.objects/<sharded>` (zero-copy, idempotent); `harden_object_readonly` chmods only on the linked path → write-through fails PermissionDenied, object intact, push/fetch/clone/flush untouched. Missing local object under `--linked` → typed `StoreError::ObjectNotFound` before any link (no dangling). Added `cow_reflink_supported(dest)` ahead-of-time CoW probe for `mirror-atomic-swap`. Took over staged test → 8/8 (no type re-points; fmt-only). ⚠️ CLI FOLLOW-UP: genuine remote-source (S3/GCS/B2/SSH) `--linked` refusal must land at cli/router (FileStore is local). Verify FULL PASS (FINAL_EXIT=0, ran ~8min under external load avg ~120): build `--locked` + workspace green + frozen lock 3/3 + fmt + clippy `--all-features`.
> **▶ NEXT (id-asc): `mirror-materialize-modes-review` (adversary)** — weakening audit of `crates/snapdir-stores/tests/mirror_materialize_modes.rs` vs staged (fmt-only delta per impl — confirm all 8 intact); re-assert 0444 write-through-fails + no-corruption + reflink-independence + missing-object-no-dangling; add impl-revealed cases (e.g. symlink-chain/canonicalization, 0444 idempotency, the CoW-probe behavior). `cargo test --workspace` + frozen lock green. Then opens the `mirror-checkout-cli` triple — which must ALSO add the remote-source `--linked` refusal flagged here.
>
> ✅ `mirror-materialize-modes-spec-tests` (adversary, ledger-only) — BLACK-BOX spec staged at `.gatesmith/pending-tests/mirror_materialize_modes.rs` (9 `#[test]`s). Assumed API for stores: `MaterializeMode{Auto,Linked}` + `FileStore::fetch_files_with_mode(&Manifest,&Path,MaterializeMode)->Result<(),StoreError>` (Auto == today's `fetch_files` byte-for-byte). Pins: (a) `--linked` → symlinks to local sharded `.objects/<key>`, read-through = source content, zero-copy; (b) objects `0444` → write-through FAILS PermissionDenied + object byte-identical + never re-chmod'd; (c) unresolvable linked source = typed non-panic error, no dangling link; (d) auto reflinks on CoW (gated) else copies, INDEPENDENT editable inode, edit leaves source object byte-identical (clone + `SNAPDIR_CLONEFILE=0` copy paths). ⚠️ FLAGGED: true "remote object source" refusal likely belongs at CLI/router (FileStore is local) — review adds a CLI case; CoW asserts gated like `reflink.rs`. Expected un-passable until impl.
> **▶ NEXT (id-asc): `mirror-materialize-modes-impl` (stores)** — implement `MaterializeMode::Linked` symlink materialization (local objects only) + `0444` object hardening + ahead-of-time CoW probe; hard-error remote/writable-link combos; auto reflink/copy unchanged; take over the staged test → green, fmt + clippy `--all-features`, frozen lock. Then `mirror-materialize-modes-review` (adversary).
>
> 🏁 **PRUNE-SET TRIPLE COMPLETE** (spec→impl→review). ✅ `mirror-prune-set-review` (adversary, code `38631e8`) — weakening audit CLEAN (staged-vs-landed delta = cargo-fmt reflow only; all 25 originals intact, none `#[ignore]`d). +11 impl-revealed cases (25→**36**): regex-metachar excludes (`.` any-char, anchored vs unanchored, invalid-regex→protects-nothing + doesn't suppress a valid sibling), symlink dest entries (classified by path presence; `PathType` is File|Directory, no Symlink variant; never followed), input-permutation + byte-order ordering stability, duplicate-entry no-dedup, both-keys (`./p`+`./p/`) pruned independently. NO real bug, NO reopen. Verify FULL PASS (FINAL_EXIT=0): mirror_prune_set 36/36 + workspace green + frozen lock 3/3.
> **▶ NEXT (id-asc): `mirror-materialize-modes-spec-tests` (adversary)** — opens the materialize-modes triple (stores). BLACK-BOX spec for `--linked` symlinks to LOCAL store objects + `0444` object hardening (write-through fails, store uncorrupted) + `--linked` to NON-LOCAL object source = HARD ERROR + auto reflink-on-CoW (`CopyMethod::Cloned`) vs copy, editable independent inodes. (The new linked-fastpath triple, once enqueued, will depend on this one.)

## ✅ PHASE 31 COMPLETE — 20/20 — Catalog default · files-in-flux robustness · `autocomplete` → snapdir 1.9.0 RELEASED (2026-06-17) — 295/295 total

> 🏁 **snapdir 1.9.0 RELEASED END-TO-END (2026-06-17).** Canonical snapdir/snapdir main @ `0a33b73` = 1.9.0, tag `v1.9.0`, GitHub release live (signed + SLSA), **ALL 6 crates on crates.io @ 1.9.0 — CLEAN publish, no TP 403** (release.yml run 27718183104 SUCCESS). Ships 3 adversarially-verified clusters: catalog default-on + `--catalog none` disable sentinel (fixes `revisions --catalog none`); files-in-flux robustness (SIGBUS handler incl. glibc `__sigsetjmp`, typed `WalkError` variants, two `expect()` panics removed; 10,500-iter fuzz 0 kills/0 panics; ids byte-identical on a static tree); `snapdir autocomplete <shell>`. Frozen format untouched.
> ✅ `phase31-complete` + `release-tag-crates-1.9.0` (HUMAN ✋, operator-authorized 2026-06-17). ✅ `release-pr-upstream-1.9.0` (PR #16 squash-merged → `0a33b73`; all 16 CI green — the one macos/MSRV `adaptive_wire` timing flake passed on re-run). **RELEASE GOTCHA (1.9.0): the pre-push CI mirror caught (a) a `typos` miss — `disabl` substring in catalog_default tests, allowlisted in `_typos.toml`; and bench-verify caught (b) a glibc-only link error — `sigsetjmp` is a macro over `__sigsetjmp` on glibc, fixed by a cfg'd `#[link_name]`. Impl-gate verifications run macOS-only fmt/clippy/test — they should ALSO run `typos` and a glibc/musl link check to catch these at gate time.**

> ✅ `release-pr-upstream-1.9.0` (generic, HUMAN ✋ IRREVERSIBLE, operator-authorized "merge + complete the release", merge `0a33b73`) — assistant opened **PR #16** (release-verify/1.9.0 → snapdir/snapdir), full CI matrix went green on ALL 16 checks; the single Test(1.91.1/macos) failure was the documented `adaptive_wire_healthy_stream_does_not_spuriously_shrink` timing flake (passed on `gh run rerun --failed`, unrelated to Phase-31). Squash-merged → **upstream/main @ `0a33b73` = 1.9.0**, ZERO .gatesmith.
> **▶ NEXT: `release-tag-crates-1.9.0` (packaging, HUMAN ✋, IRREVERSIBLE — assistant-driven per operator authorization).** ⚠️ TP was confirmed working with a CLEAN publish at 1.8.0 (no 403) → TP is attached on all 6 crate pages. Tag `v1.9.0` on `0a33b73` → `release.yml` builds + GitHub release + idempotent 6-crate publish. Then `phase31-complete` ✋.

> ✅ `release-verify-branch-1.9.0` (generic, HUMAN ✋ APPROVED 2026-06-17, artifact `e5fc35d`, fc=1) — after the operator reviewed the 1.9.0 release explainer (`.gatesmith/reviews/snapdir-1.9.0-release.html`). Rebuilt `release-verify/1.9.0` off upstream/main with the sigbus glibc fix + `disabl` typos allowlist (origin `e5fc35d`, ZERO .gatesmith, tip=1.9.0); pre-push CI mirror PASSED; **bench-verify.yml run 27710429139 = SUCCESS on Linux** (`__sigsetjmp` links; determinism + scenarios + benches green). Recovered from the earlier fc=1 link-error fail+reopen.
> **▶ NEXT: `release-pr-upstream-1.9.0` (generic, HUMAN ✋, IRREVERSIBLE — OPERATOR-DRIVEN)** — open PR `release-verify/1.9.0` → snapdir/snapdir (title `release: 1.9.0`, body = CHANGELOG `[1.9.0]`), full CI incl. the required reflink Btrfs FICLONE job, squash-merge. Then `release-tag-crates-1.9.0` ✋ (**TP pre-check on all 6 crate pages!** the 1.7.0 ssh-store 403 lesson) → `phase31-complete` ✋.

> ✅ `flux-impl-core-sigbus` RE-PASSED (code `b779426`, fc=1) — fixed the glibc link: `#[cfg(target_env="gnu")]` binds `__sigsetjmp` via `#[link_name]` (call site unchanged), keeps `sigsetjmp` on musl/macOS/BSD; `siglongjmp` shared. macOS verify green (frozen 3/3, concurrent_mutation 11/11, goldens byte-identical, fmt+clippy `--all-features`); musl cross-build clean. Downstream flux gates unaffected (link-only cfg). **Linux confirmation pending the re-pushed bench-verify.**
> **▶ NEXT (id-asc, failed first): `release-verify-branch-1.9.0` (generic, HUMAN ✋, fc=1)** — RE-ATTEMPT: rebuild `release-verify/1.9.0` off upstream/main with the sigbus fix (it'll be in dev's net diff), force-push to origin, `bench-verify.yml` re-runs on Linux (the real `__sigsetjmp` confirmation). Then operator drives PR ✋ → tag+crates ✋ (TP pre-check all 6) → phase31-complete ✋.

> ⛔ `release-verify-branch-1.9.0` FAILED (fc=1) + 🔁 `flux-impl-core-sigbus` REOPENED (passed→failed, fc=1). Built `release-verify/1.9.0` off upstream/main (`be76b31`, ZERO .gatesmith), pushed to origin; pre-push CI mirror PASSED after a release-mechanical typos allowlist fix (`disabl` — dev `cbd9ade`, folded into the verify commit). BUT **bench-verify.yml run 27708685191 FAILED on Linux**: step 'Determinism + scenarios gate' → `rust-lld: undefined symbol: sigsetjmp`. ROOT CAUSE: `crates/snapdir-core/src/sigbus.rs:70` `extern "C" { fn sigsetjmp }` — bare symbol links on macOS but NOT glibc Linux (it's a macro over `__sigsetjmp` there). The local macOS pre-push couldn't catch it. **LESSON: impl-gate verifications run fmt+clippy+test on macOS only — a glibc-link/typos check would catch these at gate time, not release time.**
> **▶ NEXT (id-asc, failed first): `flux-impl-core-sigbus` (core, fc=1)** — fix `sigbus.rs` Linux linking: `#[cfg(target_env="gnu")]` declare/call `__sigsetjmp(env, savesigs)`, keep `sigsetjmp` on macOS/BSD; `siglongjmp` real on both. Keep behavior + macOS goldens byte-identical. Then re-attempt `release-verify-branch-1.9.0` (rebuild branch with the fix, re-push, bench-verify re-runs on Linux = the real confirmation).

> ✅ `release-prep-1.9.0` (generic, code `8f88366`) — bumped 1.8.0→1.9.0 (5 pins: workspace.package + snapdir-core/-catalog/-stores path-dep pins + flagship snapdir-cli pin) + Cargo.lock. Folded CHANGELOG `[Unreleased]`→`[1.9.0] - 2026-06-17` (Added: `autocomplete <shell>`; Changed: default-on catalog + `--catalog none` sentinel; Fixed: files-in-flux robustness) + fresh `[Unreleased]` + compare-links. No residual 1.8.0 version pins. `cargo build --workspace --locked` + fmt + clippy `--all-features` clean. Gatesmith-free commit.
> **▶ NEXT: `release-verify-branch-1.9.0` (generic, HUMAN ✋)** — PM builds `release-verify/1.9.0` OFF `upstream/main` applying the net Phase-31 code diff (exclude `.gatesmith`/`.claude`/`utils/dx`), tip=1.9.0, ZERO `.gatesmith`; push origin; `bench-verify.yml` green; then escalate the approval. Then operator drives: PR ✋ → tag+crates ✋ (**TP pre-check on all 6 crates!**) → phase31-complete ✋.

> 🏁 **FLUX CLUSTER COMPLETE — ALL 3 FEATURE CLUSTERS DONE** (autocomplete · catalog · flux). ✅ `flux-fuzz-verify` (adversary/HAIKU, independent, ledger-only) — built `target/debug/snapdir` from source; black-box fuzz against the REAL binary: **10,500 outcomes** (10k concurrent-mutation + 500 mid-mmap-truncation stress) → 9938 valid 64-hex ids + 562 clean typed errors + **0 unacceptable** (zero signal-kills, zero panics, zero empty-success). VERDICT: INVARIANT HELD — PASS. The robustness story is independently proven against the shipping binary.
> **▶ NEXT (id-asc): `release-prep-1.9.0` (generic)** — all deps satisfied (catalog-default-review ✓ + flux-fuzz-verify ✓ + autocomplete-review ✓). Bump 1.8.0→1.9.0 (workspace.package + 3 path-dep pins + flagship snapdir-cli pin); fold CHANGELOG `[Unreleased]`→`[1.9.0]` + compare-links; `cargo build --workspace --locked` + fmt + clippy `--all-features`; gatesmith-free code commit. Then verify-branch ✋ → PR ✋ → tag+crates ✋ (TP pre-check!) → phase31-complete ✋.

> ✅ `flux-tests-review` (adversary/sonnet, code `d3d3f77`) — weakening audit: **no weakening** (only stylistic allows + dead-import removal + fmt; fuzz loop NOT ignored, 300×{1,4,None}=900 walks intact). +5 impl-revealed edges (6→**11**): atomic rename-replace mid-hash, dir-deleted-mid-finalize → `TreeStructureChanged` (former panic sites), followed-symlink target-change guard-skip correctness, sub-threshold shrink via size-drift guard, large-file truncation → `FileChangedDuringWalk` not `Io` (`is_mmap_fault` path). `concurrent_mutation` 11/11; `cargo test --workspace` green; no reopen.
> **▶ NEXT (id-asc): `flux-fuzz-verify` (adversary/HAIKU, INDEPENDENT)** — build from source, run a ≥10k-iteration concurrent-mutation fuzz + the mid-mmap truncation repro against the freshly-built binary; prove no panic/no SIGBUS-kill, every outcome valid-id-OR-typed-error → `.gatesmith/evidence/flux-fuzz-verify.log` with a PASS verdict + no PANIC/SIGBUS markers. Closes the flux cluster → then `release-prep-1.9.0` (deps: catalog-default-review ✓ + flux-fuzz-verify + autocomplete-review ✓) → verify-branch ✋ → PR ✋ → tag+crates ✋ → phase31-complete ✋.

> ✅ `flux-impl-cli` (cli, code `baef35d`) — CLI surfacing of the new `WalkError` variants is ALREADY automatic (`id`/`manifest`/`stage`/`push` → `walk_with*` `.with_context("walking <root>")` → `run`'s `eprintln!("{err:#}")` + `ExitCode::FAILURE`; the chain names the file + change kind, non-zero exit). No `cli.rs` change (KEYSTONE: stdout byte-identical). Deliverable = CHANGELOG `[Unreleased] ### Fixed` robustness entry (verified vs core Display strings). Teammate reverted a clap long-help note to avoid touching adversary-owned trycmd goldens. LANE-FENCE-EXC: gate-scoped cli CHANGELOG bullet (diff = `docs/rust-port/CHANGELOG.md` only). `cargo test --workspace` green; fmt + clippy `--all-features` clean.
> **▶ NEXT (id-asc): `flux-tests-review` (adversary/sonnet)** — weakening audit of `concurrent_mutation.rs` vs staged; re-assert the invariant across walk_jobs; add impl-revealed edges (atomic rename-replace mid-hash, dir deleted mid-finalize hitting former panic paths, zero-byte victim); confirm quiescent-control golden + frozen lock. Then `flux-fuzz-verify` (haiku, ≥10k iters, independent) → release-1.9.0 chain.

> ✅ `flux-impl-core-detect` (core, code `6773205`) — design §B+§C. Added `WalkError::{FileVanishedDuringWalk,FileChangedDuringWalk,TreeStructureChanged}` (each names the path). `hash_pending`: ENOENT→Vanished; SIGBUS-guard mmap-fault→Changed (sigbus.rs now uses a downcastable `MmapFault` marker, not a string); bytes-hashed ≠ discovery `FileRecord.size` (size drift, **skipped for followed symlinks**)→Changed; genuine perm/IO→`Io`. Converted the `walk.rs:399 & :653` `expect()` panics → `TreeStructureChanged` (let-else). **Took over the staged test → `concurrent_mutation` 6/6.** 🎯 **SIGBUS handler REAL EXERCISE: mid-mmap-truncation (50×) + 900-walk fuzz ran to completion, NO kill, NO panic** — the single-thread guard held. Frozen lock 3/3; goldens unchanged (ids byte-identical); fmt + clippy `--all-features` clean.
> **▶ NEXT (id-asc): `flux-impl-cli` (cli)** — surface the new `WalkError` variants with actionable messages + non-zero exit (largely automatic via `run`'s `eprintln!("{err:#}")`); add a CHANGELOG `[Unreleased]` robustness entry; document the quiescent-tree model. Then `flux-tests-review` (adversary) → `flux-fuzz-verify` (haiku, ≥10k iters) → the release-1.9.0 chain.

> ✅ `flux-impl-core-sigbus` (core, code `3d6ebf0`) — design §A. New `crates/snapdir-core/src/sigbus.rs` (`#[cfg(unix)]`): lazy `Once` sigaltstack + `SA_SIGINFO` SIGBUS handler that **chains the prior handler** for non-ours faults and `siglongjmp`s out of a guarded mmap-hash region on the faulting thread (thread-local `IN_GUARD` flag + `JMP_TARGET`; async-signal-safe). `hash_file.rs`: both BLAKE3 engines now single-thread `update_mmap` (dropped `update_mmap_rayon` intra-file fan-out) wrapped in `guard_mmap_hash` on unix → truncation = clean `io::Error`, not a kill. Cross-file rayon untouched; **ids byte-identical** (fast_walk 18 / compat_golden 16 / golden_b3sum 8 green); frozen lock 3/3. No new dep. Verify PASS (+ fmt/clippy `--all-features`). Live-fault repro deferred to `flux-fuzz-verify`.
> **▶ NEXT (id-asc): `flux-impl-core-detect` (core)** — add `WalkError::{FileVanishedDuringWalk,FileChangedDuringWalk,TreeStructureChanged}`; in `hash_one` map ENOENT→Vanished, stat-before/after + bytes-streamed-vs-`FileRecord.size` guard (skip symlink byte-check), keep `Io` for genuine faults; convert the `walk.rs:399 & :653` expect() panics. **Takes over the staged `concurrent_mutation.rs`** → green it. Re-verify frozen lock + goldens. Then `flux-impl-cli` → `flux-tests-review` → `flux-fuzz-verify`.

> ✅ `flux-spec-tests` (adversary/sonnet, ledger-only) — BLACK-BOX `concurrent_mutation.rs` staged (6 `#[test]`s; authored from design + `fast_walk.rs` API only): (1) 300iter × `walk_jobs{1,4,None}` fuzz, 5 mutation ops, fixed PRNG → always a `Result`, Ok=64-hex id + CONTROL byte-identical, Err= one of the 4 named `WalkError` variants naming a path; (2) mid-mmap 4MiB truncation SIGBUS regression guard; (3) directory-vanish no-panic; (4) quiescent-control id deterministic; (5) empty/0-byte no-panic; (6) silent-wrong-size oscillation. References the 3 new variants by name (uncompilable until impl). Expected un-passable until impl.
> **▶ NEXT (id-asc): `flux-impl-core-sigbus` (core)** — new `crates/snapdir-core/src/sigbus.rs` (unix; chains prior handler) + switch `update_mmap_rayon`→single-thread `update_mmap` armed by `sigsetjmp` in `hash_file.rs`. Does NOT take over the staged test yet (lands after detect). Re-verify frozen lock + walk/`fast_walk` goldens (ids byte-identical) + fmt/clippy. Then `flux-impl-core-detect` → `flux-impl-cli` → `flux-tests-review` → `flux-fuzz-verify`.

> ✅ `flux-design` (generic, HUMAN ✋ APPROVED 2026-06-17, ledger-only) — locked files-in-flux model in `.gatesmith/reviews/flux-robustness-1.9.0.md`: (A) unix SIGBUS handler (chains prior) + single-thread `update_mmap` (faulting thread == sigsetjmp thread; keeps mmap+cross-file parallelism); (B) stat-before/after + bytes-streamed guard → `WalkError::{FileVanishedDuringWalk,FileChangedDuringWalk,TreeStructureChanged}`; (C) `walk.rs:399 & :653` expect()→errors; (D) fail-fast (defer `--on-change`). Invariant: never silent-kill/panic/silent-wrong-size — valid id OR typed error naming the file. KEYSTONE ids byte-identical on a static tree; frozen format untouched; no new dep.
> **▶ NEXT (id-asc): `flux-spec-tests` (adversary/sonnet)** — `concurrent_mutation.rs`: mutator thread (truncate/grow/delete/replace) racing `walk()` over >256 KiB victims, ≥thousands iters × `walk_jobs {1,4,None}`; invariant = no panic/no SIGBUS-kill, valid-id-OR-typed-error; + mid-mmap truncation repro + quiescent-control golden. Then `flux-impl-core-sigbus` → `flux-impl-core-detect` → `flux-impl-cli` → `flux-tests-review` → `flux-fuzz-verify`, then the release-1.9.0 chain.

> 🏁 **CATALOG CLUSTER COMPLETE** (design→spec→impl→review). ✅ `catalog-default-review` (adversary/sonnet, code `fc2f88c`) — weakening audit: **no weakening** (21 cli edits shape-only); the 3 `--cache-dir` trycmd goldens **blessed**. +7 cases (21→**28**): disabled-msg-on-stderr (revisions/locations/ancestors), `--cache-dir` round-trip, default-catalog locations/ancestors end-to-end, defaults full-path. `cargo test --workspace` all green; no flagship/parity breakage; no reopen.
> **▶ NEXT (id-asc): `flux-design` (generic, HUMAN ✋)** — next tick ESCALATES via AskUserQuestion to lock the files-in-flux model: SIGBUS handler + single-thread `update_mmap`; stat-guards + typed `WalkError` variants; convert the two `expect()` panics; fail-fast. Then the flux impl chain (sigbus → detect → cli → tests-review → fuzz-verify). (autocomplete + catalog clusters DONE; only the flux cluster + the 5-gate release chain remain.)

> ✅ `catalog-default-impl-cli` (cli, code `f4d55a3`) — default-on catalog + `--catalog none` disable sentinel. `catalog_db_path`→`resolve_catalog` returning `CatalogTarget{Enabled(path)|Disabled}` (`none`/empty→Disabled — kills the literal `none-catalog.redb` bug). Unset → `<cache_dir>/default-catalog.redb`: push/stage write, revisions/locations/ancestors read; manifest logs only with explicit `--catalog`, `id` never. Disabled query → "catalog disabled (--catalog none)" stderr + exit 0. `run_defaults` shows catalog+source. **WIRING FIX:** query cmds now honor `--cache-dir`/`SNAPDIR_CACHE_DIR` (added to `CatalogArgs`) — the round-trip blocker. Took over staged test → **21/21**; regenerated 3 `help-{ancestors,locations,revisions}.trycmd` for the additive `--cache-dir` (flagged for adversary). Full verify PASS: build + fmt + clippy `--all-features`; siblings green; KEYSTONE manifest/id byte-identical.
> **▶ NEXT (id-asc): `catalog-default-review` (adversary/sonnet)** — weakening audit of `catalog_default.rs` vs staged; bless/confirm the 3 regenerated trycmd goldens + the `--cache-dir` addition; re-assert all clauses; **run `cargo test --workspace`** (Phase-30 lesson: check flagship `crates/snapdir/tests/parity.rs` for obsolete catalog-shape assertions). Closes the catalog cluster. (`flux-design` ✋ is the other pickable.)

> ✅ `catalog-default-spec-tests` (adversary/sonnet, ledger-only) — BLACK-BOX spec tests staged at `.gatesmith/pending-tests/catalog_default.rs` (21 `#[test]`s; env_clear + isolated HOME/XDG/SNAPDIR_CACHE_DIR per-test). Pins (a) no-flag push+stage → no-flag revisions lists both + `default-catalog.redb` exists; (b) `--catalog none`/empty disables (no `none-catalog.redb`, revisions/locations/ancestors exit 0 with disabled msg); (c) named-catalog isolation both ways; (d) defaults catalog+source; (e) KEYSTONE manifest/id stdout byte-identical; (f) path-like catalog; precedence flag>env>default. Expected un-passable until impl.
> **▶ NEXT (id-asc): `catalog-default-impl-cli` (cli)** — `catalog_db_path()`: `none`/empty→None sentinel; unset→default catalog for push/stage writes + query reads; `run_defaults` catalog+source; clear disabled message in revisions/locations/ancestors. Take over the staged test; green it (+ fmt/clippy --all-features). Then `catalog-default-review`. (`flux-design` ✋ is the other pickable.)

> ✅ `catalog-default-design` (generic, HUMAN ✋ APPROVED 2026-06-17, ledger-only) — locked default-on catalog semantics in `.gatesmith/reviews/catalog-default-1.9.0.md`: `--catalog` unset → default catalog `<cache_dir>/default-catalog.redb`; **push & stage auto-record**; manifest logs only with explicit `--catalog`, `id` never; revisions/locations/ancestors READ the default when unset. `none`/empty = explicit **DISABLE sentinel** (`catalog_db_path`→None; `revisions --catalog none` prints clear "catalog disabled" msg, exit 0). Named/path catalogs unchanged + isolated; precedence flag>env>default. KEYSTONE: manifest/id stdout byte-identical (side-effect only); frozen format + redb schema untouched.
> **▶ NEXT (id-asc): `catalog-default-spec-tests` (adversary/sonnet)** — black-box spec tests → `.gatesmith/pending-tests/catalog_default.rs` pinning (a) no-flag push+stage round-trip via default catalog; (b) `--catalog none` disables + clear msg; (c) named-catalog isolation; (d) defaults source; (e) manifest/id byte-identical; (f) path-like catalog. Then `catalog-default-impl-cli` → `catalog-default-review`. (`flux-design` ✋ is the other pickable.)

> 🏁 **AUTOCOMPLETE CLUSTER COMPLETE** (spec→impl→snapshots→review). ✅ `autocomplete-review` (adversary/sonnet, code `568f6b7`) — weakening audit: **no weakening**; the cli's zsh-source `compinit` setup fix CONFIRMED legit (clap zsh scripts end in `compdef`, which needs `compinit`; empirically original=exit127/fixed=exit0, assertion unchanged). +3 cases (19→**22**): zsh alias byte-identity, `completions` alias hidden from `--help`, unknown-shell error lists EXACTLY the 5 shells. `cargo test -p snapdir-cli` 22/22 + `cargo test --workspace` all green.
> **▶ NEXT (id-asc): `catalog-default-design` (generic, HUMAN ✋)** — next tick ESCALATES via AskUserQuestion to lock the default-on catalog semantics (default catalog for push/stage + query reads; `--catalog none`/empty disable sentinel; clear disabled message). Then `flux-design` ✋, then the catalog + flux impl triples → release-1.9.0 chain.

> ✅ `autocomplete-ci-snapshots` (cli, code `3adb8b1`; **GATE-BUMP** owner ci→cli — trycmd/man are CLI-lane, release.yml is packaging-lane & needs no change via the hidden `completions` alias; pass_criteria unchanged) — added `crates/snapdir-cli/tests/cmd/help-autocomplete.trycmd` (per-command help snapshot, Phase-30 convention) capturing `autocomplete --help` + per-shell wiring. No other trycmd regen needed. Verification PASS: full `cargo test -p snapdir-cli` (cli_surface trycmd incl.) + `snapdir man` renders.
> **▶ NEXT (id-asc): `autocomplete-review` (adversary/sonnet)** — audit `dx_autocomplete.rs` no-weakening (esp. confirm/re-author the cli's zsh-source-test `compinit` setup fix), re-assert every shell emits a sourceable snapdir script, `autocomplete` visible + wiring example, unknown-shell→exit2, hidden `completions` alias works; real-shell sourcing. Closes the autocomplete cluster → then the two design ✋ gates.

> ✅ `autocomplete-impl-cli` (cli, code `cfded0a`) — promoted the hidden `completions` to a VISIBLE `autocomplete <shell>` (`#[command(name="autocomplete", alias="completions")]` — alias clap-hidden so release.yml gen-assets + scripts keep working byte-identical) with long-help wiring examples (`eval "$(snapdir autocomplete bash|zsh)"` + fish/powershell/elvish). Took over the staged test (`git mv` → `crates/snapdir-cli/tests/dx_autocomplete.rs`), **19/19 green**; `help.trycmd` +1 line. Full verification PASS: build `--locked` + fmt + clippy `--all-features -D warnings`. ⚠️ REVIEW FLAG: cli made a harness-shape-only fix to the zsh-source test (`compinit` before `source`, success assertion unchanged) — `autocomplete-review` (adversary) to confirm/re-author.
> **▶ NEXT (id-asc): `autocomplete-ci-snapshots` (ci)** — regenerate the affected `tests/cmd/*.trycmd` + man page; update `release.yml` gen-assets if needed (the hidden `completions` alias keeps it working); actionlint clean. Then `autocomplete-review` (adversary), and the two design ✋ gates (`catalog-default-design`, `flux-design`).

> ✅ `autocomplete-spec-tests` (adversary/sonnet, ledger-only) — BLACK-BOX spec tests staged at `.gatesmith/pending-tests/dx_autocomplete.rs` (19 `#[test]`s). Pins `snapdir autocomplete <shell>`: bash/zsh/fish/powershell/elvish → exit 0 non-empty snapdir script; `autocomplete` VISIBLE in `--help` + a wiring example (`eval`/source) for bash+zsh; unknown shell → exit 2 listing valid shells; hidden `completions` alias byte-identical; real-shell sourcing (`bash -n` always, zsh/fish gated-on-PATH); missing-arg→exit 2; determinism; bash-completion marker. Expected un-passable until impl lands. Fence PASS (test artifacts only).
> **▶ NEXT (id-asc): `autocomplete-impl-cli` (cli)** — promote the hidden `completions` to a VISIBLE `autocomplete <shell>` (keep `completions` as a hidden alias) with per-shell wiring help; take over `dx_autocomplete.rs`; green it. Then the two design ✋ gates (`catalog-default-design`, `flux-design`) + the catalog/flux impl triples → release-1.9.0 chain. (Phase 31 = 20 gates: 3 adversarial-triple clusters + 5-gate release.)

## ✅ PHASE 30 COMPLETE — 37/37 — Adversarial CLI DX/UX review → snapdir 1.8.0 RELEASED (2026-06-16) — 275/275 total

> 🏁 **snapdir 1.8.0 RELEASED END-TO-END (2026-06-16).** Canonical snapdir/snapdir @ 1.8.0 (merge `7f9be38`), tag `v1.8.0`, GitHub release live (signed + SLSA), **ALL 6 crates on crates.io @ 1.8.0** — clean publish, **no TP 403** (the 1.7.0 ssh-store lesson applied: TP pre-checked on all 6 pages before tagging). The adversarial DX review (sealed personas, calibration 3/3, 7 fix triples, independent loop-closer 12/12) shipped: per-command arg validation (BREAKING flags-follow-subcommand; --debug/--paths removed; scoped --help), defaults+source, progress file-%, id-stdin round-trip, nonexistent-store errors, sync unique counts, recovery heal+detect, clearer errors. Frozen format untouched; ids byte-identical.
> ✅ `phase30-complete` + `release-tag-crates-1.8.0` (HUMAN ✋ APPROVED 2026-06-16). ✅ `release-pr-upstream-1.8.0` (HUMAN ✋ APPROVED 2026-06-16, merge `7f9be38`) — PR #15 (release-verify/1.8.0 → snapdir/snapdir) opened by PM, **FULL CI matrix GREEN on ALL 16 checks** (test matrix + loopback-sshd + required Reflink Btrfs FICLONE + static musl + lint/doctests/coverage/bench-iai + supply-chain + cargo-deny + crate-age; adaptive flake did NOT fire). Operator authorized squash-merge → `7f9be38`. upstream/main = 1.8.0, ZERO .gatesmith.
> **▶ NEXT: `release-tag-crates-1.8.0` (packaging, HUMAN ✋, IRREVERSIBLE).** ⚠️ **TP PRE-CHECK on all 6 crate pages BEFORE tagging** (1.7.0 ssh-store 403 lesson). Then tag `v1.8.0` → `release.yml` publishes 6 crates (idempotent). Then `phase30-complete` ✋.
> ✅ `release-verify-branch-1.8.0` (HUMAN ✋ APPROVED 2026-06-16, artifact `fb67069`) — after operator reviewed the full report (`.gatesmith/reviews/phase30-dx-review-report.md`, in Sublime). release-verify/1.8.0 off upstream/main, ZERO .gatesmith, tip 1.8.0, all 7 DX workstreams; pushed origin, **full pre-push CI mirror passed** (after release-mechanical fixes: typos allowlist + `cargo fmt --all` + clippy `--all-features` cleanup, applied to dev too: `54e7ecf`+`ca3de8a`); **bench-verify.yml run 27587056321 = SUCCESS.** `utils/dx/` excluded (dev-only).
> **▶ NEXT: `release-pr-upstream-1.8.0` (HUMAN ✋, IRREVERSIBLE — OPERATOR-DRIVEN).** PR release-verify/1.8.0 → snapdir/snapdir + full CI (incl. reflink Btrfs job) + squash-merge. Then `release-tag-crates-1.8.0` ✋ (**TP pre-check on all 6 crates!** the 1.7.0 ssh-store 403 lesson) → `phase30-complete` ✋.
> ✅ `release-prep-1.8.0` (generic, code `f9788b8`) — bumped 1.7.0→1.8.0 (5 pins: workspace version + 3 path-dep pins + flagship snapdir-cli pin) + Cargo.lock; folded CHANGELOG `[Unreleased]`→`[1.8.0] - 2026-06-16` + fresh `[Unreleased]` + compare-links. No residual 1.7.0 pins; `cargo build --workspace --locked` OK; gatesmith-free (4 files, no .gatesmith). release.yml publishes all 6 crates.
> **▶ NEXT: `release-verify-branch-1.8.0` (HUMAN ✋).** PM builds `release-verify/1.8.0` off upstream/main (net Phase-30 DX code diff, ZERO .gatesmith, tip=1.8.0) + pushes origin + bench-verify, then escalates the approval. Then operator drives: PR ✋ → tag+crates ✋ (**TP pre-check on all 6 crates!**) → phase30-complete ✋.
> 🏁 **FIX PHASE COMPLETE + SIGNED OFF.** ✅ `dx-complete` (generic, code `e08c733`) — CHANGELOG `[Unreleased]` filled (+61) with all 7 DX workstreams (BREAKING flags-follow-subcommand + scoped --help; defaults+source; progress file-%; id-stdin; nonexistent-store errors; sync counts; recovery; clearer errors). Heading kept `[Unreleased]` (release-prep folds → 1.8.0). workspace 0 failed.
> **▶ NEXT: `release-prep-1.8.0` (generic)** — bump 1.7.0→1.8.0 (root Cargo.toml version + crates/snapdir/Cargo.toml snapdir-cli pin) + fold CHANGELOG `[Unreleased]`→`[1.8.0]` + compare-links + `cargo build --workspace --locked`. Then verify-branch ✋ → PR ✋ → tag+crates ✋ (TP pre-check!) → phase30-complete ✋.
> ⚠️ Pre-existing `adaptive::controller_driver_throttle…` timing flake (out of scope) — re-run CI if it trips at verify-branch/PR.
> 🎯 **✅ `dx-fix-verify` PASS — 12/12 RESOLVED.** The independent loop-closer rebuilt the binary and black-box re-verified ALL 12 prioritized findings RESOLVED vs the 1.8.0 build (the 2 help-text gaps now fixed; the other 10 carried forward — isolated changes don't touch them). Strengthened machine-check satisfied (PASS, no NOT-RESOLVED). workspace 0 failed (adaptive flake confirmed flake: 180/180 isolated). **THE DX REVIEW DELIVERED — every finding fixed + independently verified.**
> **▶ NEXT (id-asc): `dx-complete` (generic)** — CHANGELOG `[Unreleased]` entries for all 7 fix workstreams + `cargo test --workspace` sign-off. Then `release-prep-1.8.0` → verify-branch ✋ → PR ✋ → tag+crates ✋ → phase30-complete ✋.
> 🏁 **HELP-TEXT TRIPLE COMPLETE** (both loop-closer gaps resolved). ✅ `dx-helptext-review` (adversary, code `4194b56`) — dx_helptext byte-identical (no weakening); +4 cases (3→**7**) covering all 4 store-URI sites + bare-path-vs-scheme routing. workspace green, lock 3/3. ⚠️ Known pre-existing flake `adaptive::controller_driver_throttle…` (timing) — release-CI re-run risk, not DX-scope.
> **▶ NEXT: `dx-fix-verify` RE-RUNS** (all 7 reviews now satisfied; strengthened verification). Rebuild + black-box re-check all 12 findings → expect 12/12 RESOLVED. Then `dx-complete` (CHANGELOG) → release-prep-1.8.0 → verify-branch ✋ → PR ✋ → tag+crates ✋ → phase30-complete ✋.
> ✅ `dx-helptext-impl-cli` (cli, code `f933ef8`) — clause 1 fixed: `verify --help` now "Verify the integrity of a snapshot in a store (requires --store/--id)" (no "staged"). Regenerated help-verify/help trycmd. dx_helptext **3/3**; workspace 0 failed; lock 3/3. **Both loop-closer gaps resolved.**
> **▶ NEXT (id-asc): `dx-helptext-review` (adversary)** → then **`dx-fix-verify` re-runs** (strengthened verification; expect 12/12) → `dx-complete` (CHANGELOG) → release-prep-1.8.0 → verify-branch ✋ → PR ✋ → tag+crates ✋ → phase30-complete ✋.
> ✅ `dx-helptext-impl-stores` (stores, code `4039237`) — clause 2 fixed: router `InvalidProtocol` error now names the URI form (`file://<path>` local; `<scheme>://…` external helper) instead of bare "invalid store protocol". Covers all store-URI sites. PM repro: bare-path `--store` error names `file://`. stores 0 failed, lock 3/3.
> **▶ NEXT (id-asc): `dx-helptext-impl-cli` (cli)** — clause 1: fix `verify` help (drop "staged", mention store) + take over dx_helptext.rs + green all 3. Then `dx-helptext-review` → **`dx-fix-verify` re-runs** → `dx-complete` → release.
> ✅ `dx-helptext-spec-tests` (adversary, ledger-only) — `.gatesmith/pending-tests/dx_helptext.rs`: 3 tests pinning the 2 loop-closer gaps (verify --help drops "staged" + mentions store; invalid-store-protocol error lists `file://`). **Split (37):** clause 2 confirmed = stores `router.rs:35` `#[error]` → `dx-helptext-impl-stores` (router) before `dx-helptext-impl-cli` (verify-help + green all 3).
> **▶ NEXT (id-asc): `dx-helptext-impl-stores` (stores)** → `dx-helptext-impl-cli` → `dx-helptext-review` → **`dx-fix-verify` re-runs** → `dx-complete` → release.
> ⛔ **`dx-fix-verify` ran → 10/12 RESOLVED, 2 NOT-RESOLVED (NOT PASS).** The independent loop-closer (black-box vs the freshly-built 1.8.0 binary) confirmed all 3 calibration smells + 7 others fixed (progress file-% / defaults+source / arg-hygiene / id-stdin round-trip / silent-empty-store / sync unique-count / recovery heal+detect). But caught **2 accepted findings the errors-spec never pinned**: (1) `verify --help` still says "staged" (checks the STORE; cli.rs:626); (2) `file://` scheme undiscoverable (router.rs:35 error lists no schemes). **Scheduled a help-text triple (33→36)** + strengthened the loop-closer's verification (old grep would falsely pass a NOT-PASS log) + added `dx-helptext-review` to its deps so it re-runs. dx-fix-verify stays pending.
> **▶ NEXT (id-asc): `dx-helptext-spec-tests` (adversary)** → `dx-helptext-impl-cli` → `dx-helptext-review` → **`dx-fix-verify` re-runs** → `dx-complete` → release.
> 🏁 **RECOVERY CLUSTER COMPLETE — ALL SIX FIX WORKSTREAMS DONE** (arg-hygiene, defaults, errors, id-from-stdin, progress, recovery). ✅ `dx-recovery-review` (adversary, code `82e5310`) — dx_recovery byte-identical (no weakening); +6 cases (11→**17**) incl. CACHED-fast-path perf keystone, dedup heal, corrupt-vs-missing. workspace 0 failed, lock 3/3, no reopen.
> **▶ NEXT: `dx-fix-verify` (adversary, INDEPENDENT loop-closer)** — deps ALL 6 reviews now satisfied. Builds the binary + re-runs the calibration set + persona scenarios BLACK-BOX vs the NEW binary, per-finding RESOLVED/NOT-RESOLVED → `.gatesmith/evidence/dx-fix-verify.log`. Then `dx-complete` (CHANGELOG) → release-prep-1.8.0 → verify-branch ✋ → PR ✋ → tag+crates ✋ → phase30-complete ✋.
> ✅ `dx-recovery-impl-cli` (cli, code `b82282f`) — recovery gaps fixed (lane re-route confirmed; no stores deferral): fetch returns CACHED only when manifest + all objects present, else re-fetches missing; verify-cache enumerates + flags missing (`Missing object <hash> for <path>`, nonzero exit); checkout pre-flight names path+hash. **PM repro: delete cache object → verify-cache exit 1 + "…for ./b.txt"; fetch restores it; healthy verify-cache exit 0.** dx_recovery 11/11; workspace 0 failed; lock 3/3.
> **▶ NEXT (id-asc): `dx-recovery-review` (adversary)** — audit + add cases (partial-pool, missing-manifest-vs-object, healthy round-trip). Closes the LAST fix cluster → then **`dx-fix-verify`** (independent loop-closer) → `dx-complete` → 1.8.0 release chain.
> ✅ `dx-recovery-spec-tests` (adversary, ledger-only) — `.gatesmith/pending-tests/dx_recovery.rs`: 11 tests (3 keystone pass / 8 bug-clause fail) pinning: fetch/pull/--force RESTORE a deleted cache object; verify-cache DETECTS missing (nonzero+named); errors name the file path; KEYSTONE healthy round-trip byte-identical. **FIX-LANE PROBED → CLI, not stores** (smoking gun: flush-cache+fetch heals → stores API fine; bug = `run_fetch`/`run_pull` short-circuit on cached manifest + `run_verify_cache` enumeration). **Re-routed (gate-bump):** `dx-recovery-impl-stores`→`dx-recovery-impl-cli` (owner cli, test→snapdir-cli/tests).
> **▶ NEXT (id-asc): `dx-recovery-impl-cli` (cli)** — make fetch/pull verify objects + re-fetch missing; verify-cache flag missing; take over dx_recovery, green all 11. Then `dx-recovery-review`, then **`dx-fix-verify`** (independent loop-closer) → `dx-complete` → release.
> 🏁 **PROGRESS CLUSTER COMPLETE** (spec→impl-core→impl-cli→review) — **the operator's headline finding is fixed.** ✅ `dx-progress-review` (adversary, code `7261f97`) — dx_progress no-weakening; PTY clauses confirmed RUN (13 passed, 0 ignored); +5 cases (8→**13**) incl. exact 2089 file-denominator. **Caught+fixed an over-strict flake in-lane:** the new "reaches ≥99%" capture-ceiling failed 5/5 in isolation (finish() clears the final frame; PTY caps ~98%) → relaxed to `max_done>=total/2`+`max_pct>40` (climb is the guarantee; impl correct, not reopened) → 10/10 deterministic. workspace 0 failed, lock 3/3. **5 fixes done: arg, defaults, errors, id-stdin, progress.**
> **▶ NEXT (id-asc): `dx-recovery-spec-tests` (adversary)** — opens the LAST fix cluster (fetch must restore a MISSING cache object; verify-cache must detect MISSING). Then impl-stores → review → `dx-fix-verify` → `dx-complete` → release.
> ✅ `dx-progress-impl-cli` (cli, code `ce3299d`) — renderer half: `Phase::Discovering`→"discovering N files"; Hashing renders **determinate "NN% done/total files" over the FILE count** (not bytes — the exact bug fixed); synchronous first frame + warm-up cadence so fast trees still show discovery; wired a meter into `id`/`manifest` (reporter.finish() before stdout). dx_progress **8/8 under PTY**; full cli + workspace 0 failed; lock 3/3. **KEYSTONE: id stdout byte-identical progress on/off == ce2b0312…8399.** Progress fix complete (core+cli).
> **▶ NEXT (id-asc): `dx-progress-review` (adversary)** — audit dx_progress no-weakening + re-assert (files-not-bytes, determinate %, discovery visible, keystone determinism) + add cases; runs `SNAPDIR_PTY_TEST=1`. Then the recovery cluster.
> ✅ `dx-progress-impl-core` (core, code `b8cea2b`) — walk/meter half: `Phase::Discovering` + `Meter.objects_discovered` + `MeterSnapshot` field (additive); `walk.rs` threads meter into `discover_dir` (per-file discovery count), starts in Discovering, and **`set_total(pending.len())` + `set_phase(Hashing)` between discovery and hashing** → the hash phase now has a real file-count total. Hot loop untouched; **snapshot-id keystone held** (meter-vs-none golden + fast_walk goldens pass); frozen lock 3/3. ⚠️ Expected: cli renderer won't compile until the next gate wires the new MeterSnapshot field (core gate tests core only).
> **▶ NEXT (id-asc): `dx-progress-impl-cli` (cli)** — `Phase::Discovering` render arm + MeterSnapshot field in renderer/test-helpers + wire a meter into `manifest`/`id` (reporter.finish() before stdout); take over dx_progress.rs; **gate runs `SNAPDIR_PTY_TEST=1 cargo test --test dx_progress`**. Then `dx-progress-review`, then recovery.
> ✅ `dx-progress-spec-tests` (adversary, ledger-only) — `.gatesmith/pending-tests/dx_progress.rs`: 8 PTY tests (openpty + SNAPDIR_PTY_TEST harness from progress_e2e) pinning the headline fix: **denominator is FILES not bytes** (sharpest catch), determinate advancing % (not frozen 0%), visible discovery phase, --no-progress/--quiet suppress, KEYSTONE id/manifest stdout byte-identical + id==ce2b0312…8399, empty-dir no divide-by-zero, stdout clean. PTY clauses skip without `SNAPDIR_PTY_TEST` → **PM gate-bumped** dx-progress-impl-cli + review verification to run `SNAPDIR_PTY_TEST=1 cargo test --test dx_progress` (else they'd pass trivially).
> **▶ NEXT (id-asc): `dx-progress-impl-core` (core)** — add `Phase::Discovering` + `objects_discovered` + `set_total(file count)` before hashing in walk.rs/progress.rs (snapshot-id keystone + frozen lock protected). Then `dx-progress-impl-cli` (renderer + wire id/manifest), `dx-progress-review`. Then the recovery cluster.
> 🏁 **id-from-stdin FIX COMPLETE** (verify→impl→review). ✅ `dx-id-stdin-review` (adversary, code `a5abd66`) — dx_id_stdin byte-identical to staged (no weakening); +6 cases (2→**8**) incl. cwd-independent round-trip, empty/`#`-comment/trailing-newline, malformed→clean error. Pinned the defensible nuance: handler keys on TTY-ness (loud bail only on a real terminal); `/dev/null`/empty-pipe → empty-manifest id, never a cwd walk. workspace 0 failed, frozen lock 3/3. **4 fixes done: arg, defaults, errors, id-stdin.**
> **▶ NEXT (id-asc): `dx-progress-spec-tests` (adversary)** — opens the headline progress cluster (visible discovery + real % instead of stuck-at-0%; touches core+cli, 4 gates). Then `dx-recovery-spec-tests`.
> ✅ `dx-id-stdin-impl-cli` (cli, code `dc6b389`) — `id` no-PATH now reads a manifest from stdin (piped → `Manifest::parse` + frozen `snapshot_id`; TTY → loud error; positional unchanged). **PM repro: from `/tmp`, `manifest <tree> | id` == `id <tree>` == ce2b0312…8399 byte-identical, cwd-independent** — the documented round-trip is now true. Core untouched (frozen lock 3/3); dx_id_stdin 2/2; workspace 0 failed.
> **▶ NEXT (id-asc): `dx-id-stdin-review` (adversary)** — audit + add cases (empty stdin, trailing-newline, `#`-line manifest, malformed→clean error). Then `dx-progress-spec-tests` (the headline progress fix) + `dx-recovery-spec-tests`.
> ✅ `dx-id-stdin-verify` (adversary, ledger-only) — **VERDICT: REAL-BUG.** `snapdir id` with no PATH is documented to read a manifest from stdin but the handler never reads stdin — it silently walks **cwd** (`build_manifest(None)`→`resolve_root(None)`→`current_dir()`). So `manifest <dir> | id` hashes cwd; persona-2's "non-determinism" = cwd changing between runs. Documented stdin feature never implemented (false promise). Proof: `id < man` == `id <cwd>` regardless of stdin content. **Fix triple scheduled (31→33):** `dx-id-stdin-impl-cli` (implement no-PATH stdin read + reuse frozen snapshot_id; no-PATH+TTY→loud error) → `dx-id-stdin-review`; `dx-fix-verify` rewired to gate on the review. Repro staged (`pending-tests/dx_id_stdin.rs`).
> **▶ NEXT (id-asc): `dx-id-stdin-impl-cli` (cli)** — implement the stdin read so `manifest|id` round-trips to `id <dir>`. Then `dx-id-stdin-review`, then the progress + recovery clusters.
> 🏁 **ERRORS CLUSTER COMPLETE** (spec→impl-stores→impl-cli→review). ✅ `dx-errors-review` (adversary, code `755e745`) — dx_errors.rs no-weakening (only a clippy allow); +8 impl-revealed cases (dx_errors 11→**15** + a stores nonexistent-root test) incl. the inverse split-hint branch (hint doesn't fire when `--objects-store` is supplied). workspace 0 failed, no reopen. **3 of the fix clusters done: arg-hygiene, defaults, errors.**
> **▶ NEXT (id-asc): `dx-id-stdin-verify` (adversary)** — investigate the persona-2 `id`-from-stdin non-determinism (REAL-BUG|NOT-A-BUG verdict; stage a repro if real). Then `dx-progress-spec-tests`, `dx-recovery-spec-tests` and their clusters.
> ✅ `dx-errors-impl-cli` (cli, code `15b01d7`) — error hints: (1) missing store → "no store configured: pass --store <uri> or set the SNAPDIR_STORE environment variable" (6 sites); (2) split-store → `split_read_hint` wraps "object not found" with a --objects-store/--from-objects hint (only when no pool supplied). Clauses 3-4 already passed. Took over dx_errors.rs 11/11; **full cli + `cargo test --workspace` 825 passed 0 failed; clippy clean**. Errors-cluster impl done (stores + cli).
> **▶ NEXT (id-asc): `dx-errors-review` (adversary)** — audit dx_errors.rs no-weakening + re-assert all 6 clauses (incl. §6) + add impl-revealed cases. Closes the errors cluster. Then id-stdin-verify, progress, recovery.
> ✅ `dx-errors-impl-stores` (stores, code `8b0a4b2`) — §6 silent-wrong fixes below the CLI: (5) `list_manifest_ids` errors on a nonexistent store root (`try_exists`), existing-empty still valid → diff/sync no longer fabricate a full delta against a typo'd store; (6) `sync_snapshot` dedups by checksum → counters/total reflect UNIQUE objects, skipped=already-in-dest. PM repro-verified: nonexistent→exit1 named, empty→exit0, dedup sync "2 copied, 0 skipped". stores tests 0 failed; frozen lock OK; CLI relays SyncReport (no cli change). ⚠️ Pre-existing flake noted: `adaptive::controller_driver_throttle_drives_gate_decrease` (timing) — de-flake candidate.
> **▶ NEXT (id-asc): `dx-errors-impl-cli` (cli)** — fix clauses 1-4 (store/limit-rate/split/unknown-id hints) + take over `dx_errors.rs` + green ALL 11 (stores half done) + run `--workspace`. Then `dx-errors-review`. Other eligible: id-stdin-verify, progress, recovery.
> ✅ `dx-errors-spec-tests` (adversary, ledger-only) — `.gatesmith/pending-tests/dx_errors.rs`: 11 black-box tests pinning the error/silent-wrong contract (missing-store hint; split-store/--from-objects hint; --limit-rate names accepted forms; unknown-id hint preserved; §6 silent-empty-store nonexistent→error vs existing-empty→valid; §6 sync-miscount unique-object counts + 0-skipped-on-empty). Mixed pass/fail vs current (correct for authoring). **LANE SPLIT (gate-bump, 30→31):** clauses 5-6 are snapdir-stores → new `dx-errors-impl-stores` (fixes 5-6) runs BEFORE `dx-errors-impl-cli` (fixes 1-4 + wires/greens dx_errors.rs); chain = spec→impl-stores→impl-cli→review.
> **▶ NEXT (id-asc): `dx-errors-impl-stores` (stores)** — fix the silent-empty-store resolution + sync miscount in `crates/snapdir-stores/src`. Then `dx-errors-impl-cli`, `dx-errors-review`. Other eligible: id-stdin-verify, progress, recovery.
> 🏁 **DEFAULTS CLUSTER COMPLETE** (spec→impl→review). ✅ `dx-defaults-review` (adversary, code `8cc87ca`) — dx_defaults.rs zero-diff (no weakening); +7 impl-revealed cases (19→**26**); the obsolete `crates/snapdir/tests/parity.rs` (asserted the removed bash-oracle defaults shape) rewritten in-lane to `defaults_prints_effective_config` (NOT a reopen — obsolete test, impl correct); dx_args clippy tidied. **workspace 0 failed, clippy clean.** Lesson logged: cli output changes need a `--workspace` check (the flagship `snapdir` crate carries parity snapshots).
> **▶ NEXT (id-asc): `dx-errors-spec-tests` (adversary)** — opens the errors cluster. Remaining eligible: errors, id-stdin-verify, progress, recovery.
> ✅ `dx-defaults-impl-cli` (cli, code `9c31b95`) — `defaults` rewritten: prints `<knob> <value> source=<flag|env|default>` for all 18 effective knobs + other-env superset; reuses existing resolvers; `DefaultsArgs` group makes it reflect its own `--cache-dir`/`--jobs` (fixes the ignores-own-flags bug) while still rejecting staging flags. Dropped legacy 4-line output. dx_defaults 19/19; **full cli suite green** (also updated defaults_command/ratelimit_select + help-defaults.trycmd). Resolves persona-6's "defaults useless." (Known minor: 7 clippy backtick warnings in dx_args.rs to tidy pre-release.)
> **▶ NEXT (id-asc): `dx-defaults-review` (adversary)** — audit dx_defaults.rs no-weakening + re-assert. Then by id-asc: `dx-errors-spec-tests`, `dx-id-stdin-verify`, `dx-progress-spec-tests`, `dx-recovery-spec-tests`.
> ✅ `dx-defaults-spec-tests` (adversary, ledger-only) — `.gatesmith/pending-tests/dx_defaults.rs`: 19 black-box tests pinning the defaults-rewrite contract (every effective knob + resolved value + source flag|env|default; flag/env override reflected + anti-regression of the "ignores its own --cache-dir" bug; precedence flag>env; legacy SNAPDIR_MANIFEST_* not shown as live; deterministic/greppable). Substance-based (impl keeps format latitude). Expected to FAIL until impl lands.
> **▶ NEXT (id-asc): `dx-defaults-impl-cli` (cli)** — rewrite `run_defaults()` reusing the existing resolvers to print effective config + source; take over dx_defaults.rs; update help-defaults.trycmd. Then `dx-defaults-review`. Other clusters (errors/id-stdin/progress/recovery) follow.
> 🏁 **ARG-HYGIENE CLUSTER COMPLETE** (spec→impl[1 reopen]→snapshots→review). ✅ `dx-arg-review` (adversary, code `376f93e`) — dx_args.rs zero-diff vs staged (no weakening); snapshot-gate edits clean (fast_walk GOLDEN_ID + list_options + catalog_logging keystones preserved); +13 impl-revealed cases (dx_args 20→**33**, all green); `cargo test --workspace` 0 failed. No reopen.
> **▶ NEXT (id-asc): `dx-defaults-spec-tests` (adversary)** — opens the defaults cluster. Remaining eligible spec/investigate gates: defaults, errors, id-stdin-verify, progress, recovery (all dep `dx-findings-signoff` ✅).
> ✅ `dx-arg-snapshots` (cli, code `bd6ce47`) — **full `cargo test -p snapdir-cli` GREEN (0/27 failed)** + man renders, against the fixed binary. 17 trycmd help snapshots regenerated to the scoped per-command help; 4 integration test files adapted (catalog_commands/catalog_logging flag-reposition + id-logs-via-env; list_options global-exclude-now-rejected; fast_walk --walk-jobs retarget + both-knobs id→push, GOLDEN_ID preserved). Fence clean (no src, dx_args untouched).
> **▶ NEXT: `dx-arg-review` (adversary)** — adjudicate the landed dx_args.rs vs staged (no weakening) + re-assert the contract; ALSO glance at the non-feature test edits (fast_walk GOLDEN_ID, list_options semantic). Closes the arg-hygiene cluster. Then the other clusters (defaults/progress/errors/id-stdin/recovery) open up by id-asc.
> ✅ `dx-arg-impl-cli` (cli, code `92653c4`, retry after 1 reopen) — approach-B restructure (`4280b7b`) + the 3 regression fixes (`92653c4`): catalog logging restored (--catalog on TransferArgs + Manifest, merges wired; on TransferArgs only — a dup panics clap), plumbing `--store` via new PlumbingArgs group, `sync --from` SNAPDIR_STORE env restored. **PM independently repro-verified all 3** (catalog db written + `locations` returns it; `objects-needed --store` accepted; sync env fallback fires). dx_args 20/20; build --locked; clippy clean. The reopen rule worked: bug fixed in impl, test never weakened.
> **▶ NEXT: `dx-arg-snapshots` (cli)** — regenerate the 21 trycmd help snapshots + update the ~20 stale integration tests (flags-after-subcommand) **against the now-fixed binary**, make the FULL `cargo test -p snapdir-cli` green + `snapdir man` renders. Then `dx-arg-review` (adversary). — **approach-B restructure landed.** Universal group + per-command-family `#[command(flatten)]` groups; clap natively rejects inapplicable flags (exit2) + scoped per-command --help. Parse-boundary merge → flat Resolved/Ctx keeps valid invocations **byte-identical** (KEYSTONE id unchanged). Folded: `--debug` removed; `--color`→ValueEnum + `--limit-rate` validator (bogus rejected); `manifest --id` rejected; `--paths` removed (was false-promise). dx_args **20/20**; workspace builds --locked; clippy clean. **BREAKING (approach-B inherent, operator-chosen):** global flags must now follow the subcommand. GATE-BUMP: `dx-arg-ci-snapshots`→`dx-arg-snapshots`, owner ci→cli (lane fix).
> **▶ NEXT: `dx-arg-snapshots` (cli)** — must make the FULL `cargo test -p snapdir-cli` green: regen 21 trycmd help snapshots + **update ~20 pre-existing integration tests** (catalog_commands/plumbing/cli_surface/list_options) now red stale-by-design (flags-before-subcommand) + drop the obsolete global-`--exclude`-precedence test + `snapdir man` renders. Then `dx-arg-review` (adversary). Other clusters (defaults/progress/errors/id-stdin/recovery) wait.
> ✅ `dx-arg-spec-tests` (adversary, ledger-only) — `.gatesmith/pending-tests/dx_args.rs`: 20 black-box `#[test]`s (assert_cmd) pinning the approach-B contract (inapplicable flag→exit2 naming flag+cmd; per-command --help scoped; --debug removed; env-inapplicable silent; universal flags everywhere; id byte-identical KEYSTONE; folded: manifest --id/--paths/bogus --limit-rate/--color). Expected to FAIL until impl lands (verified structurally). 
> **▶ NEXT (id-asc): `dx-arg-impl-cli` (cli)** — the approach-B per-command arg restructure (split GlobalArgs into universal + per-command-family groups; rethread ~60 self.globals reads; remove --debug; take over dx_args.rs). Then `dx-arg-ci-snapshots` (regen 21 trycmd + man) → `dx-arg-review`. Other eligible spec/investigate gates wait their turn (defaults/progress/errors/id-stdin/recovery).
> ✅ `dx-findings-signoff` (HUMAN ✋ APPROVED 2026-06-15) — operator reviewed `dx-findings-1.8.0.md` and LOCKED scope. **All 4 clusters confirmed** + **all 4 §6 correctness findings included.** SCOPE EXPANDED 26→30 gates: silent-empty-store + sync-miscount FOLDED into the `dx-errors-*` cluster (descriptions expanded, pass_criteria unchanged); NEW `dx-id-stdin-verify` (adversary investigate — id-from-stdin non-determinism, REAL-BUG|NOT-A-BUG verdict, fix-if-real); NEW recovery triple `dx-recovery-spec-tests`→`-impl-stores`→`-review` (fetch must restore a MISSING cache object; verify-cache must detect missing). `dx-fix-verify.depends_on` += the two new chains (GATE-BUMP, pass_criteria unchanged).
> **▶ DISCOVERY DONE. Fix phase begins.** 6 eligible spec/investigate gates now unblocked (all dep `dx-findings-signoff` ✅): `dx-arg-spec-tests`, `dx-defaults-spec-tests`, `dx-errors-spec-tests`, `dx-id-stdin-verify`, `dx-progress-spec-tests`, `dx-recovery-spec-tests`. **▶ NEXT (id-asc): `dx-arg-spec-tests` (adversary).** Adversarial triples run one gate/tick: spec(adversary) → impl(lane) → review(adversary).
> ✅ `dx-judge-synthesis` (PM-judge, ledger-only) — arg-matrix + 6 persona reports synthesized → `.gatesmith/reviews/dx-findings-1.8.0.md` (174 ln; deduped, categorized, severity-ranked + calibration section). **Calibration 3/3 → harness genuinely adversarial.** Findings → 4 planned clusters + a **NEW §6** (correctness beyond DX, for operator decision): id-from-stdin non-determinism (BLOCKER?, verify-first), silent-empty-store→fabricated diff (CI-hostile), recovery gaps (fetch won't restore missing / verify-cache blind to missing), sync miscount. Judge rec: keep 4 clusters; fold manifest-`--id`/`--paths`/`--linked`/verify-help in; ADD silent-empty-store + sync-miscount; verify-then-decide id-from-stdin; consider-defer recovery gaps.
> **▶ NEXT: `dx-findings-signoff` — HUMAN ✋.** Next tick ESCALATES via AskUserQuestion: operator reviews `.gatesmith/reviews/dx-findings-1.8.0.md` and LOCKS the 1.8.0 fix scope. That unblocks the 4 fix-cluster triples (all dep `dx-findings-signoff`).
> ✅ `dx-qa-personas` (adversary, ledger-only) — 6 SEALED independent personas (parallel, black-box, isolated, NOT told the smells) filed friction reports → `.gatesmith/evidence/dx-personas/`. **🎯 UNBIASED HARNESS VALIDATED — calibration 3/3:** progress smell HIT (persona-3: "% frozen at 0%, denominator is byte count, bar never fills"), defaults smell HIT (persona-6: "prints just 4 env vars, none of the real knobs"), flag smell HIT (persona-3: --debug<--verbose; persona-1: --verbose inconsistent). **NEW serious findings:** `id` from stdin manifest NON-deterministic (BLOCKER); typo'd `--to`/`--store` silently treated as EMPTY → fabricated diff exit 0 (CI-hostile); `manifest --id` silently ignores --id, re-walks cwd; `verify --help` says "staged" but checks the STORE; fetch/pull won't restore a missing cache object; verify-cache blind to MISSING objects; sync miscounts file-refs as objects + "1976 skipped" into empty dest; integrity errors name hash not path; silent success on pull/checkout/verify; `file://` scheme never in help; `--catalog` accepts any string.
> **▶ NEXT: `dx-judge-synthesis` (generic = PM judge)** — deps `dx-arg-matrix-audit` ✅ + `dx-qa-personas` ✅ now satisfied. Dedupe + categorize + severity-rank the matrix + 6 persona reports into `.gatesmith/reviews/dx-findings-1.8.0.md` with the formal calibration-coverage section. Then `dx-findings-signoff` (human ✋ — operator locks scope).
> ✅ `dx-arg-matrix-audit` (adversary, ledger-only) — black-box (command×flag) audit of installed 1.7.0 vs sandbox → `.gatesmith/reviews/dx-arg-matrix.md` (197 ln, ~48 cells: ~16 EFFECTIVE / ~7 REJECTED / dozens SILENT-NOOP). Root cause confirmed: clap flattens ~28 global flags onto every subcommand. **NEW smells beyond the operator's 3:** `--paths` total no-op on manifest/id; `checkout --linked` yields a plain file (help says symlinks); `defaults` ignores its own `--cache-dir`; validation inconsistency (`--limit-rate`/`--color` accept bogus, `--jobs`/`--adaptive` reject); `fetch --dryrun` silent. Known smells reproduced: `--debug` ghost (0 effect anywhere), `--verbose` only 4/16 commands.
> ✅ `dx-sandbox-setup` (generic, code `80896de`) — reproducible QA sandbox: `utils/dx/build-sandbox.sh` (deterministic pure-shell, idempotent, no compile) materializes `.gatesmith/evidence/dx-sandbox/{tree,store,objects-store}` — tree = 2089 files / 43 dirs / 29 MB (many-small, 3×8MB large, deep-12, wide-64, dup-content, empties, excludable node_modules/+skip/). `snapdir id` (installed 1.7.0) = ce2b0312…8399, STABLE across rebuilds. Committed catalog `.gatesmith/reviews/dx-scenario-catalog.md` = 6 GOAL-ONLY personas + black-box rules; bias audit CLEAN.
> **▶ NEXT: `dx-qa-personas` (adversary)** — the last discovery gate before the judge. PM spawns ~6 SEALED persona teammates (black-box, no source, NOT told the smells). Then `dx-judge-synthesis` (deps arg-matrix ✅ + personas).

> 🎯 **Goal:** an UNBIASED adversarial DX/UX audit of the CLI, judged into a findings report, then a 1.8.0 minor that fixes it. Plan: `.claude/plans/...greedy-stearns.md` (top "Phase 30"). 26 gates: (A) discovery — `dx-sandbox-setup` (reuse `benches` Scenario::materialize) → `dx-arg-matrix-audit` + `dx-qa-personas` (~6 SEALED personas, black-box vs installed 1.7.0, NO source, NOT told the known smells) → `dx-judge-synthesis` (PM judges + CALIBRATION-COVERAGE check: did personas independently rediscover progress-0% / defaults-cruft / ghost-flags?) → `dx-findings-signoff` (human ✋ — operator LOCKS fix scope). (B) fixes — 4 adversarial-triple clusters (all dep `dx-findings-signoff`): arg-hygiene **approach B** (per-command arg groups; clap natively rejects inapplicable flags + per-command --help; remove `--debug` ghost; regen 21 trycmd + man), `defaults` rewrite (effective config + resolved value + source), progress observability (visible discovery + real % via `set_total` before hashing; `Phase::Discovering`), error hints; then `dx-fix-verify` (INDEPENDENT — build + re-run calibration black-box vs the NEW binary) → `dx-complete`. (C) release — `release-prep-1.8.0` → verify-branch ✋ → PR ✋ → tag+crates ✋ (**TP PRE-CHECK on all 6 pages BEFORE tagging — the 1.7.0 ssh-store 403 lesson**) → `phase30-complete` ✋.
> **KEYSTONE:** frozen manifest format untouched; snapshot ids BYTE-IDENTICAL (progress + arg changes are output-orthogonal). Discovery = black-box, no compile; fixes build from source.
> **Operator decisions (2026-06-15):** arg fix = approach B (full per-command restructure); all 4 clusters in scope (re-lockable at `dx-findings-signoff`).
> **▶ NEXT: `dx-sandbox-setup` (generic)** — first eligible gate. Run `/gatesmith:loop` to drive Phase 30. Discovery artifacts: tracked `.gatesmith/reviews/` (dev-only); raw persona logs + fix-verify log: gitignored `.gatesmith/evidence/`.

## ✅ PHASE 29 COMPLETE — 37/37 — RELEASE 1.7.0 + perf (operator-requested 2026-06-12; SHIPPED 2026-06-15) — 238/238 total

> 🏁 **snapdir 1.7.0 RELEASED END-TO-END (2026-06-15).** Canonical snapdir/snapdir @ 1.7.0 (merge `a477489`), tag `v1.7.0`, GitHub release live (notes = CHANGELOG [1.7.0], signed archives + SHA256SUMS), **ALL 6 crates on crates.io @ 1.7.0** (snapdir-core, snapdir-catalog, snapdir-stores, snapdir-ssh-store, snapdir-cli, snapdir). Ships Phase-28 (--objects-store split, `snapdir diff`, sync --from/--to-objects), Phase-27 (SNAPPACK 1Z zstd + receive-pack durability; SNAPDIR_FSYNC=batch default, ~20% small-files cost accepted+documented, SNAPDIR_FSYNC=off hatch), and the Phase-29 PERF work: parallel/mmap walk (--walk-jobs, ~7.6× id), cross-platform CoW (macOS clonefile + Linux FICLONE reflink, SNAPDIR_CLONEFILE=0), clone-path re-hash elision (SNAPDIR_VERIFY_COPIES=1; stage 2.38×/checkout 1.51×). Frozen format untouched; ids byte-identical; default behavior unchanged when new flags unset.
> 📌 **TP-scope blocker RESOLVED:** the crates.io publish initially 403'd on `snapdir-ssh-store` (Trusted Publishing not valid for the workflow — the 1.6.0 "confirm TP attached" follow-up). Operator added TP (repo=snapdir/snapdir, workflow=release.yml, no env) for snapdir-ssh-store + verified snapdir-cli/snapdir; PM re-ran `gh run rerun 27518493078 --failed` → idempotent skip-guard published the 3 remaining. **LESSON for next release: verify TP is attached for ALL 6 crate pages BEFORE tagging** (memory [[release-crates-io-state]] updated).

> ✅ `release-pr-upstream-1.7.0` (generic, HUMAN ✋ APPROVED 2026-06-15, merge `a477489`) — PR #14 (release-verify/1.7.0 → snapdir/snapdir, "release: 1.7.0") opened by the PM at operator's "do it". **FULL CI matrix GREEN on ALL 17 checks** incl. the NEW required **Reflink (Btrfs FICLONE)** job — FICLONE genuinely FIRED on the GH-runner loopback Btrfs (clone_skip + clone_skip_race ran on REAL reflink, not just ext4 fallback); the continue-on-error fallback was NOT needed. Also: Test stable/beta/1.91.1 × ubuntu+macos (loopback-sshd), Static musl debug+release (zstd-sys), Lint/Doctests/Coverage, Supply chain, cargo-deny, Crate age ≥3d, Bench(iai). Operator authorized squash-merge → merge commit a4774898 (short `a477489`). VERIFIED: upstream/main Cargo.toml version = "1.7.0", ZERO .gatesmith tracked (count=0). **▶ NEXT: `release-tag-crates-1.7.0` (packaging, HUMAN ✋, IRREVERSIBLE — tag v1.7.0 + 6-crate crates.io publish via release.yml).**

> ✅ `release-verify-branch-1.7.0` (generic, HUMAN ✋ APPROVED 2026-06-15, artifact `ddb710f`) — release-verify/1.7.0 built off upstream/main (8baefa9) as the squashed NET CODE DIFF of Phases 27+28+29 + release-prep on top; tip = version 1.7.0. VERIFIED: ZERO .gatesmith/.claude tracked (count=0); CHANGELOG [1.7.0]; all four perf features present (fast-walk SNAPDIR_WALK_JOBS, clonefile, clone-skip copy_guard.rs, Linux reflink try_reflink + reflink.rs) + the required reflink Btrfs CI job; `cargo build --workspace --locked` exit 0. **Pushed to origin (bermi/snapdir)** — the local pre-push hook ran the FULL ci.yaml mirror and reported "All checks passed. Safe to push." after ONE fix: typos flagged "mis-addressed" (mis- prefix split on hyphen) as a false positive → allowlisted `mis = "mis"` in _typos.toml on BOTH dev (commit 3334492) + the release-verify branch (folded into ddb710f) so upstream CI typos passes. **bench-verify.yml run 27517145743 = SUCCESS** (~9 min; determinism + iai + criterion). Operator approved via AskUserQuestion ("Approve — gate passed").
> **▶ NEXT: `release-pr-upstream-1.7.0` (generic, HUMAN ✋, IRREVERSIBLE — OPERATOR-DRIVEN).** Per the "I drive verify-branch, you do upstream" partition the PM PAUSES here. The operator opens PR release-verify/1.7.0 → snapdir/snapdir (title "release: 1.7.0", body = CHANGELOG [1.7.0]), waits for the FULL CI matrix incl. the NEW required `reflink` Btrfs-loopback FICLONE job (first GH-runner run; continue-on-error fallback documented if loopback flaky), squash-merges → upstream/main at 1.7.0. Then `release-tag-crates-1.7.0` ✋ (tag v1.7.0 + 6-crate crates.io publish via release.yml) → `phase29-complete` ✋. The PM records each gate as the operator confirms it landed.

> ⚙️ **PROCESS NOTE (cfg(linux) test gates):** native macOS clippy compiles a `#![cfg(target_os="linux")]` test to nothing, and a lib-only musl clippy misses test lints — so a Linux-target-only `clippy::pedantic` violation can slip impl gates and only fail CI's ubuntu `clippy --workspace --all-targets -D warnings`. `reflink-tests-review` caught 6 such in reflink.rs. Going forward, cfg(linux) test gates should run `cargo clippy --target x86_64-unknown-linux-musl --all-targets -D warnings` (zig wrapper) locally.

> 🛠️ **PM tooling note (reflink Linux cross-check from macOS):** `cargo check -p snapdir-stores --target x86_64-unknown-linux-musl --tests` needs a zig-cc wrapper because `ring`'s C/asm can't cross-compile from macOS with the default toolchain AND zig rejects cc-rs's `--target=x86_64-unknown-linux-musl` (vendor field → `UnknownOperatingSystem`). Fix: a wrapper `/tmp/zigcc` translating `--target=x86_64-unknown-linux-musl` → `zig cc -target x86_64-linux-musl`; export `CC_x86_64_unknown_linux_musl`/`AR_…`/`CARGO_TARGET_…_LINKER`. CI compiles natively on ubuntu (no zig). Reuse this for any future Linux cross-check tick.

> 🆕 **reflink chain inserted (2026-06-13, operator-prioritized, BLOCKS 1.7.0) — ledger 231→238.** Linux FICLONE copy-on-write → 1.7.0's CoW story goes **cross-platform** (macOS clonefile + Linux reflink). Drops into `copy_file` as a `#[cfg(target_os="linux")]` branch; **simpler than macOS** (FICLONE clones data-only → no BSD-flag/uchg hazard; `chattr +i` not cloned), **no new dep** (`libc::FICLONE`), and **inherits the clone-skip stage/checkout win for free** (persist/CopyTrust/CopyGuard are platform-agnostic). Counter/knob = option B (reuse `SNAPDIR_CLONEFILE` + `clonefile_hits()`, doc-generalized). **CI (operator: local-VM + ext4 + Btrfs):** existing matrix covers the ext4 fallback; a NEW required `reflink` job creates a loopback Btrfs image (`sudo mkfs.btrfs` + `mount -o loop`; no special runner — loopback-sshd precedent) and runs reflink+clone_skip+clone_skip_race on it with `TMPDIR=/mnt/reflink` + `SNAPDIR_REFLINK_TEST_REQUIRE=1` so the suites hit REAL FICLONE; plus `utils/ci/reflink-vm-check.sh` pre-flight. 7 gates (decoupled author≠coder≠verifier): `reflink-spec-tests`(adversary) → `-stores`(stores) → `-ci-leg`(ci) → `-tests-review`(adversary) → `-race-verify`(adversary, real-reflink TOCTOU + chattr+i/EXDEV) → `-bench` → `-complete`. Chain off `clone-skip-complete`; **`release-prep-1.7.0` now also deps `reflink-complete`**. Plan: `.claude/plans/...greedy-stearns.md` (Follow-up 3).

> ⚠️ **DESIGN CORRECTION @ `clone-skip-stores` (PM-accepted, no weakening):** the plan claimed *fetch* clone-skip was "provably safe (immutable verified object)" — that's WRONG. A store object can be CORRUPT on disk, and `fetch_files`' re-hash is what catches it (pinned by phase-28 adversary `split.rs` corruption tests). So `TrustedObject` (fetch) was implemented as **verify-source-once then skip the redundant *temp* re-hash** — corruption detection preserved, all existing tests green, but **fetch wall-clock is NEUTRAL** vs clone-without-skip. **The real clone-skip win is STAGE** (StatGuarded ⇒ zero reads at persist; the walk already hashed). The literal "zero-read fetch skip" is REJECTED (would weaken checkout corruption detection + break phase-28 tests → would need explicit human approval; recommended AGAINST). `clone-skip-bench` should therefore expect **stage win + fetch neutral** (honest).

> 🆕 **clone-skip chain inserted (2026-06-13, operator-prioritized, BLOCKS 1.7.0) — ledger 223→231.** Makes the clonefile fast-path actually FASTER by skipping `persist()`'s redundant post-copy re-hash on the clone path (the apfs-clone ~1.0× was the re-hash = a 2nd full read). **Fetch/checkout** skip = unconditionally safe (immutable read-verified object). **Stage/push** skip = stat-validated trust (walk records `(size,mtime,ctime,ino)`; `persist` re-stats at clone time, skips iff unchanged, else re-hashes). Honest posture: stat-guard catches the benign mid-stage race at write-time; an adversarial mtime+ctime+size forge is caught at READ-time (`get_object` BLAKE3-verify) — never silent corruption. APFS-snapshot "freeze the source" = deferred opt-in. Low-ripple: frozen `Store` trait untouched (stage guards = FileStore state set by CLI), `merkle.rs`/`manifest.rs` stay frozen-lock-identical, manifest text format never changes. 8 gates, decoupled (author≠coder≠verifier): `clone-skip-spec-tests`(adversary) → `-walkstat-core`(core, `CopyGuard`+walk side-channel) → `-stores`(`CopyMethod`+persist trust-enum skip+`SNAPDIR_VERIFY_COPIES=1` strict) → `-cli`(run_stage guard plumbing) → `-tests-review`(adversary) → `-race-verify`(adversary, INDEPENDENT TOCTOU/concurrency/`touch -t`/inode-reuse fuzz proving no silent mis-addressed object) → `-bench`(stage+checkout before/after — the win must finally show) → `-complete`(generic CHANGELOG+sign-off). Chain deps off `apfs-clone-complete`; **`release-prep-1.7.0` now also deps `clone-skip-complete`**. Plan: `.claude/plans/what-s-our-std-fs-read-concurrency-greedy-stearns.md` (Follow-up section).

> 🆕 **Two perf gate-chains inserted (2026-06-13, operator-requested) — ledger 212→223.** (A) **fast-walk** (7 gates, **release-BLOCKING** — `release-prep-1.7.0` now also deps `fast-walk-complete`): parallel + mmap walk/hash so `snapdir id`/manifest/stage/push stop single-threaded `fs::read`+one-shot-hashing every file. Triple-shaped: `fast-walk-spec-tests`(adversary ✅) → `fast-walk-core-hash` → `fast-walk-core-parallel` (core) → `fast-walk-jobs-cli` (cli, `--walk-jobs`/`$SNAPDIR_WALK_JOBS`) → `fast-walk-tests-review` (adversary) → `fast-walk-bench` → `fast-walk-complete` (generic). **KEYSTONE:** snapshot ids BYTE-IDENTICAL (determinism is structural — `directory_checksum` sort+dedup + `manifest.sort()`). **Frozen-safe:** `fast-walk-core-hash` routes the file-hash trait into a NEW `snapdir-core` module (`HashFile` in `src/hash_file.rs`) so `merkle.rs` (sha-locked) stays byte-identical — the gate verification re-runs `shasum -c` the lock. (B) **apfs-clone** (4 gates, **NON-BLOCKING** — deps `fast-walk-complete`, NOT in `release-prep` deps): macOS APFS `clonefile` CoW fast-path in `file_store.rs` `copy_file()` for stage/push/checkout (raw libc, no new dep, errno fallback). Rides 1.7.0 if green before the release ✋; else 1.8.0. Plan: `.claude/plans/what-s-our-std-fs-read-concurrency-greedy-stearns.md`.
> ✅ `fast-walk-spec-tests` (adversary, code `4c6107a`) — staged `fast_walk_core.rs` (13 #[test]+8 proptest) + `fast_walk_cli.rs`; goldens captured from current binary on deterministic bench-Shape fixtures (location-independent).
> ✅ `fast-walk-core-hash` (core, code `9af911c`) — NEW `src/hash_file.rs`: `HashFile` trait + `MMAP_THRESHOLD=256KiB` + 4 impls (Blake3 `update_mmap_rayon`≥thr / plain-read below / empty never mmap; Md5/Sha256 read+hash_hex). blake3 `mmap`+`rayon` features; `WalkOptions.walk_jobs` field (INERT — parallelism is the next gate); walk hashes via `hash_file_hex`. All 6 per-Shape goldens BYTE-IDENTICAL; fast_walk 13/13; frozen `merkle.rs`/`manifest.rs` untouched (`shasum -c` OK). ⚠️ AUDIT @ `fast-walk-tests-review`: +38-line shape-edit on the moved test (`#![allow]` pedantic + a `..Default::default()` spread) — coder claims no weakening. ⚠️ `Blake3KeyedHasher` is read+hash_hex (no mmap) — derive-key context is private in frozen merkle.rs (byte-identical, keyed loses only mmap speedup). ⚠️ workspace build now BREAKS at cli (cli.rs ~2124 constructs `WalkOptions` with explicit fields) until `fast-walk-jobs-cli` adds `walk_jobs` — no in-between gate builds the workspace.
> ✅ `fast-walk-core-parallel` (core, code `a29b197`) — `discover_dir` enumerates single-threaded + defers hashing; `hash_pending` `par_iter` inside a scoped `pool.install()` sized to `walk_jobs` (`Some(n)`=n, `Some(0)`/`None`=`available_parallelism` clamped [1,16], `Some(1)`=no-pool); writes into fixed `FileRecord` slots → byte-identical id; Meter in the parallel closure (atomic). Oversubscription guard: `pending>=jobs` → per-file single-thread `update_mmap` (new `hash_file_hex_seq`), else blake3-rayon. `rayon=1.12.0` direct dep (matches lock). fast_walk 13/13 incl. genuine 8-thread `walk_jobs=8` == `Some(1)` id; frozen lock OK; `tests/fast_walk.rs` untouched.
> ✅ `fast-walk-jobs-cli` (cli, code `6bc4625`) — global `--walk-jobs <N>` + `SNAPDIR_WALK_JOBS` (separate from transfer `--jobs`); plumbed `walk_jobs` into the single `WalkOptions{}` at cli.rs:2134 (shared `build_manifest` → id/manifest/stage/push). **Workspace build GREEN again** (widened the cli `walk_with` wrapper bound to `Hasher+HashFile+Sync`); 17 `help*.trycmd` snapshots refreshed; cli fast_walk 5/5 (id byte-identical across `--walk-jobs 1/8/env/unset`); full cli suite green. ⚠️ AUDIT @ review: moved `tests/fast_walk.rs` shows a 14-line delta vs staged (coder: fmt whitespace).
> ✅ `fast-walk-tests-review` (adversary, code `cfec626`) — **NO weakening** in either landed suite (core `#![allow]`+fmt deltas and cli 14-line delta both audited legitimate; every load-bearing assertion/golden/`walk_jobs` value intact). Added **5 impl-revealed core cases** (+268 ln): single-huge-file intra-file `update_mmap_rayon`==seq id; many-files cross-file rayon + per-file `hash_file_hex_seq` determinism; `hash_file_hex_seq`==rayon==one-shot blake3 across the threshold; `walk_jobs` `Some(0)`==`Some(1)`; followed-symlink hashes target content. **No real bug** (no impl gate reopened). core fast_walk **18/18**, cli 5/5, `cargo test --workspace` exit 0, fmt+clippy -D clean. **🏁 fast-walk feature TRIPLE COMPLETE** (spec→impl×3→review).
> ✅ `fast-walk-bench` (bench, code `44fee8e`, evidence `fast-walk-bench.log`) — **before/after quantified**: criterion `bench_walk` seq(walk_jobs=1) vs parallel — many_small **2.64×** (177→67ms), few_large **7.01×** (24.3→3.5ms); end-to-end `snapdir id` on a generic synthetic large dir (~2.8 GiB, warm cache, 12-core) **~7.6×** (2.39s→0.31s) with **byte-identical snapshot id**. No regression; bench-only. 📌 PM-NOTE for release: the `snapdir` BINARY builds from package `snapdir` (`-p snapdir`), not `snapdir-cli` (now a lib).
> ✅ `fast-walk-complete` (generic, code `13feacc`) — CHANGELOG `[Unreleased]` ### Added bullet for `--walk-jobs`/`$SNAPDIR_WALK_JOBS` parallel mmap walk (gatesmith-free, cherry-pickable; heading kept `[Unreleased]` for `release-prep` to fold). `cargo test --workspace` exit 0 (all green). **🏁 FAST-WALK FEATURE FULLY COMPLETE** (spec→impl×3→review→bench→changelog; ~7.6× `id`, ids byte-identical). `release-prep-1.7.0`'s blocking dep on `fast-walk-complete` is now SATISFIED — it remains gated only on the release ✋ `release-perf-linux-1.7.0`.
> ✅ `apfs-clone-spec-tests` (adversary, code `452774b`) — staged `.gatesmith/pending-tests/apfs_clone.rs` (9 black-box tests, attestation YES). Contracted for the impl: **`snapdir_stores::clonefile_hits() -> u64`** (pub, process-global AtomicU64, re-exported at crate root) + **`SNAPDIR_CLONEFILE=0`** forces `fs::copy`. Pins clone-vs-copy object pools byte+filename identical, perms parity, `uchg`-source stays removable, cross-volume EXDEV fallback (skip-if-unavailable), counter fires on same-volume APFS / not under `=0`, 0-byte + read-only + dup-content. Env isolation via `ENV_LOCK` mutex (impl must read the knob PER-COPY, not cache).
> ✅ `apfs-clone-stores` (stores, code `709d998`) — `copy_file()` macOS `clonefile` fast-path: non-macOS/`SNAPDIR_CLONEFILE=0`→`fs::copy`; macOS+enabled→`try_clonefile` (errno fallback ENOTSUP/EXDEV/ENOSYS/EINVAL/EOPNOTSUPP/EEXIST). Parity: `chflags(0)` **before** chmod (load-bearing — caught a REAL bug: `uchg` source clones immutable → chmod EPERM unless flags cleared first). Knob read PER-COPY; `pub clonefile_hits()` re-exported at crate root. Clone genuinely fired on this APFS host (counter test passed, uchg + cross-volume EXDEV ran). apfs_clone 8/8 (macOS), whole stores green, frozen lock OK. ⚠️ **REVIEW MUST SCRUTINIZE @ next gate**: coder modified 2 assertions in the adversary suite (claimed test-bugs: a sorted-vs-unsorted zip; a sharding-path `contains` that ignored the `/` between shard segments → `object_path()`) — adversary must independently adjudicate these are legit test-bug-fixes, not impl-masking weakening.
> ✅ `apfs-clone-tests-review` (adversary, code `4e80167`) — both coder assertion edits **adjudicated LEGIT-FIX independently** (decisive: each fails identically under plain `fs::copy` → impl-independent test-bugs, not clone-masking — (a) sorted-vs-unsorted zip; (b) `big_sum[6..]` `contains` ignored the `/` sharding inserts → `object_path()`, strictly stronger). `#![allow]` = style lints only (no correctness masked). No weakening/deletion. **+4 impl-revealed cases**: large-file CoW determinism (sharded path+bytes==`fs::copy`), `clonefile_hits()` precision (+N push / +0 under `=0`), fetch-direction restore, xattr non-leak. apfs_clone **12/12**, whole stores green, clippy `--workspace -D` clean, frozen lock OK. No real bug. **🏁 apfs-clone TRIPLE COMPLETE** (spec→impl→review).
> ✅ `apfs-clone-complete` (generic, code `5a9135b`, evidence `apfs-clone-bench.log`) — clonefile before/after on M2 Max APFS: stage clone-ON 2.348s vs OFF 2.369s = **~1.01× (no wall-clock win on fast NVMe)**; **HONEST finding** — stage is read+hash-bound (persist copies THEN re-hashes), so on fast local disk the copy isn't the bottleneck; the real clonefile win here is **physical disk space** (a 4 GiB object materialized for ~0 physical bytes via CoW shared extents). Correctness confirmed: id ON==OFF, object pools byte-identical, clone genuinely fired (apfs_clone 12/12). CHANGELOG `[Unreleased]` bullet added (no false speedup claim — frames it as CoW space win). 💡 FOLLOW-UP (unscheduled): the latent wall-clock win needs skipping persist() re-hash on the clone path (CoW is bit-identical) — deferred at spec time. **🏁 BOTH PERF CHAINS COMPLETE** (fast-walk: ~7.6× `id`; apfs-clone: CoW space win, byte-identical).
> ✅ `clone-skip-spec-tests` (adversary, code `438ed83`) — staged `pending-tests/clone_skip.rs` (10 black-box tests, attestation YES). Contracted `SNAPDIR_VERIFY_COPIES=1` (clone still fires but re-hashes = strict write-time verify) + `SNAPDIR_CLONEFILE=0` control arm; race keystone (source mutated→B before copy ⇒ push Integrity OR read-time get_object/fetch rejection, never silent). Behavior-only (no pub(crate) imports).
> ✅ `clone-skip-walkstat-core` (core, code `37a0b95`) — new `src/copy_guard.rs` `CopyGuard{size,mtime_ns,ctime_ns,ino}` (`from_metadata`, unix-only, non-unix→None); new `walk_with_guards()` returns `(Manifest, HashMap<PathBuf,CopyGuard>)`; `walk`/`walk_with_meter` signatures unchanged (shared `walk_inner`). Guard emitted only for plain regular files (symlink-conservative). Core green (incl. fast_walk 18 + goldens); frozen lock OK; manifest text unchanged.
> ✅ `clone-skip-stores` (stores, code `9112b5e`) — `copy_file→CopyMethod{Cloned,Copied}`; `persist` + `CopyTrust{TrustedObject,StatGuarded(CopyGuard),Untrusted}` threaded via the job tuple; `SNAPDIR_VERIFY_COPIES=1` strict (read per-copy); `FileStore.copy_guards`+`with_copy_guards`. Empty guards ⇒ byte-for-byte today. clone_skip 9/9 (10 in file, 1 cfg-gated; no deletion), whole stores green, frozen lock OK, no other test touched. See the design-correction note above (fetch verifies-source; stage is the win).
> ✅ `clone-skip-cli` (cli, code `8fc6328`) — `run_stage` → `build_manifest_with_guards` (shared `resolve_walk` ⇒ byte-identical traversal) → `cache.with_copy_guards(map)` before `push`; guard keys == push's `source.join(rel)` abs paths (verified 3/3 match). **StatGuarded skip now ENGAGES end-to-end for stage** on this APFS host; stage id unchanged. `SNAPDIR_VERIFY_COPIES` stays env-only (no help drift); workspace build + full cli suite green; frozen lock OK.
> ✅ `clone-skip-tests-review` (adversary, code `8e79085`) — NO weakening (staged↔landed = fmt + 2 style allows; real `#[test]` 9==9, the "10" was a doc-comment grep artifact). `TrustedObject`=verify-source adjudicated **safer-not-weaker** + independently confirmed: a corrupt store object fetched on a clone-capable host still → `Integrity` (no silent-corrupt-checkout hole). +5 deterministic cases (corrupt-fetch, StatGuarded skip≡VERIFY_COPIES, stale-guard→re-hash, VERIFY_COPIES overrides matching guard, Untrusted==reference). clone_skip 14/14, whole workspace green, no reopen.
> ✅ `clone-skip-race-verify` (adversary, code `3b9968d`) — new `tests/clone_skip_race.rs` (7 tests) PROVED the two-layer model: write-time stat-guard catches the same-size mtime-spoof via **ctime** (utimensat can't move ctime back), and the read-time `get_object`/`fetch` backstop rejects a fully-defeated-guard mis-address (B-under-A). Concurrent mid-stage mutation (8 schedules), inode-reuse, mtime-granularity, 36-combo property loop. **No silent mis-address under any schedule; no reopen.** clone-skip correctness FULLY VERIFIED.
> ✅ `clone-skip-bench` (bench, code `f4bd10f`, evidence `clone-skip-bench.log`) — clone-skip ON vs OFF (`SNAPDIR_VERIFY_COPIES=1`), clone ON both arms (isolates the re-hash skip): **STAGE 2.38×** (2.63s→1.11s) + **CHECKOUT 1.51×** (1.52s→1.01s) — the win materialized on BOTH (checkout exceeded the conservative neutral prediction: destination temp re-hash elided, source still verified). Object pool + snapshot id byte-identical both arms. Beats apfs-clone-bench's ~1.0× (which had the re-hash in both arms).
> ✅ `clone-skip-complete` (generic, code `4c419dc`) — CHANGELOG `[Unreleased]` bullet for the clone-skip re-hash elision (stage/checkout speedup; stat-validated stage; `SNAPDIR_VERIFY_COPIES=1` strict; read-time BLAKE3 backstop). `cargo test --workspace` exit 0 (752 tests). **🏁 clone-skip chain COMPLETE (8/8); ALL THREE perf features done + verified** (fast-walk ~7.6× `id`; clonefile CoW; clone-skip stage 2.38× / checkout 1.51×). `release-prep-1.7.0` deps now: fast-walk-complete ✅ + clone-skip-complete ✅ + `release-perf-linux-1.7.0` ✋ (the only blocker).
> ✅ `reflink-spec-tests` (adversary, code `93305e1`) — staged `pending-tests/reflink.rs` (cfg(linux), env-gated on `SNAPDIR_REFLINK_TEST_DIR`/`_REQUIRE`, attestation YES). Reuses existing `clonefile_hits()`/`SNAPDIR_CLONEFILE` (option b). Cases: FICLONE-fired (counter Δ≥2), pool+id identity vs disabled, >256KiB+0-byte, EXDEV fallback, perms parity, `chattr +i`→removable-object. Compiles to nothing on macOS (correct); fired-clone cases run on the Btrfs CI leg.
> ✅ `reflink-stores` (stores, code `ff7e98c`) — `#[cfg(target_os="linux")]` `try_reflink` in `copy_file` (`libc::FICLONE`, errno fallback EOPNOTSUPP/ENOTTY/EXDEV/EINVAL/ENOSYS/EBADF, `set_permissions` parity, no flag-clearing). Returns `CopyMethod::Cloned` → inherits clone-skip win free; persist/CopyTrust/CopyGuard UNTOUCHED. Option B (reuse `SNAPDIR_CLONEFILE`/`clonefile_hits()`); no new dep. Took over `reflink.rs` (6 tests). **Linux branch + test cross-compiled CLEAN for musl** (zig wrapper); native macOS suite green; frozen lock OK.
> ✅ `reflink-ci-leg` (ci, code `56071b4`) — required `Reflink (Btrfs FICLONE)` job in ci.yaml (loopback Btrfs `mkfs.btrfs`+`mount -o loop`; runs reflink+clone_skip+clone_skip_race with `TMPDIR=/mnt/reflink`+`SNAPDIR_REFLINK_TEST_REQUIRE=1` → FICLONE-fired assertions + clone-skip suites on REAL reflink); existing ext4 matrix + macOS APFS leg unchanged; `utils/ci/reflink-vm-check.sh` local pre-flight added. actionlint + shellcheck clean. (The live FICLONE fire runs in this CI job on a Linux runner — the PM can't run it on macOS; here verified YAML+content+script.)
> ✅ `reflink-tests-review` (adversary, code `13e40a3`) — NO weakening (5 assertion-neutral hunks; 6 tests + env-gating intact); contract matches landed `try_reflink`; +2 impl-revealed cases (CLONEFILE=0 counter-flat; non-reflink/EXDEV fallback correct, no false counter bump). **Caught+fixed a CI-breaker**: 6 Linux-target-only clippy::pedantic lints in reflink.rs that ubuntu's `clippy --all-targets -D warnings` would have failed (invisible to macOS/lib-only clippy) — fixed via `#![allow]`. reflink.rs now 8 tests; musl cross-check + musl `--all-targets` clippy clean; native green.
> ✅ `reflink-race-verify` (adversary, code `dbd8860`) — +3 cfg(linux) race cases on clone_skip_race.rs (concurrent mid-stage mutation, `chattr +i`→removable-object, EXDEV cross-mount) — no silent mis-address on real reflink; the existing two-layer cases run vs real FICLONE via TMPDIR on the Btrfs leg. musl check+clippy clean, native green. **time_t RECONCILED**: native `clippy --all-targets -D warnings` exit 0 proves the `libc::time_t` deprecation is musl-binding-only — CI's ubuntu Lint is glibc (clean), musl leg is build-only → NOT a CI-breaker (no fix needed).
> ✅ `reflink-bench` (bench, code `95db1e0`) — `benches/reflink-compare.sh` reflink ON-vs-OFF stage/checkout harness; gracefully skips on macOS/ext4; real numbers run on the Btrfs CI leg / VM (expected ~2.4×/~1.5× mirroring macOS).
> ✅ `reflink-complete` (generic, code `ae79b1d`) — CHANGELOG generalized to **cross-platform CoW** (macOS clonefile + Linux FICLONE reflink on Btrfs/XFS/ZFS/OCFS2/bcachefs; reflink ×4, FICLONE ×3). `cargo test --workspace` **exit 0 (61 groups, 0 failures)**. **🏁 REFLINK CHAIN COMPLETE (7/7); ALL FOUR perf features done+verified** (fast-walk ~7.6× id; clonefile CoW; clone-skip stage 2.38×/checkout 1.51×; Linux reflink cross-platform). `release-prep-1.7.0` deps now: fast-walk-complete ✅ + clone-skip-complete ✅ + reflink-complete ✅ + `release-perf-linux-1.7.0` ✋ (the only blocker).
> **Next ready: `release-perf-linux-1.7.0` (generic, HUMAN ✋)** — the ONLY remaining eligible gate. Phase-27 SNAPPACK durability accept+document confirmation (operator chose Option 3 2026-06-12; machine-checks already true — `writeout_hint` in pack.rs + `SNAPDIR_FSYNC` documented). **The next tick ESCALATES to the operator for the release-go decision.** Approving unblocks `release-prep-1.7.0` (bump 1.6.0→1.7.0 + fold [Unreleased]→[1.7.0]) → verify-branch ✋ → PR ✋ → tag+publish ✋ (6 crates) → phase29-complete ✋. All four perf features (gatesmith-free code commits) ride into 1.7.0 via the verify-branch cherry-pick. Then `release-perf-linux-1.7.0` (HUMAN ✋, Phase-27 durability accept+document — machine-checks already true) → `release-prep-1.7.0` (now deps fast-walk-complete ✅ + clone-skip-complete ✅ + reflink-complete + the ✋) → verify-branch ✋ → PR ✋ → tag+publish ✋ (6 crates) → phase29-complete ✋. **FOUR** perf features (fast-walk, clonefile, clone-skip, Linux reflink — all gatesmith-free code commits) ride into 1.7.0 via the verify-branch cherry-pick. Then the release chain: **`release-perf-linux-1.7.0` (generic, HUMAN ✋)** — Phase-27 SNAPPACK durability accept+document confirmation (operator already chose Option 3 2026-06-12: ~20% Linux batch-vs-off ACCEPTED, `SNAPDIR_FSYNC=batch` default kept; machine-checks already true). → `release-prep-1.7.0` (now deps fast-walk-complete ✅ + clone-skip-complete + the ✋) → verify-branch ✋ → PR ✋ → tag+publish ✋ (6 crates) → phase29-complete ✋. All three perf features (fast-walk, clonefile, clone-skip — gatesmith-free code commits) ride into 1.7.0 via the verify-branch cherry-pick.

> ✅ Option-1 reverted (`25cd322`, original +19.5%/+29.9% design restored) + ✅ **cost documented** (`durability-cost-docs`, docs, code `0441e25`) — the ~20% small-files-receive cost of `SNAPDIR_FSYNC=batch` + the `off` escape hatch are now in ssh-wire-protocol.md / flagship README / root README / CHANGELOG (receive-pack path only, no overclaim).
> **Next gate:** `release-perf-linux-1.7.0` — **human ✋** the accept+document CONFIRMATION (operator already chose Option 3 at the escalation). The next tick escalates; it's a quick yes (revert landed + cost documented, both machine-checked). Then `release-prep-1.7.0` (bump 1.6.0→1.7.0 + CHANGELOG [1.7.0]) → verify-branch → PR → tag+publish (6 crates) → `phase29-complete`.

> 🔬 **DURABILITY PERF RESOLVED → Option 3 (accept + document), operator-approved 2026-06-12.** Two Linux measurements settled it:
> &nbsp;&nbsp;1. ORIGINAL design (with per-object hints), run 27390795784: **v1 +19.5% / zstd +29.9%** (>5%).
> &nbsp;&nbsp;2. OPTION 1 (drop per-object hints), run 27417335469: **v1 +56.8% / zstd +64.8%** — REGRESSED. Hypothesis FALSIFIED: the per-object `sync_file_range(WRITE)` hints are beneficial *async writeback-START* that PIPELINE the disk writes; removing them serializes the barrier. (macOS misled — its per-object hint was a blocking `fsync`.) **Batch durability has a ~20% inherent floor on the 5k-small-files receive; ≤5% is unreachable.**
> **Decision (background):** revert Option 1 (✅ done), keep `SNAPDIR_FSYNC=batch` crash-safe default, document the ~20% cost + `SNAPDIR_FSYNC=off`, relax the unrealistic ≤5% gate, ship. Evidence: `release-perf-linux-1.7.0-LINUX-RESULT.log` (original), `...-REMEASURE-2.log` (Option-1 regression). Memory: [[phase27-snappack-release-gate]].

> **Goal:** release the UNRELEASED Phase-27 (SNAPPACK 1Z zstd + recv-pack durability) + Phase-28 (--objects-store split, manifest-listing, `snapdir diff`, sync --from-objects/--to-objects) work as **1.7.0** (additive features → MINOR bump 1.6.0 → 1.7.0). Same machinery as the 1.5.0 release (Phase 25): prep → verify-branch off `upstream/main` (cherry-pick the gatesmith-free Phase-27/28 code commits, ZERO `.gatesmith`) → upstream PR + squash-merge → tag `v1.7.0` + publish all **6** crates. dev carries `.gatesmith`; main/upstream never do.
> **Linux perf number comes from the GitHub `ubuntu-latest` runner** (operator decision). The Phase-27 perf gate is split into two: **(1a) `release-perf-job-ci-1.7.0`** (ci, auto) — a ci teammate adds the snappack RECEIVE bench (`pack/receive/a`) to the existing `bench-verify.yml` (already ubuntu-latest + workflow_dispatch + tees to bench-output.log), so the batch-vs-off delta is readable from the run/artifact (no threshold enforced in CI); **(1b) `release-perf-linux-1.7.0`** (generic, **human ✋**) — the operator dispatches bench-verify.yml on the runner, reads the receive batch-vs-off delta on scenario (a) for v1+zstd, and confirms ≤5%. **If >5%, do NOT release** — escalate. (Memory: [[phase27-snappack-release-gate]].)
> ✅ `release-perf-job-ci-1.7.0` (ci, code `dc90251`) — added the `snappack receive perf` step to the ubuntu-latest `bench-verify.yml` (informational, no CI threshold).
> **When the perf gate (1b) is reached — OPERATOR ACTION at its checkpoint:** the PM pushes dev (after the typos fix), dispatches `bench-verify.yml` on the runner, downloads the `bench-output` artifact, and reads the receive `batch`-vs-`off` delta on `pack/receive/a` for v1+zstd; operator confirms ≤5% (if >5%, do NOT release — escalate).
> **Then:** `release-prep-1.7.0` (bump + CHANGELOG [1.7.0] + lock) → `release-verify-branch-1.7.0` (human ✋) → `release-pr-upstream-1.7.0` (human ✋, IRREVERSIBLE merge) → `release-tag-crates-1.7.0` (packaging, human ✋, IRREVERSIBLE tag+publish; 6 crates) → `phase29-complete` (human ✋ sign-off).
> 🔄 Ralph loop re-armed on `/gatesmith` (iteration 1+). The next tick spawns the ci teammate for `release-ci-clean-1.7.0` (auto). The PM does the dev push + workflow dispatch + artifact read itself (gh authed as bermi); the human-checkpoint perf gate is just the ≤5% sign-off.

## ✅ PHASE 28 COMPLETE — 17/17 — objects-store split + `diff` (UNRELEASED — shipping in Phase 29 / 1.7.0)

> 🏁 **Phase 28 signed off** (`phase28-complete` ✅ generic, human-approved @ 2026-06-12T01:17:10Z, git `7e80e5d`; `cargo test --workspace` = **696 passed / 0 failed** across 55 groups). Shipped (UNRELEASED): the **`--objects-store` / `$SNAPDIR_OBJECTS_STORE`** shared-pool split (one `.objects` pool ⊥ many manifest locations — scheduled inventories without re-uploading objects), **`list_manifest_ids`** (name a diff side by a manifest prefix), **`snapdir diff`** (manifests-only file-level diff across manifest paths, A/D/M, `--all`/`--json`/`--exit-code`/`--on-conflict`), and **`sync --from-objects/--to-objects`** (bucket-to-bucket cross-pool dedup). Docs on the flagship + root README + CHANGELOG `[Unreleased]`.
> 🛡 **FIRST phase under the adversarial test-separation model** (operator-designed). 10 adversary gates (5 black-box authoring + 5 review) across the 5 feature triples. Record: every suite authored independently black-box (attested no impl read) before the code existed; the PM-fence proved the impl could never weaken a test (staged tests moved as git renames, zero inline `src/` tests, adversary diff-audits); the model **caught 2 real `diff` directory-leak bugs** → forced `src` fixes, not test-softening; and when a coder edited one test, the adversary **independently adjudicated** it (read the source to verify) and hardened it anyway. The model worked as designed.
> ⛔ **Release still gated:** UNRELEASED — release is a separate future phase. The standing **Phase-27 release gate** remains: confirm the **Linux** receive `batch`-vs-`off` ≤5% via a CI/Linux perf run before any 1.x ship (macOS bench was a Darwin fsync artifact). See [[phase27-snappack-release-gate]] in memory.
> **Ledger:** all production gates `passed`; no ready pending gates remain. The next PM tick reports `===== PROJECT COMPLETE — all gates green =====` for this ledger (until a new phase is opened).

> **Model in effect:** each testable feature is an `adversary`→lane-impl→`adversary`-review triple (see `PM_PROMPT.md` → "Adversarial test separation").
> ✅ **ALL 5 features COMPLETE** (each a full triple — suite adversary-owned, zero weakening, strengthened on review):
> &nbsp;&nbsp;• **`split-store`** — `SplitStore` (shared `.objects` pool ⊥ many manifest locations); **21→26** tests. code `0a37039`/`99ed4e5`.
> &nbsp;&nbsp;• **`manifest-listing`** — `list_manifest_ids` (StreamStore-default-fail-closed + FileStore-shard-walk / S3+GCS / B2 / `SplitStore`→manifests); **17→22** tests. code `84b6a2d`/`a23bf97`.
> &nbsp;&nbsp;• **`objects-store-cli`** — **`--objects-store` / `$SNAPDIR_OBJECTS_STORE`** (the user-facing "one shared pool, many manifest locations" — scheduled inventories without re-uploading objects; wraps `SplitStore`, external-rejected both sides, unset==unchanged, catalog records `--store`); **11→17** e2e tests (review added error-msg exactness, flag>env-poison, `store_is_external` split-is-in-process). code `9a4e84d`/`8dcae54` (last @ 2026-06-11T23:38:58Z). NON-BUG NOTE: missing-`--store` on push hits the cli.rs:677 guard (names `--store`, clean/no-panic), not the :1270 `$SNAPDIR_STORE` message.
> &nbsp;&nbsp;• **`diff-command`** — **`snapdir diff`** (manifests-only file-level diff across manifest paths — the second headline capability; `--from/--to` unioned, `--all`/`--json`/`--exit-code`/`--on-conflict`; `resolve_side` calls ONLY `list_manifest_ids`+`get_manifest`, never an object store); **19→28** tests. 🐛 **The adversary's black-box tests caught 2 REAL dir-leak BUGS** → fixed in `src/diff.rs`, NOT the tests (review confirmed the bug-catching tests kept their assertions verbatim). code `6b7404b`/`b8599b4` (last @ 2026-06-12T00:09:21Z).
> **Model proven — and it earned its keep:** independent spec & impl agree first-run for clean features; and when the impl shipped bugs (`diff`), the INDEPENDENT tests CAUGHT them and the PM-fence forced a `src` fix, not a test softening — exactly the failure mode a self-testing author misses. Review consistently adds coverage (fault injection, boundary attacks, error-exactness, dir-handling) on now-visible internals.
> &nbsp;&nbsp;• **`sync-objects-split`** — **`sync --from-objects/--to-objects`** (per-side object pools for bucket-to-bucket sync; source & dest can be different buckets; `run_sync` wraps each side in `SplitStore`, **`sync_snapshot` engine UNCHANGED**); **7→9** e2e tests. The review **independently adjudicated** a coder probe-change (`manifest`→`fetch`) as a *legitimate-fix* (read `cli.rs` to confirm `manifest` can't resolve a store, so the original probe could never fail; the primary manifest-last `count==0` assertion was intact/load-bearing/reached) and strengthened it anyway (explicit absent-`<id>` dest assertion, per-side external rejection, per-side flags independent of the global). code `cc8bcf0`/`87da4c4` (last @ 2026-06-12T00:32:26Z).
> **Model verdict:** independent spec & impl agree first-run for clean features; the impl can never weaken the suite (PM-fence + adversary diff/help-snapshot audit + independent probe adjudication); and the model CAUGHT real bugs (`diff`, 2×) the fence forced fixed in `src`. Across 5 features the adversary review added fault injection, boundary attacks, error-exactness, dir-handling, and absent-manifest assertions a self-tester would miss.
> **Next gate:** `phase28-complete` — **human checkpoint ✋** (owner generic). The next PM tick escalates to the operator via AskUserQuestion (no teammate). Verification `cargo test --workspace --locked`. Sign-off covers: the `--objects-store` shared-pool / `diff` / split-sync feature set, all delivered under the adversarial test-separation model (independent black-box authoring → lane impl → adversary review; the model caught 2 real `diff` bugs and forced src-fixes, never test-softening). UNRELEASED — release is a separate future phase (and remember the standing Phase-27 release-gate: Linux batch-vs-off ≤5% before any 1.x ship).
> **Next gate:** `split-store-impl` (stores) — implement `SplitStore` in `crates/snapdir-stores/src/split.rs` (Store + StreamStore delegation, shared push/fetch helper) AND **take over** the staged tests: move `.gatesmith/pending-tests/split.rs` → `crates/snapdir-stores/tests/split.rs`, fixing only wiring/compile (never weakening). Verified by `cargo test -p snapdir-stores --test split` going green + the staged file being consumed.

## ✅ PHASE 27 COMPLETE — 10/10 — SNAPPACK zstd + recv-pack durability — UNRELEASED (release-gated, see follow-up)

> 🏁 **Phase 27 signed off** (`phase27-complete` ✅ generic, human-approved @ 2026-06-11T22:19:53Z, git `db1e90e`; `cargo test --workspace` = 594 passed / 0 failed across 50 groups). Shipped: batched receive-pack crash-durability (`SNAPDIR_FSYNC=batch` default, fsync barrier before manifest) + additive `SNAPPACK 1Z` zstd transport (wire stays 1, receiver sniffs, `snappack-zstd` cap) wired stores↔CLI↔ssh-engine with full back/forward compat to 1.5.0 peers, proven hermetically AND over real sshd. Compression zstd/v1 = **0.208** (text-like small files). **UNRELEASED** — release is a separate future phase.
> ⛔ **RELEASE-GATING FOLLOW-UP (operator decision @ sign-off — "Approve, Linux # as follow-up"):** before ANY 1.x release that ships SNAPPACK durability, confirm the **Linux** receive `batch`-vs-`off` delta is **≤5%** via a CI/Linux perf run. The macOS bench showed +28% (v1) / +17% (zstd) — a Darwin per-object-`fsync` artifact, NOT the acceptance number (Linux uses amortized `sync_file_range`). This was NOT measured locally (no Linux host); it must be produced from CI before release.
> 📌 **Doc accuracy note (carryover):** `SNAPDIR_SSH_ZSTD_LEVEL` is permissive — parse env, default 3, **clamp** to 1..=19 in lib `write_pack` (NOT a CLI hard-error). Docs already describe the shipped behavior.
> **Last passed:** `wire2-compat-matrix` ✅ (stores, `crates/snapdir-ssh-store` lane-exc, code `b2e2f33` @ 2026-06-11T21:12:26Z) — full SNAPPACK v1/1Z back-forward compatibility matrix: (1) new↔new = 1Z on the wire, byte-identical store, pack **smaller** than v1 on a compressible fixture, `--pack-format zstd` logged; (2) new client ↔ 1.5.0 remote = accel still taken on v1, no zstd flag; (3) 1.5.0 client ↔ new remote pinned to v1; (4) `FORCE_ACCEL` error text zstd-free; (5) `loopback_sshd` dumb-vs-accel-**zstd** byte-identical over REAL sshd; + a `SNAPDIR_FSYNC=batch` interplay smoke through the full accel path. `cargo test -p snapdir-ssh-store` (whole crate, no skips) + `--test plumbing` both exit 0. Reused `install_caps_only_snapdir`/`install_logging_snapdir` fakes; test-only, no production change.
> **Prior:** `snappack-docs` ✅ (docs, code `29e892f` @ 2026-06-11T21:05:03Z) — `ssh-wire-protocol.md` SNAPPACK 1Z section (magic, single-zstd-frame over the unchanged v1 grammar, decompressed bounds 128B/64MiB, CPU-only bomb reasoning, sniff-on-read/advertise-to-write negotiation) + Durability section (batch model, journal-ordering, `SNAPDIR_FSYNC`, non-journaling-fs caveat ≤ git's claim); README perf note (auto-negotiate, `SNAPDIR_SSH_ZSTD_LEVEL` 1–19/clamped/default 3, WAN/HPN `-o Compression=no`); CHANGELOG `[Unreleased]`. typos clean + greps pass; all values verified vs landed code. **Accuracy note:** `SNAPDIR_SSH_ZSTD_LEVEL` is permissive — parse env, default 3, **clamp** 1..=19 in lib `write_pack` (NOT a CLI hard-error as the gate prose implied); docs describe the shipped behavior.
> **Prior:** `wire2-zstd-ssh` ✅ (stores, `crates/snapdir-ssh-store` lane-exc, code `4330276` @ 2026-06-11T20:51:36Z) — ssh engine runtime zstd negotiation: local `ZSTD_CAP` (pinned to `snapdir_stores::WIRE_CAPS`) + a `snapdir_local_zstd` probe of the local binary's caps (`|| true`-guarded so an older 1.5.0 local that rejects `--capabilities` degrades cleanly under `set -e` — a real robustness bug the teammate caught). Push picks `--pack-format zstd` iff remote advertises `snappack-zstd` AND local supports it; fetch bakes TWO static remote `send-pack` variants (±` --pack-format zstd`) and picks at runtime (ids/ids_all pattern); local `receive-pack` sniffs the magic. `PUSH_CAPS`/`FETCH_CAPS` + `FORCE_ACCEL` text + the `wire=1` literal are byte-identical; `--pack-format zstd` is always a STATIC literal (no runtime env interpolated into baked remote command lines); 1.5.0 peers fall back to v1 with accel still taken. 12/12 accel + whole crate (incl. loopback_sshd) green.

> ⚠️ **CARRYOVER for `phase27-complete` sign-off:** the SNAPPACK acceptance "receive `batch` ≤5% over `off`" is a **Linux** target (the batch optimization amortizes Linux `sync_file_range`). On macOS the durability path is per-object blocking `fsync` BY DESIGN, so `snappack-bench` measured **+28% (v1) / +17% (zstd)** on (a) on this Darwin box — NOT a regression. The PM must present the **Linux** batch-vs-off delta (CI/linux perf run) at the `phase27-complete` human checkpoint, not the Darwin number.
> 🛠 **Process note (cross-lane test breakage):** `wire2-zstd-cli` found that `plumbing_capabilities_line_exact` (cli) had been silently RED since the `wire2-zstd-format-stores` tick — the stores `WIRE_CAPS += snappack-zstd` change broke a CLI assertion, but that gate's stores-only verification never ran the CLI tests. Now fixed; a full `cargo test --workspace` (51 groups, 0 fail) confirms the tree is green. Lesson: a lane changing a const/contract that another lane's tests pin won't be caught by per-crate verification until the consuming lane's gate runs.
> **Last passed:** `wire2-zstd-cli` ✅ (cli, code `57211f4` @ 2026-06-11T20:37:06Z) — hidden `--pack-format <v1|zstd>` on `send-pack` (clap value-enum, `hide`, **default `v1`** → old invocations byte-identical, help surface unchanged) → `write_pack_with_format(PackFormat::{V1,Zstd(level)})`, level from `SNAPDIR_SSH_ZSTD_LEVEL` read at the CLI seam (lib stays env-free). `receive-pack` untouched (magic sniff). `version --capabilities` already advertises `snappack-zstd` from `WIRE_CAPS`. 17 plumbing + full workspace (51 groups) green.
> **Prior:** `snappack-bench` ✅ (bench, code `f5c42c7`) — criterion `snappack.rs` send/receive (v1 vs 1Z × off/batch) + std-only generators (splitmix64-mixed seeds; fixed a false 0.625 ratio on the "incompressible" corpus) + bytes-on-wire `#[test]` + `iai_hot` baseline. Measured zstd/v1 = 0.208 (a) / 1.000 (b). (See batch-vs-off carryover above.)
> **Prior:** `dist-smoke-musl` ✅ (ci, confirmation, git `8ccde8c`) — existing ci.yaml musl job builds `--workspace --all-features` under `x86_64-unknown-linux-musl` w/ musl-tools, covering zstd-sys's bundled-libzstd cc build; ring TLS; no ci.yaml change.
> **Prior:** `wire2-zstd-format-stores` ✅ (stores, code `bad2a7f`) — SNAPPACK 1Z additive zstd transport: `WIRE_MAGIC_ZSTD "SNAPPACK 1Z\n"`, whole post-magic stream = one zstd frame of the unchanged record grammar; `read_pack` sniffs the magic → zstd Decoder feeds the UNCHANGED parser (incremental BLAKE3 untouched); receiver accepts v1 AND 1Z forever; `snappack-zstd` appended to `WIRE_CAPS`, **`WIRE_VERSION` stays 1** (older peers ignore the unknown cap — no dumb fallback). Bounds enforced on decompressed bytes; level 3 default (1..=19 param, library env-free). New dep `zstd 0.13` on snapdir-stores only. 43 `pack` tests green; `cargo deny` ok. **Crate-age verification PM-attested** (registry scan is network-slow): `Cargo.lock` diff = +5 crates / 0 removed / 0 version-bumps; all 5 new crates clear the 3-day floor (jobserver 291d, pkg-config 60d, zstd 475d, zstd-safe 448d, zstd-sys 280d), every other crate pre-existing & age-monotonic, scan 267/442 @ 0-FAIL when stopped, teammate's pre-run executed the full command green → `AGE_EXIT=0` guaranteed. LANE-FENCE-EXC: `Cargo.lock` delta with the in-lane `zstd` dep add.
> **Next phase:** **Phase 28** — `--objects-store` shared-pool/manifest-location split (SplitStore) + manifest-listing + split-aware sync + manifests-only `diff` (17 gates, all gated on `phase27-complete` which is now ✅). **Phase 28 is the first phase under the new adversarial test-separation model** — each testable feature is an `adversary`→lane-impl→`adversary`-review triple (see `PM_PROMPT.md` → "Adversarial test separation"). The next ready gate is `split-store-spec-tests` (owner `adversary`, black-box test authoring from spec).

## ✅ Receive-pack durability COMPLETE end-to-end (stores barrier + CLI knob) — phase-27 gates 1-2

> **`recv-fsync-knob-cli`** ✅ (cli, code `ed8906a`) — `SNAPDIR_FSYNC=batch|off` env knob in `run_receive_pack` (default `batch`; fail-closed on unknown) → `FileSink::with_durability`; overrode `RecordingSink::flush_barrier` to delegate (closed the cross-lane gap where the no-op default swallowed the barrier). No new CLI flags; 16 plumbing tests green.
> **`recv-fsync-batch-stores`** ✅ (stores, code `aff3e0d`) — cfg-gated `src/fsync.rs` over libc; `PackSink::flush_barrier()` before `put_manifest`; `FileSink` `Durability` + `write_manifest_durable`; exactly 2 full syncs/pack; `FileStore::push`/`put_object` untouched; library env-free. 32 `pack` tests green.

> **Phase 28 registered (17 gates):** `--objects-store` shared-pool/manifest-location split (SplitStore) + manifest-listing + split-aware sync + manifests-only `diff`, authored under the NEW **adversarial test separation** model — each testable feature is an `adversary`→lane-impl→`adversary`-review triple (see PM_PROMPT "Adversarial test separation"). Gated on `phase27-complete`.

## ✅ PHASE 25 COMPLETE — 5/5 — snapdir 1.5.0 RELEASED (2026-06-10); PHASE 26 NEXT (170/184 total)

> **snapdir 1.5.0 is RELEASED**: upstream/main `8cc8e0c`, tag `v1.5.0`, GitHub release live (13 signed assets incl. `snapdir-ssh-store` + `snapdir-sftp-store` bins), **all 5 crates** at 1.5.0 on crates.io. snapdir-ssh-store's FIRST publish hit crates.io's "TP tokens cannot create new crates" 403 → recovered: manual publish with the operator's keychain token + idempotent job rerun finished snapdir-cli via TP. OPERATOR FOLLOW-UP: add TP on the snapdir-ssh-store crate page. Phase 27 SNAPPACK recv-fsync-batch + wire2-zstd queued (10 gates, plan-approved; NOTE snapdir fsyncs NOTHING today — fsync gate ADDS crash-durability to manifest-last).

## ✅ PHASE 26 COMPLETE — 4/4 — `cargo install snapdir` LIVE; snapdir 1.6.0 RELEASED (2026-06-11) — 174/184; PHASE 27 NEXT

> **snapdir 1.6.0 released**: upstream/main `8baefa9`, tag v1.6.0, GH release 13 assets, ALL SIX crates at 1.6.0 (incl. first publishes of `snapdir` — flagship name OWNED — both manual-token recoveries per the known TP limitations). `cargo install snapdir` smoke-verified from crates.io. snapdir-cli = impl library (≤1.5.0 still installs the old bin). Rescued never-shipped `b42b23c` (snapdir.org homepage/READMEs). **OPERATOR FOLLOW-UPS:** TP registration on `snapdir` + `snapdir-ssh-store` crate pages; ~28-file dev↔upstream drift = candidate reconciliation gate; apt-get-update hardening for the openssh install step (runner MS-repo flake) → fold into the next ci gate. NEXT: Phase 27 recv-fsync-batch + wire2-zstd (10 gates, both roots now eligible).

## ✅ PHASE 26 GATE DETAIL — own the `snapdir` crate name (172/184 total)

> - `snapdir-name-crate` ✅ (cli, code `a064003` @ 2026-06-10) — **design (b)**: the `snapdir` bin moved to the new flagship `crates/snapdir` (3-line shim over the new `snapdir_cli::run()` lib); duplicate-bin design (a) killed by cargo's output-filename-collision warning (verified 1.91.1 + 1.96.0). snapdir-cli stays published as the impl lib; ≤1.5 installs keep the old bin. Parity tests byte-identical; workspace 558/0.
> - pending: `snapdir-name-docs` → `snapdir-name-release-1.5.1` (human ✋: bump+branch+PR+tag+publish, **TP/manual-first-publish for the `snapdir` name with the keychain token**, release.yml CLI_CRATE/dist follow-ups flagged by the crate gate) → `phase26-complete` (human ✋).

## ✅ PHASE 25 GATE DETAIL — release snapdir 1.5.0

> - `release-prep-1.5.0` ✅ (generic, code `1c416d9` @ 2026-06-10, gatesmith-free) — workspace + 3 internal dep versions → 1.5.0, lock regenerated, CHANGELOG [1.5.0] cut with fresh [Unreleased] + compare links; release.yml 5-crate loop confirmed.
> - `release-verify-branch-1.5.0` ✅ (operator-approved 2026-06-10) — branch `21ee6be` off upstream/main, 12 clean cherry-picks, ZERO .gatesmith; bench-verify run 27247039135 green (5/5 determinism, iai+criterion artifact consulted).
> - IN FLIGHT: PR snapdir/snapdir#12 open, CI matrix running (watched in background).
> - pending: `release-pr-upstream-1.5.0` (human ✋ merge) → `release-tag-crates-1.5.0` (human ✋, incl. **crates.io TP registration for snapdir-ssh-store BEFORE tagging**) → `phase25-complete` (human ✋).
> - Phase 26 queued (gated on phase25-complete): own the `snapdir` crate name — `cargo install snapdir` (name verified FREE on crates.io 2026-06-10); 4 gates registered.

## ✅ PHASE 24 COMPLETE — 12/12 (operator sign-off 2026-06-10) — sftp:// + ssh:// stores

> Operator sign-off 2026-06-10. Shipped per the approved plan: the latent CLI external-store wiring bug fixed (affected ALL `snapdir-*-store` binaries), new crate `crates/snapdir-ssh-store` (`snapdir-ssh-store` + `snapdir-sftp-store` bins, snapdir-core-only deps), un-weakenable modern-only OpenSSH ≥8.5 floor, pure-SFTP engine (internal-sftp chroot-safe), ssh:// dumb engine (atomic tar pipeline, hostile-remote allowlist), SNAPPACK 1 wire format + hidden plumbing (`objects-needed`/`send-pack`/`receive-pack`, `version --capabilities`, wire=1) + `StreamStore::objects_needed`, runtime-negotiated acceleration (byte-identical dumb-vs-accel oracle PROVEN hermetically and over real sshd; NO_ACCEL/FORCE_ACCEL/PULL_SENDALL), loopback-sshd T2 suite required in ci/coverage/pre-push, docs (README, crates.io page, `ssh-wire-protocol.md`, ADR-0027, CHANGELOG), release wiring (publish loop + archives). Workspace 554 tests / 0 failed with sshd suite forced. **UNRELEASED** — next release phase: 1.5.0 bump, release-verify branch, upstream PR, tag + publish, and crates.io Trusted-Publishing registration for the new crate name. Frozen format untouched. dev carries `.gatesmith`; main/upstream never do.

## ✅ PHASE 24 GATE DETAIL — 12/12 — sftp:// + ssh:// stores

> Operator plan-approved 2026-06-09 (full spec: `.gatesmith/plans/phase24-ssh-stores.md`): system-OpenSSH external stores via the emit-command contract — `sftp://` pure-SFTP engine (works against `ForceCommand internal-sftp` chroots), `ssh://` shell engine + wire=1 SNAPPACK acceleration (`objects-needed`/`send-pack`/`receive-pack` hidden plumbing, byte-identical-oracle gated) with graceful fallback; un-weakenable modern-only security floor; new workspace crate `crates/snapdir-ssh-store` (2 bins, snapdir-core-only deps).
> - `external-cli-wiring` ✅ (cli, code `bdfae15` @ 2026-06-09) — PREREQ BUG FIX: cli passed TREES where the external contract expects SHARDED store roots; push now stages into the cache then pushes from the cache root (`push --id` skips scratch), fetch lands objects in the cache root and commits the manifest LAST via `put_manifest`. Native store paths byte-identical. New e2e `external_store_roundtrip.rs` drives the real binary vs `mock://`.
> - `pack-wire-format` ✅ (stores, code `18f76d3` @ 2026-06-09) — SNAPPACK 1 in `pack.rs`: hex64-validated obj/manifest records, incremental-BLAKE3 temp-sibling FileSink (O(1) mem), manifest commits only after a verified `end` trailer (truncation never publishes), duplicates verified-then-skipped; `WIRE_VERSION=1`/`WIRE_CAPS`; `StreamStore::objects_needed` defaulted (order-preserving, fail-closed). 23 pack tests. blake3 dep = exact req core already pins (LANE-FENCE-EXC, lock +1 line, no new crate).
> - `cli-plumbing` ✅ (cli, code `187b53d` @ 2026-06-09) — hidden `version --capabilities` (wire=1 negotiation line) + `objects-needed`/`send-pack`/`receive-pack` thin glue over pack.rs; fail-closed validation, FileSink streaming on file://, `--require-manifest` pins the committed id; help surface byte-stable; 12 e2e plumbing tests incl. OS-pipe round-trip + truncation/idempotency.
> - `ssh-store-scaffold` ✅ (stores, code `f306d0c` @ 2026-06-10) — new crate `crates/snapdir-ssh-store` (2 bins, snapdir-core-only): URL grammar, env family, ORDERED un-weakenable security floor, ssh -V ≥8.5 gate, bash-3.2-clean ControlMaster skeleton; 37 table tests; root Cargo.toml delta = 1 members line (LANE-FENCE-EXC; stores lane extended in PM_PROMPT).
> - `sftp-engine` ✅ (stores, code `4aca5fb` @ 2026-06-10) — pure-SFTP engine (works under `ForceCommand internal-sftp`): probe+pwd-liveness, JOBS-parallel put-tmp/rename/chmod chunks, manifest LAST, exact ERROR wording; fake-sftp fixture + 10 fault-injection tests via the real shim.
> - `ssh-engine-dumb` ✅ (stores, code `1762463` @ 2026-06-10) — ssh:// dumb engine: batched existence probe + single `tar|ssh` atomic pipeline + manifest-last; fetch gated on an exact-match tar allowlist (hostile-remote safe); `_snapdir_dumb_*` functions = accel seam; fake-ssh fixture + 11 fault-injection tests.
> - `ssh-accel` ✅ (stores, code `3aa3989` @ 2026-06-10) — runtime-negotiated SNAPPACK accel: one combined probe RT, wire=1 exact-match dispatch, objects-needed diff + send-pack|receive-pack manifest-last stream, NO_ACCEL/FORCE_ACCEL/SENDALL toggles, graceful fallback (no silent mid-stream fallback). **Byte-identical dumb-vs-accel oracle proven.** 10/10 tests, zero skips.
> - `loopback-sshd-suite` ✅ (tests, code `afd5080` @ 2026-06-10, failure_count 1 — TMPDIR race fixed on retry) — real-OpenSSH suite vs self-spawned sshd (3 flavors, no docker): dumb+accel round-trips, restricted-sftp-only, host-key fail-closed + EXTRA_OPTS-cannot-weaken behavioral proof, accel oracle over sshd, FORCE_ACCEL error, scoped no-leak; 7/7 ×3 runs + --test-threads=8; skips without sshd unless SNAPDIR_SSH_TEST_REQUIRE=1.
> - `ssh-ci-wire` ✅ (ci, code `167e92d` @ 2026-06-10) — test+coverage jobs: Linux openssh-server install + `SNAPDIR_SSH_TEST_REQUIRE=1` (no silent skips, coverage keeps ssh-store paths); pre-push mirror exports the same env; actionlint/deny/crate-age clean.
> - `ssh-docs` ✅ (docs, code `0a40a05` @ 2026-06-10) — README store rows + SSH/SFTP section, crates.io crate README (verbatim floor flags, ≥8.5 policy), normative `ssh-wire-protocol.md`, ADR-0027, CHANGELOG Added/Fixed; all claims code-verified.
> - `ssh-packaging-dist` ✅ (packaging, code `9475aab` @ 2026-06-10) — publish loop core→catalog→stores→ssh-store→cli (skip-if-published shape unchanged), both bins staged into every release archive incl. musl; **release-time operator TODO: crates.io Trusted-Publishing registration for the new crate name** (job is TP-OIDC only). No bump/tag/publish.
> - pending: `phase24-complete` (human checkpoint — operator sign-off).

## ✅ PHASES 0–23 COMPLETE — 153/153 — snapdir 1.4.0 RELEASED

> snapdir-rs is the Rust port at byte-for-byte manifest interop (frozen format locked). **snapdir 1.4.0 is RELEASED** (2026-06-09): canonical snapdir/snapdir at 1.4.0 (`4b2e40c`), tag `v1.4.0`, GitHub release live (13 assets, signed + SLSA), all 4 crates published to crates.io. dev carries `.gatesmith`; main/upstream never do.

## ✅ PHASE 23 COMPLETE — 6/6 gates — release snapdir 1.4.0

> Shipped the Phase 20–22 work: input-path normalization, `--store`/`sync --from` → `$SNAPDIR_STORE`, the rate-limiting + exponential-backoff-retry feature (Retry-After + per-call request-rate limiting + per-backend defaults; adaptive stays opt-in), and per-crate crates.io READMEs. Zero new deps; frozen format untouched.
> - `release-prep-1.4.0` ✅ (code `f269316`) — bump → 1.4.0 + CHANGELOG `[1.4.0]`.
> - `deny-ignore-procmacroerror2` ✅ (ci, code `bfb3a8c`) — pre-push `cargo deny` caught the new RUSTSEC-2026-0173 (proc-macro-error2 unmaintained, bench dev-dep); justified ignore added.
> - `release-verify-branch-1.4.0` ✅ — release-verify/1.4.0 off upstream/main (12 cherry-picks, ZERO .gatesmith), bench-verify run 27215144981 green.
> - `release-pr-upstream-1.4.0` ✅ — PR snapdir/snapdir#11, 15/15 CI green (incl. Crate-age ≥3 days), squash-merged → upstream/main `4b2e40c` at 1.4.0.
> - `release-tag-crates-1.4.0` ✅ (packaging) — tag `v1.4.0` → release.yml run 27216975334 success: GitHub release live + all 4 crates 1.4.0 on crates.io.
> - `phase23-complete` ✅ (operator sign-off) — 1.4.0 released end-to-end.

## ✅ PHASE 22 COMPLETE — 1/1 gate — release / crates.io polish

> **Operator-reported 2026-06-08:** the snapdir crate pages on crates.io rendered NOTHING for the README (published packages carried no README).
> - `crates-io-readme` ✅ **PASSED** (code `3188e9f` @ 2026-06-09) — each crate (core/catalog/stores/cli) got a concise per-crate `README.md` in its own dir + a literal `readme = "README.md"` in `[package]` (per-crate, not workspace inheritance which warns "outside of the package"; root Cargo.toml unchanged). `cargo package --list` shows a README for all four; ships on the next crates.io publish. Metadata/docs only.

---

## ✅ PHASE 21 COMPLETE — 7/7 gates green (operator sign-off 2026-06-09) — rate limiting + exponential-backoff retries

> **Operator-requested 2026-06-08 (plan-approved):** full-jitter **exponential backoff** honouring server `Retry-After` on transient (429/503/timeout/conn-reset) network failures; a **request-rate (req/s)** limiter; the adaptive controller proven live (kept OPT-IN). Configurable via flags/env (`--flag > SNAPDIR_* env > per-backend default > global`) with **researched per-backend defaults** (S3/GCS/B2). **NO new deps** (hand-rolled SplitMix64 jitter); each SDK's built-in retries disabled. `file://` path + frozen format UNTOUCHED. Retry default 5 / 250ms / ×2 / 30s / full jitter. `phase21-complete` ✅ signed off 2026-06-09 (`cargo test --workspace --locked` 433 passed/0 failed, green ×5 after the flaky-fix).
>
> **Gates (7) — all passed:**
> - `ratelimit-docs` ✅ **PASSED** (code `555555b` @ 2026-06-09) — README "Rate limiting & retries" section (backoff + Retry-After, retry/request-rate flag/env tables, per-backend default table w/ AWS/GCS/Backblaze sources, flag>env>backend>global precedence, adaptive opt-in) + CHANGELOG [Unreleased] Added entry. Docs only; accurate to code; no frozen-format change.
> - `ratelimit-cli-select` ✅ **PASSED** (code `32ddc65` @ 2026-06-09; DESC-CORRECTED) — new --max-retries/--retry-base-ms/--retry-max-ms (→ resolve_retry_policy → TransferConfig.with_retry) + --max-requests/SNAPDIR_MAX_REQUESTS (→ max_requests_per_sec); resolve_rate_limits(scheme) layers user > limits::for_scheme conservative min(read,write) > None (b2 20rps/25MiB, s3 3500rps, gs 1000rps, file none); reused existing --limit-rate/--adaptive (no dup flags). Byte-identical when knobs unset. 6 ratelimit tests; 16 trycmd refreshed (additive). [--max-rate/--adaptive dropped as already-shipped.]
> - `stores-backoff-wire` ✅ **PASSED** (code `db5425a` @ 2026-06-09) — each S3/GCS network call (key_exists/get_bytes/put_bytes; b2 via S3) now runs through `retry_network`: acquire 1 request-rate token then drive the SDK call via the retry.rs full-jitter backoff. `s3_attempt_from_err`/`gcs_attempt_from_err` classify transient (429/503/RESOURCE_EXHAUSTED) + extract a Retry-After floor; SDK built-in retries DISABLED (S3 RetryConfig::disabled, GCS NeverRetry); `TransferConfig.retry` seam defaults to `RetryPolicy::default()` (CLI sets it next). 12 `backoff_wire` tests green. Frozen object-key/verify unchanged; no new deps. [DEP-CORRECTION: ratelimit-cli-select now deps on this.]
> - `retry-backoff-core` ✅ **PASSED** (code `f96ba82` @ 2026-06-09) — new `retry.rs`: `RetryPolicy` (5/250ms/30s) + overflow-safe full-jitter backoff with `Retry-After` floor; hand-rolled SplitMix64 `Jitter` + injectable stub; async/blocking `Sleeper` traits + recording test fakes; SDK-agnostic `retry_async`/`retry_blocking` over `Attempt{err,transient,retry_after}`. 11 `retry::` tests green (both engines). NO new deps; not yet wired into S3/GCS/B2 (→ stores-backoff-wire).
> - `adaptive-wire-live` ✅ **PASSED** (re-passed @ `09bbc55`; orig `82beb1b`; failure_count 1; RE-SCOPED, operator-approved) — DISCREPANCY: the AdaptiveController was ALREADY wired live since 1.3.0; re-scoped to TEST-ONLY keeping opt-in (8 tests/adaptive_wire.rs: AIMD shrink/recover, Off-path run_concurrent, first-error-wins). **FLAKY-FIX:** the recovery test was timing+CPU-flaky under full-suite load (ControllerDriver::tick() samples real clock + live CPU; parallel load tripped the >85% CPU guardrail). Fixed via a behavior-preserving injectable `tick_with(now,cpu,rss)` seam; verified full workspace green 3x. No production behavior change.
> - `backend-rate-limits` ✅ **PASSED** (code `4edb244` @ 2026-06-08) — `limits.rs::for_scheme()` table (s3 5500/3500 rps bytes-∞; gcs 5000/1000 rps bytes-∞; b2 20/50 rps 25/100 MiB/s; file/unknown ∞, cited to AWS/GCS/Backblaze) + `TransferConfig.max_requests_per_sec` and a req/s limiter REUSING the existing token bucket. 7 `limits::` tests green. DESC-CORRECTION: BackendLimits = rate caps only (retry policy stays global). Live wiring deferred to `stores-backoff-wire`.
> - `retry-backoff-core` (stores) — `retry.rs`: `RetryPolicy` + full-jitter backoff honouring `Retry-After`, injectable `Sleeper`/`Jitter`, SDK-agnostic `Attempt{err,transient,retry_after}` engine. →
> - `stores-backoff-wire` (stores, dep retry-backoff-core + backend-rate-limits) — wrap key_exists/get_bytes/put_bytes through `retry_async`, extract Retry-After at the concrete SDK boundary, DISABLE SDK built-in retries. →
> - `adaptive-wire-live` (stores, dep backend-rate-limits) — `run_adaptive` in fetch/push fed by `classify_error()`; ON by default for network stores, `file://` stays `run_concurrent`. →
> - `ratelimit-cli-select` (cli, dep retry-backoff-core + backend-rate-limits) — `--max-retries/--retry-base-ms/--retry-max-ms/--max-rate/--max-requests/--adaptive` + `SNAPDIR_*` via manual-env resolvers; `resolve_rate_limits(scheme)` layering user>for_scheme>global. →
> - `ratelimit-docs` (docs, dep ratelimit-cli-select). →
> - `phase21-complete` (generic, human✋, dep all-6) — `cargo test --workspace --locked` + operator sign-off.
>
> **Invariants:** zero new deps (dependency-cooldown honoured); `file://` path + frozen format UNTOUCHED; blake3 behaviour unchanged; re-verify the 3 SHA locks each tick.

---

## ✅ PHASE 20 COMPLETE — 3/3 gates green (operator sign-off 2026-06-08) — input-path normalization + `--store` SNAPDIR_STORE default

> **Re-scoped 2026-06-08 (operator):** the env/flag-selectable **checksum** work was **REMOVED** from this phase (its 3 gates dropped; held WIP stashed in `stash@{0}`; the sha256/md5-can't-round-trip-through-blake3-stores escalation is now moot). Phase 20 shipped two CLI bug fixes.
>
> - `push-path-normalize` ✅ **PASSED** (code `4075b53`) — `resolve_root` (cli.rs) lexically normalizes the absolute root via `lexically_normalize_root` over `Path::components()` (drops `.` segments, strips trailing `/`, preserves `..`/RootDir, NO canonicalize) so `foo`/`./foo`/`foo/`/`./foo/` all hand the frozen walk an identical clean root → spec `./`-relative output for all four. 5 `path_normalize` tests green; core walk.rs/merkle.rs UNTOUCHED.
> - `store-env-default` ✅ **PASSED** (code `5d5d8f7`) — clap `env = "SNAPDIR_STORE"` on the global `--store` + `sync --from`; explicit flag overrides, neither → existing required-arg error preserved; `--to` stays explicit + from≠to differ-check intact. 7 `store_env` tests green; 16 trycmd snapshots refreshed (only `[env: SNAPDIR_STORE=]` added).
> - `phase20-complete` ✅ **PASSED** (human✋ operator sign-off 2026-06-08; `git bb5c0fa`) — `cargo test --workspace --locked` exit 0, 0 failed across 32 binaries. Frozen merkle.rs/walk contract untouched, ZERO new deps.

---

## ✅ PHASE 19 COMPLETE — 6/6 gates green (operator sign-off 2026-06-05) — snapdir 1.3.0 RELEASED — ALL 136 GATES GREEN

> **Progress:**
> - `release-prep-1.3.0` ✅ PASSED (code `c4e9e08`) — bump 1.2.0→1.3.0 + `[1.3.0]` CHANGELOG + compare links.
> - `aimd-typos-allowlist` ✅ PASSED (code `d70f9d6`) — **release-blocking ci fix discovered during the push:** `typos` rejected the acronym `AIMD` (Phase::Aimd enum + CHANGELOG); added `aimd` to `_typos.toml` allowlist so `typos`/CI-lint pass. (The Phase-18 gates ran cargo build/test, not typos, so it slipped through.)
>
> **The 8 commits to cherry-pick onto `release-verify/1.3.0` = the 6 Phase-18 SHAs (`de3fb7e b771917 f408032 5b05b41 e37267b e55c28b`) + the prep `c4e9e08` + the typos fix `d70f9d6`.** The local release-verify/1.3.0 branch currently has the first 7; the typos fix (`d70f9d6`) must be cherry-picked on before re-pushing (the earlier push was blocked by the pre-push typos gate).
>
> - `release-verify-branch-1.3.0` ✅ PASSED (operator confirm 2026-06-05; branch `01b1797`) — `release-verify/1.3.0` (8 gatesmith-free commits, tip 1.3.0, ZERO .gatesmith) pushed to origin; bench-verify run `27032438443` → **success**, artifact consulted: determinism 5+3 pass incl. `determinism_adaptive_round_trip_preserves_snapshot_id`, iai Ok/0-regressed.
>
> - `release-pr-upstream-1.3.0` ✅ PASSED (operator-authorized merge 2026-06-05; merge `2c50936`) — PR snapdir/snapdir#10 `release: 1.3.0` went 15/15 CI green and was squash-merged; **upstream/main now at 1.3.0 with ZERO .gatesmith tracked**. Canonical repo is on 1.3.0.
>
> - `release-tag-crates-1.3.0` ✅ PASSED (operator confirm 2026-06-05; tag `b8ac3a2`) — **snapdir 1.3.0 RELEASED.** Tag `v1.3.0` → release.yml run `27035729846` cut the GitHub release (live, 13 assets) + published **all 4 crates at 1.3.0** via the idempotent loop (no 403s; TP already registered). `cargo install snapdir-cli` → 1.3.0.
> - `phase19-complete` ✅ PASSED (operator sign-off 2026-06-05; `cargo test --workspace --locked` 377 passed/0 failed). **Phase 19 complete. snapdir 1.3.0 shipped. All 136 gates green.**
>
> **OPEN follow-ups (noted, not gated):** docs prose for `--adaptive`/`--max-jobs` (clap auto-generates man/completions; CHANGELOG already covers it); a committed iai-bench baseline; **Phase 20 `snapdir export`** (designed — the next FEATURE phase). dev carries `.gatesmith`; main/upstream never do.

> Operator-requested: ship the Phase-18 opt-in `--adaptive` transfer tuner + clearer progress line as **1.3.0**, full verify-branch + artifact path (same as the 1.2.0 release). **Lighter than 1.2.0:** bench-verify.yml, the idempotent `release.yml` publish, and Trusted Publishing for all 4 crates already shipped with 1.2.0 → no new CI/packaging gates, **no operator prerequisite** (just tag and go).
>
> **Gates (5, linear chain), ready head = `release-prep-1.3.0`:**
> - `release-prep-1.3.0` (generic, machine) — bump 1.2.0→1.3.0 + internal deps + lock + `[1.3.0]` CHANGELOG (--adaptive + progress) →
> - `release-verify-branch-1.3.0` (generic, human✋) — cherry-pick the 6 Phase-18 SHAs (`de3fb7e b771917 f408032 5b05b41 e37267b e55c28b`) + the prep onto `release-verify/1.3.0` off `upstream/main` (ZERO `.gatesmith`), push to origin → bench-verify artifact (determinism incl. adaptive round-trip + iai + criterion) → consult →
> - `release-pr-upstream-1.3.0` (generic, human✋) — PR → snapdir/snapdir, merge at 1.3.0 →
> - `release-tag-crates-1.3.0` (packaging, human✋) — tag `v1.3.0` → GH release + idempotent crates publish (TP already done) →
> - `phase19-complete` (generic, human✋) — sign-off.
>
> No frozen-interface mutation (version bump + CHANGELOG + orchestration). `release-prep-1.3.0` lands gatesmith-free on dev (cherry-pickable). **DEFERRED:** Phase 20 `snapdir export` (next FEATURE phase after this release).

---

## ✅ PHASE 18 COMPLETE — 7/7 gates green (operator sign-off 2026-06-05) — ALL 130 GATES GREEN

> **Progress:**
> - `adaptive-sys-samplers` ✅ PASSED (code `de3fb7e`) — NEW `snapdir-core/src/resources.rs`: best-effort CPU%-of-capacity (getrusage), RSS (proc/mach), total-RAM (sysconf/hw.memsize) samplers (Option/None-safe) + `libc` edge; Meter += advisory `current_limit`/`target_rate` atoms.
> - `adaptive-controller` ✅ PASSED (code `b771917`) — NEW `snapdir-stores/src/adaptive.rs`: `AdaptiveGate` (resizable permit pool, async tokio-Semaphore + zero-dep Mutex/Condvar blocking semaphore, one `set_limit`, no-deadlock shrink) + `set_rate` on both rate limiters + the PURE deterministic `AdaptiveController` (slow-start→AIMD + gradient + throttle/CPU/memory/ceiling guardrails + ~15s re-probe + fraction target; injected clock/metrics). 12 adaptive tests.
>
> - `adaptive-wire` ✅ PASSED (code `f408032`) — plugged the gate/controller into BOTH backends (async push/fetch via gate-acquire + 250ms tokio tick driver; rayon FileStore/sync via acquire_blocking + std::thread tick driver), `TransferConfig.adaptive: AdaptivePolicy` (Off default — unchanged; On{fraction,ceiling} via `with_adaptive`), `classify_error→Throttle|HardErr`. Off path byte-identical; 7 wire tests incl Off-vs-On same snapshot id + concurrency≤ceiling.
>
> - `adaptive-cli` ✅ PASSED (code `5b05b41`) — global `--adaptive[=FRACTION]` (default 0.8, env SNAPDIR_ADAPTIVE) + `--max-jobs` (ceiling, cap 64); `transfer_config()` builds `AdaptivePolicy::On` only when the flag is present (default path byte-for-byte unchanged, proven by `adaptive_unset_equals_pre_phase18_value`); `--verbose` reports the target+ceiling; fixed the carry-forward MeterSnapshot literal + regenerated --help snapshots. **`cargo test --workspace` compiles again.**
>
> - `adaptive-determinism-recheck` ✅ PASSED (code `e37267b`) — NEW `determinism_adaptive_round_trip_preserves_snapshot_id` (push→sync→fetch under `AdaptivePolicy::On{0.8, ceiling 2}` on all 3 legs) re-ids to the SAME golden as the non-adaptive path → **speed-only/byte-identical invariant proven**; Phase-16 suite still green.
>
> - `progress-clarity-eta` ✅ PASSED (code `e55c28b`) — renderer rework: byte-based ETA refreshed ≤2s + damped (`--` until stable); `<done>/<total> files` vs unit-suffixed sizes; fixed-width/latched-unit columns so digits don't reflow (test asserts constant column offsets); adaptive readout `jobs <in>/<ceiling> (auto <fraction>)` using the TRUE `--adaptive` fraction threaded from the CLI (PM rejected a first attempt that showed a bogus `current_limit/target_rate` ratio).
>
> - `phase18-complete` ✅ PASSED (operator sign-off 2026-06-05; `cargo test --workspace --locked` → 377 passed / 0 failed). **Phase 18 complete. All 130 gates green.**
>
> **Code lands on `dev`** (gatesmith-free, cherry-pickable toward a future **1.3.0**); nothing pushed. **DEFERRED (noted, not gated):** docs prose for `--adaptive`/`--max-jobs` (rides next release-prep); the iai-bench committed baseline follow-up; **Phase 19 `snapdir export`** (designed in the plan — sync sibling that materializes the expanded tree via a new `ExpandedSink`).

> Operator-requested: an **opt-in** `--adaptive[=FRACTION]` (default **0.8**) in-band congestion-control tuner for transfers, plus a clearer, width-stable status line with a smoothed ETA. **Adaptive is OPT-IN — default behavior is unchanged (full speed, OS schedules);** the polite tuner (which holds at a fraction of capacity to spare the host/neighbours) only engages when asked. The flag's optional arg IS the politeness fraction.
>
> **Design (proven congestion control):** TCP slow-start → AIMD + latency-gradient (TCP Vegas / Netflix concurrency-limits) + Little's Law; in-band measurement (the real get/put ops are the probe — no separate partial-file probe); guardrails = throttle/timeout backoff + CPU ~85% + memory-budget cap + ceiling, ~15s re-probe, operate at FRACTION×knee. One resizable `AdaptiveGate` (tokio Semaphore for async S3/GCS/B2 + hand-rolled zero-dep counting semaphore for the rayon local/sync path) makes one controller work across all stores. Correctness invariant: **speed-only, byte-identical output** (guarded 3×). Plus a renderer rework: byte-based smoothed/≤2s-throttled/damped ETA + `…/… files` vs `…/… <unit>` labels + fixed-width fields so moving digits don't reflow.
>
> **Gates (7), single ready head = `adaptive-sys-samplers`:**
> - `adaptive-sys-samplers` (core) — zero-dep CPU%/RSS/total-RAM samplers + Meter `current_limit`/`target_rate` atoms → fans out to:
> - `adaptive-controller` (stores) — AdaptiveGate + `set_rate` + the deterministic controller (slow-start→AIMD + gradient + guardrails)
> - `progress-clarity-eta` (cli) — the renderer rework (parallel with adaptive-controller)
> - `adaptive-wire` (stores ← controller) — wire both backends + error classification; byte-identical invariant
> - `adaptive-cli` (cli ← wire) — `--adaptive[=FRACTION]` + `--max-jobs`; default path unchanged
> - `adaptive-determinism-recheck` (bench ← wire) — proves `--adaptive` doesn't change snapshot ids
> - `phase18-complete` (generic, human_checkpoint) — sign-off
>
> No frozen-interface mutation (adaptive changes scheduling/rate only; manifest locks untouched); zero new deps. **DEFERRED (noted, not gated now):** docs prose for the new flags (rides next release-prep) + **Phase 19 `snapdir export`** (sync sibling materializing the expanded file tree via a new `ExpandedSink`; designed in the plan). All gatesmith-free code lands on `dev`, cherry-pickable toward a future 1.3.0.

---

## ✅ PHASE 17 COMPLETE — 7/7 gates green (operator sign-off 2026-06-05) — snapdir 1.2.0 RELEASED — ALL 123 GATES GREEN

> **Progress:**
> - `release-bench-verify-workflow` ✅ PASSED (code `5ed8851`) — NEW `.github/workflows/bench-verify.yml`: on `release-verify/**` push + dispatch, runs the determinism test + iai_hot 5%-gate (both fail the job) + criterion pipeline/hot_paths (informational), uploads `bench-verify-1.2.0` (target/criterion + target/iai + bench-output.log) `if:always()`.
> - `release-crates-idempotent` ✅ PASSED (code `a72ddd3`) — `release.yml` crates publish now queries the crates.io sparse index and SKIPS any already-published `crate@version` (safe partial-release re-runs; fixes the 1.1.0 death); first-publish 404-safe; order/auth/`--locked` unchanged; comment flags the TP-registration gap for catalog/stores/cli.
> - `release-prep-1.2.0` ✅ PASSED (code `0ff697f`) — Cargo.toml bumped 1.1.0→1.2.0 (4 lines) + Cargo.lock regenerated (workspace-only, external deps untouched) + `[1.2.0]` CHANGELOG (sync / progress dashboard / benchmark suite + interop note) + fixed compare-link block. **All 3 engineering gates done.**
>
> - `release-verify-branch` ✅ PASSED (operator confirm 2026-06-05; branch `887e00c`) — `release-verify/1.2.0` (off upstream/main, 17 gatesmith-free commits, tip 1.2.0, **ZERO .gatesmith**) pushed to origin; bench-verify run `26992716420` → **success**, artifact `bench-verify-1.2.0` consulted: determinism+round-trip 4+3 passed/0 failed, criterion data captured, iai Ok/0-regressed (⚠ 0|N/A counts — first run, no committed baseline → vacuous pass; **follow-up: commit an iai baseline so the 5% gate enforces**).
>
> - `release-pr-upstream` ✅ PASSED (operator confirm 2026-06-05; merge `81dac7f`) — PR snapdir/snapdir#9 `release: 1.2.0` merged; **upstream/main now at 1.2.0 with ZERO .gatesmith tracked**. Canonical repo is on 1.2.0.
>
> - `release-tag-crates-1.2.0` ✅ PASSED (operator confirm 2026-06-05; tag `73f2ab5`) — **snapdir 1.2.0 RELEASED.** Tag `v1.2.0` on snapdir/snapdir → release.yml run `27010910407` cut the GitHub release (live, 13 assets: 6 signed cross-target archives + checksums + SHA256SUMS + SLSA attest) and published **all 4 crates at 1.2.0** (core/catalog/stores/cli). TP worked for all four (operator registered it for the 3); the idempotent loop fixed the 1.1.0 partial-publish failure end-to-end. `cargo install snapdir-cli` → 1.2.0.
>
> - `phase17-complete` ✅ PASSED (operator sign-off 2026-06-05) — snapdir **1.2.0 is fully released**: bench-verified on a branch via the uploaded GitHub artifact, PR-merged to snapdir/snapdir, tagged `v1.2.0` with a live GitHub release, all 4 crates published via the hardened idempotent loop; frozen format untouched. **Phase 17 complete. All 123 gates green.**
>
> **OPEN POST-RELEASE FOLLOW-UP (not a blocker):** give the iai instruction-count bench a committed baseline so its 5% perf gate actually enforces (it currently reads `0|N/A` → vacuous pass). Candidate for a future Phase 18 gate or a quick bench-lane fix.
>
> **POST-RELEASE FOLLOW-UP (noted in journal):** the iai instruction-count bench currently reports `0|N/A` counts with no committed baseline, so the 5% perf gate passes vacuously. Give it a committed baseline (or fix the count capture) so it actually enforces. Not a 1.2.0 blocker.

> Operator-requested: ship the unreleased Phase 14 (sync) + 15 (progress dashboard) + 16 (benchmark suite) as **1.2.0**. Cherry-pick dev's gatesmith-free code onto a **bench-verification branch off `upstream/main`**, push to **origin (bermi)** so its Actions **run the bench suite + upload a consultable artifact**; once verified, **PR to snapdir/snapdir**, **tag v1.2.0**, **publish all 4 crates**. The 1.1.0 publish loop wasn't idempotent (failed mid-way) + TP only covers snapdir-core — Phase 17 hardens both. AMA: version 1.2.0 (minor; format unchanged → interop); verify branch carries **ZERO `.gatesmith`**; promote via PR; idempotent publish loop + operator registers TP for the 3 crates.
>
> **Gates (7):**
> - **Engineering (parallelizable, dep only `phase16-complete`):** `release-bench-verify-workflow` (ci — NEW `.github/workflows/bench-verify.yml`, runs determinism+iai+criterion, uploads `bench-verify-1.2.0` artifact) · `release-crates-idempotent` (packaging — `release.yml` skip-if-published guard) · `release-prep-1.2.0` (generic — bump 1.1.0→1.2.0 + `[1.2.0]` CHANGELOG).
> - **Orchestration (human_checkpoint; cross-branch/remote, done between ticks with real machine checks):** `release-verify-branch` (cherry-pick 14+3 onto `release-verify/1.2.0` off `upstream/main`, push to origin, consult artifact) → `release-pr-upstream` (PR → snapdir/snapdir, merge at 1.2.0) → `release-tag-crates-1.2.0` (register TP for the 3 → tag `v1.2.0` → GH release + idempotent crates publish) → `phase17-complete` (sign-off).
>
> **Ready head: the 3 engineering gates** (id-asc → `release-bench-verify-workflow` first). No frozen-interface mutation (workflow YAML + version bump + CHANGELOG only). crates.io PREREQ: operator registers Trusted Publishing for snapdir-catalog/-stores/-cli before the tag gate (see `release-crates-io-state` memory). dev carries `.gatesmith`; the verify branch + everything downstream do NOT.

---

## ✅ PHASE 16 COMPLETE — 6/6 gates green (operator sign-off 2026-06-05) — ALL 116 GATES GREEN

> **Progress:**
> - `bench-scenarios` ✅ PASSED (code `9870162`) — benches/src/lib.rs is now a real std-only deterministic synthetic-scenario generator + catalog (Scenario{name,tier,exclude}, materialize -> regular files+dirs only, 0644/0755, deterministic bytes, no rng/time; gate_scenarios() x8 + bench_scenarios() x5) with tests/scenarios.rs proving twice-stable snapshot ids + byte-determinism + dedup(unique<files).
> - `bench-criterion-suite` ✅ PASSED (code `14fcffe`) — NEW benches/benches/pipeline.rs (2nd criterion [[bench]]) with 5 wall-clock groups over bench_scenarios(): walk+hash, snapshot_id, stage/push, checkout/fetch, sync A->B; iter_batched + fresh store/iter to measure real work (defeats skip-if-present); Throughput::Bytes. +benches/README.md (save/compare baseline workflow). Dead Rust-vs-Bash compare.sh + last-compare.json removed.
> - `bench-determinism-gate` ✅ PASSED (code `85bd874`) — NEW benches/tests/determinism.rs (4 tests, runs in cargo test --workspace, <0.2s): per gate scenario golden snapshot_id + invariants (unique objects, bytes, entry count) vs a pinned GOLDENS table, dedup(unique<files), excludes-drop-subtree, and a FULL ROUND-TRIP (push->sync A->B->fetch->re-walk->re-id == golden). SNAPDIR_BENCH_REGEN=1 reprints goldens for intentional frozen-format changes. This is the deterministic regression gate AND integration test.
> - `bench-iai-gate` ✅ PASSED (code `e024218`) — NEW benches/benches/iai_hot.rs (iai-callgrind = "=0.16.1", 3rd [[bench]]): instruction-count benches (blake3 hash, walk+manifest, snapshot_id) over fixed tiny inputs with a 5% Ir/EstimatedCycles soft-limit regression gate. NEW executable benches/run-iai-docker.sh (pinned rust:1.91-slim-bookworm linux/amd64 + valgrind + runner 0.16.1) so macOS-via-Docker and CI share one baseline. README += instruction-count section.
>
> - `bench-ci-wire` ✅ PASSED (code `f1ae236`) — ci.yaml += Linux `bench-iai` job (toolchain 1.91.1, apt valgrind, iai-callgrind-runner 0.16.1, runs `cargo bench --bench iai_hot` → 5% regression gate in CI) + `cargo bench --workspace --no-run` compile step; utils/ci/pre-push.sh += the same compile-only check; deny.toml += `RUSTSEC-2025-0141` advisories-ignore (resolves the iai bincode-1.3.3 carry-forward blocker). actionlint clean, `cargo deny … advisories ok`.
>
> - `phase16-complete` ✅ PASSED (operator sign-off 2026-06-05; `cargo test --workspace --locked` exit 0, 0 failures). **Phase 16 complete. Project complete — all 116 gates green.**
>
> **DEFERRED (operator-driven, NOT part of the loop):** cherry-pick the Phase 14 (sync), 15 (progress), and 16 (benches) code commits `dev → main` toward a 1.2.0 release. crates.io still needs Trusted Publishing for catalog/stores/cli (only core has it) or a manual-token publish — see the `release-crates-io-state` memory. `dev` carries `.gatesmith/`; `main` never does.


> Operator-requested: a benchmark suite to make informed decisions on changes/refactorings/optimizations AND a deterministic baseline + gate to avoid regressions — local FS only, synthetic data (no network). Because snapdir is content-addressed, deterministic synthetic data -> deterministic manifest -> deterministic snapshot id, so the suite **doubles as integration testing** (assert final snapshot ids over a full local round-trip). macOS runs the valgrind/iai instruction-count gate inside a **pinned Linux Docker image** — the SAME image CI uses — so the committed baseline is authoritative on both.
>
> **Design:** a deterministic scenario generator (regular files + dirs only, explicit 0644/0755 perms, fixed bytes, no rng/time, NO symlinks) in the `benches` crate feeds three consumers: (1) a **determinism gate** = golden snapshot-id + structural-invariants + full local round-trip (push -> sync A->B -> fetch -> re-walk -> re-id), a normal `cargo test` that runs everywhere; (2) **criterion** wall-clock benches (decisions); (3) **iai-callgrind** instruction-count benches (the deterministic perf gate, Linux-CI/valgrind + macOS-Docker). The dead Rust-vs-Bash `benches/compare.sh` is removed. Frozen manifest/sharding untouched.
>
> **Gates (6):** `bench-scenarios` (bench) -> { `bench-determinism-gate` (bench), `bench-criterion-suite` (bench), `bench-iai-gate` (bench) } -> `bench-ci-wire` (ci) -> `phase16-complete` (generic, human_checkpoint). Ready head: **`bench-scenarios`**. Lanes: bench gates own `benches/`; `bench-ci-wire` owns `.github/workflows/ci.yaml` + `utils/ci/pre-push.sh`. All additive — no frozen-interface mutation.

---

## ✅ PHASE 15 COMPLETE — 6/6 gates green (operator sign-off 2026-06-04) — ALL 110 GATES GREEN

> Operator-requested: a beautiful single-line, self-updating stderr progress indicator (spinner/bar + from->to bps/iops + concurrency + best-effort mem/cpu) for push/fetch/pull/checkout/stage/sync AND the local walk/hash. ON only when stderr is a TTY (off when piped); --no-progress/--quiet/--color to control; honors NO_COLOR/TERM=dumb; stdout stays the scriptable id. HAND-ROLLED minimal-dep (anstyle+anstream already present, std IsTerminal; ONLY new dep is libc, already in the lock graph, for width ioctl + RSS/CPU). A pure Meter (lock-free atomics) lives in snapdir-core (sha-locked files untouched); the CLI renderer reads it.
>
> **Gates (6):** `progress-meter` ✅ (69a0133) -> `stores-meter-wire` (stores) + `cli-progress-renderer` ✅ (bca8e21) + `stores-meter-wire` ✅ (50f54b3) -> `cli-progress-flags-and-wire` ✅ (83deea1) -> `progress-verification` ✅ (87ed0ba) -> `phase15-complete` ✅ PASSED (operator sign-off; workspace 332/0). Phase 15 complete.

---

## ✅ PHASE 14 COMPLETE — 5/5 gates green (operator sign-off 2026-06-04) — ALL 104 GATES GREEN — PROJECT COMPLETE

> Operator-requested: `snapdir sync --id <id> --from <store> --to <store>` copies ONE snapshot (manifest + raw content-addressed objects) directly source-store -> dest-store, streaming through MEMORY only (no local-FS staging). New `StreamStore: Store` trait in snapdir-stores (get/put/has_object + put_manifest), a `sync_snapshot` orchestrator with NO `&Path` (structural no-disk guarantee) reusing the Phase-13 concurrency + rate limiter (manifest-last, skip-present/incremental), and a 15th `sync` CLI subcommand. **snapdir-core untouched**; frozen sharded keys + manifest format reused verbatim. dev brought to the 1.1.0 baseline (cherry-pick 9596a3d).
>
> **Gates (5):** `stream-store-trait` ✅ (88e5864) -> `sync-orchestrator` ✅ (5b2a899) -> `cli-sync-command` ✅ (327d1eb) -> `sync-verification` ✅ (d175a50) -> `phase14-complete` ✅ PASSED (operator sign-off; workspace 305/0). Phase 14 complete. **Project complete.** Next (deferred): cherry-pick the Phase 14 code commits dev->main toward a 1.2.0 release. Operator reviews the gates, then starts the ralph loop.

---

## ✅ PHASE 13 COMPLETE — 8/8 gates green (operator sign-off 2026-06-04) — ALL 99 GATES GREEN — PROJECT COMPLETE

> Operator-requested after Phase 12. **Audit:** all stores transfer objects **sequentially** (one-at-a-time `await` loop in a single `block_on`; B2 delegates to S3; FileStore sequential too); no concurrency/rate-limit primitives; no `--jobs`/`--limit-rate`. The `Store` trait is sync + per-manifest and **not frozen**, so concurrency goes *inside* `block_on` (`buffer_unordered`) without touching the trait or the frozen sharding/format.
>
> **Decisions:** concurrency **auto on by default** (`available_parallelism` capped at 16; `--jobs` overrides, `--jobs 1` = sequential); `--limit-rate` is **aggregate** (one shared **zero-dep** token bucket); **FileStore parallelized too** (rayon). Invariants preserved: manifest-last, skip-if-present (Phase 12), per-object verify, frozen layout. `TransferConfig` lives in `snapdir-stores` so **core is untouched**.

**Phase 13 ledger: 8 gates — 8 passed (PROJECT COMPLETE).**

- `transfer-config` ✅ PASSED (code `7fe2dab`) — `TransferConfig` + zero-dep token-bucket `RateLimiter` + `run_concurrent` (`buffer_unordered`) engine + backward-compat `*_with_config` ctors (old ctors delegate; cli unchanged). Deterministic test proves max-in-flight == min(concurrency, N) and == 1 at concurrency=1. `futures` + tokio time/sync added. No loop change yet.
- `concurrent-upload` ✅ PASSED (code `3ca5e6c`) — S3/GCS `push` upload concurrently via shared `push.rs` orchestrator; **manifest written only after all uploads succeed (all-or-nothing)**; skip-present + source verify + rate limit preserved. Hermetic test proves write_manifest is never called on a failed upload.
- `concurrent-download` ✅ PASSED (code `8af1352`) — S3/GCS `fetch_files` download concurrently via a shared `fetch.rs` orchestrator (skip-present short-circuits before any GET; rate-limited by manifest size; per-object verify + atomic write preserved). De-dups S3/GCS. Hermetic tests prove concurrency + skip + error-propagation.
- `filestore-parallel` ✅ PASSED (code `ea10f76`) — FileStore push/fetch copy objects in parallel via a rayon pool sized to `--jobs` (`parallel_copy`); manifest-last/all-or-nothing + skip-present + persist verify preserved; concurrency=1 stays sequential. Hermetic round-trip + all-or-nothing tests.
- `cli-transfer-flags` ✅ PASSED (code `94b5f75`) — `--jobs/-j` + `--limit-rate` (aggregate, wget-style) globals + env, `parse_rate`, and `Cli::transfer_config` threaded through `store_for_adapter` + `cache_store` via the `*_with_config` ctors. 15 help snapshots regenerated. Takes effect once the loops adopt the engine.
- `transfer-concurrency-verification` ✅ PASSED (code `373cd2d`) — 3 hermetic e2e tests: `--jobs 4`/`--jobs 1` round-trip byte-identical + identical snapshot id; `--limit-rate` accepted + correct. (No file:// throttle assertion — limiter is network-only.)
- `transfer-verbose-reports-jobs` ✅ PASSED (code `0b4e8e4`) — under `--verbose`, transfer commands print `transfers: N concurrent[, limit X]` to stderr (stdout byte-stable). Verified on the real binary.
- `phase13-complete` ✅ PASSED (operator sign-off; workspace 284/0). Phase 13 complete.

**Next step (deferred):** cherry-pick the Phase 12 (5) + Phase 13 (7) code commits `dev → main` (main never carries `.gatesmith/`), then push per [[gatesmith-branch-model]]. The human owns stopping ralph.

---

## ✅ PHASE 12 COMPLETE — 6/6 gates green (operator sign-off 2026-06-04) — superseded by Phase 13 above

> **PROJECT COMPLETE.** `phase12-complete` ✅ PASSED (operator sign-off + `cargo test --workspace --locked` = 254 passed / 0 failed). The four issues the operator reported while exercising the released 1.0.1 binary are all fixed and regression-tested, as **clean cherry-pickable code commits on `dev`** (gatesmith-free messages; `main` never carries `.gatesmith/`):
> - `cd1c5f1` `feat(cli)` — `--exclude`/`--paths` accept repeated + comma-delimited values, OR-combined (per-pattern `%system%`/`%common%` macro expansion).
> - `5552c1f` `fix(cli)` — `--dryrun` was a dead flag (never read → `push --dryrun` uploaded to GCS); now guards every mutating command (push/stage/fetch/checkout/pull/flush-cache + `verify-cache --purge`), zero writes.
> - `1f00981` `fix(stores)` — `fetch_files` skips dest files already present + checksum-verified (checkout leg); corrupt files repaired.
> - `89c3689` `perf(cli)` — `run_fetch` skips the store entirely when the snapshot is already cached (fetch leg) → a 2nd `pull` does **zero** store reads. *(PM-discovered: the checkout-leg fix alone left the network re-download; added `pull-fetch-skip-cached` after an empirical repro.)*
> - `9e0c85d` `test(cli)` — binary-level e2e correctness suite (idempotency, dryrun-no-writes, offline corrupt-repair, multi/comma exclude).
>
> The manifest "buffering" item is a documented **won't-fix** (global path-sort + bottom-up dir-merkle force a full walk before any line is valid — streaming would break the frozen format). Nothing pushed: the operator owns the cherry-pick `dev → main → snapdir/snapdir` and the push. The human owns stopping ralph.
>
> **Branch model (operator, 2026-06-04):** `dev` is the gatesmith workspace and the ONLY branch that carries `.gatesmith/`. `main` is the clean canonical 1.0.1 (`upstream/main`) and must **NEVER** contain `.gatesmith/`. Code fixes flow `dev → main → snapdir/snapdir` by **cherry-pick**, so each passing tick now commits the lane **code** first (gatesmith-free message — the recorded `git_sha`) and the `.gatesmith/` **ledger** separately (PM_PROMPT step 6).

**Phase 12 ledger: 6 gates — 5 passed, 1 pending (`phase12-complete`, human sign-off).**  *(grew from 5 → 6: PM added `pull-fetch-skip-cached` on 2026-06-04 after empirically finding `pull-skip-existing` only fixed the checkout leg, not the network re-download.)*

- `cli-list-options-multi` ✅ PASSED (code `cd1c5f1`) — `--exclude` (global + manifest) now takes `clap ArgAction::Append` + comma `value_delimiter`: repeated flags, comma lists, or a mix, **OR-combined** (a path is excluded if it matches ANY pattern). Each pattern is expanded individually via core `expand_excludes` (so `%system%`/`%common%` macros survive), wrapped in `(?:…)`, joined with `|` into one ERE → single `ExcludeMatcher`; single pattern is byte-identical, empty list disables filtering. `--paths` got the same arity (still unwired). `tests/list_options.rs` (7 fns) green; lane = cli only; SHA lock intact.
- `dryrun-honored` ✅ PASSED (code `5552c1f`) — `--dryrun` was a dead flag (never read), so `push --dryrun` uploaded objects (operator saw it on GCS). Now guards every persistent-write site on `self.globals.dryrun`: push (normal + staged `--id`), fetch, checkout, pull, stage, flush-cache, plus `verify-cache --purge` forced non-purging (report + non-zero exit kept). Prints `dry-run: would … (no writes)` to stderr; push/stage still print the id to stdout. `tests/dryrun.rs` (5) green.
- `pull-skip-existing` ✅ PASSED (code `1f00981`) — `fetch_files` now skips any dest file that exists with a matching locally-recomputed BLAKE3 (no copy for FileStore; no network GET for S3/GCS); corrupt/mismatched files fall through and are repaired. Shared `util::file_present_and_verified` across all three stores. Fetch-side only — keys/ordering/verify discipline + manifest format unchanged. Hermetic FileStore tests prove zero reads on a 2nd fetch (store objects deleted), corrupt-dest repair, and checksum-gated skip. **[fixed the severe re-download regression]**
- `pull-fetch-skip-cached` ✅ PASSED (code `89c3689`) — **[PM-added 2026-06-04]** `run_fetch` now returns early when the cache already holds the snapshot manifest (⇒ all objects present by the manifest-last invariant), so a 2nd `pull`/`fetch` of the same id does ZERO store reads. `--dryrun` + missing-store error precedence preserved. e2e test deletes store objects then pulls again → succeeds + re-manifests to same id; PM re-ran the binary-level repro to confirm. **Together with `pull-skip-existing` (checkout leg), the operator's pull re-download regression is now FULLY fixed.**
- `pull-push-correctness-suite` ✅ PASSED (code `9e0c85d`) — 4 binary-level e2e tests: push→pull→pull idempotency, `--dryrun` leaves store/dest/cache empty, corrupted-dest repaired from cache (offline), comma vs repeated `--exclude` agree and drop the right subtrees. Test-only; e2e 12/12 + store_roundtrip 4/4 green.
- `phase12-complete` ⏳ pending (generic, human_checkpoint) — Phase 12 sign-off.

**Next ready:** `phase12-complete` (generic, **human_checkpoint**) — all 5 production gates green; next tick ESCALATES to the operator for Phase 12 sign-off.

---

## ✅ PUSH HOLD LIFTED (2026-06-03) — `ci-green` passed, ci.yaml is GREEN

> The operator's push-hold (*"why push to paid CI before local gates exist?"*) is **cleared**: ci.yaml run 26891142751 @ a79c080 concluded **success — all 12 jobs green**. The local pre-push hook is **installed** (`make install-hooks` → `core.hooksPath=utils/git-hooks`), so every future push runs the full CI-equivalent suite first and blocks on failure. Normal (hook-guarded) pushing resumes; ledger-only pushes may use `git push --no-verify`.

## ✅ PHASE 11 COMPLETE — 85/85 gates green (signed off 2026-06-03; superseded by Phase 12 above)

`phase11-complete` ✅ PASSED (operator sign-off) — verification exit 0 (check-no-bash + check-crate-age "all 429 crates ≥3 days" + ADR index) AND operator approved. **Phase 11 (Modernize & de-bash) is complete; all 85 gates are green.** The `rust-port` branch is bash-free (oracle removed, byte-contract = `compat_golden.rs` + `manifest-format.sha.lock`, guarded by `check-no-bash.sh`), deps are modernized (rustls 0.23/hyper 1.x/ring, latest AWS SDK, unpinned google-cloud, MSRV 1.91.1) with a 3-day supply-chain cooldown, the scratch+musl+CA-certs image builds, **ci.yaml is fully green** with a hook-guarded local pre-push gate, and 27 ADRs + README/CONTRIBUTING/docs (CHANGELOG 0.6.0) are accurate. The human owns stopping ralph.

### Phase 11 — Modernize & de-bash — 85/85 gates passed (history below)

`readme-docs-final` ✅ PASSED (git 0e4969c) — **Groups D+E done.** README/CONTRIBUTING/docs/rust-port finalized: oracle prose → past-tense, accurate `FROM scratch` image story (static musl + bundled CA roots, zero runtime executables), install paths, ADR link, CHANGELOG **[0.6.0] Port-complete**. CONTRIBUTING's "frozen Bash oracle" section → "byte-format contract" (compat_golden.rs + SHA-lock). Hook-fast follow-up landed (git 944669f): the pre-push hook runs the fast legs (~2-4 min); musl+coverage in CI + `make ci-local`.

**Ledger integrity fix:** restored the `packaging-drop-windows` gate header (a pre-existing Phase-10 corruption had merged its body into `repo-makefile-rust` with duplicate keys); 85 gates now parse 1:1 (84 passed, 1 pending).

`no-bash-references-remain` ✅ PASSED (git 28a88c8) — **Group A (de-bash) COMPLETE.** `utils/ci/check-no-bash.sh` guards against oracle re-introduction/invocation (path-existence + live-invocation checks; excludes the new CI tooling + .git/target/.gatesmith/docs/*.rs comments + the sha-locked core files). Removed 3 dead bash-era scripts (`utils/{verify-docs,generate-docs,install}.sh`); scrubbed live-oracle comments → past-tense in 6 non-locked files. guard exit0, cargo build+test green (232/0), sha-locks intact. The Bash oracle + all live references are out of rust-port. **Remaining: `readme-docs-final` → `phase11-complete` (hc).**

`debash-gatesmith` ✅ PASSED — retired the oracle-differential methodology in `.gatesmith/`: `golden-fixtures.sha.lock` removed (only `manifest-format.sha.lock` remains), `PM_PROMPT.md` rewritten (FROZEN ORACLE → past-tense; byte-contract anchored on `compat_golden.rs` + the SHA-lock; ci-lane row de-staled). Oracle-differential gates (interop-*, remote-interop*, b2-*) stay `passed` as historical record, documented as archived. **Unblocks `no-bash-references-remain`** (last Group-A gate). 

`ci-green` ✅ PASSED (git a79c080) — **ci.yaml fully green** (run 26891142751: Lint, Supply-chain, Test matrix [1.91.1/stable/beta × ubuntu/macOS], both Static musl, Doctests, Coverage 78%). The confirmation push surfaced **4 distinct CI failures the local suite false-passed** (version/runtime divergence): mock-store `declare -A` (CI macOS bash 3.2), `rust-toolchain.toml` 1.96.0 pin shadowing the musl target + faking the matrix, **true MSRV 1.91.1** (AWS SDK; was falsely 1.85), and actionlint (install-action can't provide it → direct download + bump to 1.7.12 for the `macos-15-intel` label). All fixed.

`remove-bash-test-harnesses` ✅ PASSED (`58272a3`) — deleted the 4 bash differential harnesses; converted the 9 `walk::tests::*_matches_oracle` to pure-Rust golden constants (coverage preserved, cross-checked vs `compat_golden.rs`); scrubbed dead `oracle()`/skip branches + `./snapdir` tokens from cli tests. `cargo test --workspace --locked` = 232 passed / 0 failed; sha-locks intact; production walk.rs byte-identical. **PM also removed the stale `golden-fixtures.sha.lock`** (it locked the deleted qa-fixtures — false tripwire). Only `manifest-format.sha.lock` remains. **Unblocks `debash-gatesmith` + `ci-green`.** Left for `no-bash-references-remain`: `core/src/store.rs:248` comment + `catalog_logging.rs` comments.

`local-pre-push-gates` ✅ PASSED (local `0f967e8`, **unpushed**) — `utils/ci/pre-push.sh` mirrors all 6 ci.yaml groups (fmt · clippy `--all-features` -D · typos · actionlint · shear · semver · deny · audit · build/test · **musl debug+release** · doctests · **llvm-cov 75% floor**), blocks push on any failure; `make install-hooks` wires `core.hooksPath`; macOS musl builds in an amd64 rust container. **It surfaced the real red-tree failures** (narrower than CI implied): rustfmt drift · typos · 1 cargo-shear unused dep · 9 `walk::tests::*_matches_oracle` (oracle — cleared by `remove-bash-test-harnesses`). clippy `--all-features`/deny/audit/build/doctests/musl-debug already pass. **Path to `ci-green`:** `remove-bash-test-harnesses` → fix fmt/typos/shear → run the suite green → `ci-green` (the one allowed push).

`docker-modernize` ✅ PASSED (local commit, **unpushed** — push held) — self-contained root `Dockerfile` (rust:1.96-slim-bookworm builder → `FROM scratch` + CA-certs from the builder + native-arch musl binary; no bash/b3sum/apk), deleted `utils/website.dockerfile`, bumped `packaging/Dockerfile` certs to alpine:3.21, `.dockerignore` excludes target/.git. Runtime proof: `docker run --rm snapdir-phase11-test --version` → `snapdir 0.5.0` (25.4 MB scratch image).

`debash-ci` ✅ PASSED (git cc72605) — stripped the oracle from CI: deleted `sqlite3-catalog.yml`, `unit_tests.yml`, `build.yml`, `docs.yml` (oracle-only / built the deleted bash Dockerfile); repurposed `s3-store.yml`/`b2-store.yml` to drive the Rust env-gated round-trips; cleaned the devcontainer. actionlint clean; Rust coverage stays in `ci.yaml`.

**🔔 Backlog (operator request 2026-06-03):** ci.yaml is RED on rust-port (run 26877611844 @ cc1c626) and there's no local pre-push gate. Two gates added (Group F): **`ci-green`** (fix the red CI — Lint/`--all-features`/cargo-shear/cargo-semver-checks, Static musl, Coverage; the oracle Test failures clear via `remove-bash-test-harnesses`) and **`local-pre-push-gates`** (a git pre-push hook + installer running the full CI-equivalent suite — fmt, clippy `--all-features` -D, test, deny, **musl static build**, **coverage**, cargo-shear, cargo-semver-checks — that BLOCKS push on failure). `phase11-complete` now depends on both.

`remove-bash-oracle` ✅ PASSED (git 98cd355, **operator-approved + operator-applied** 2026-06-03) — **the frozen Bash oracle is retired from rust-port.** Deleted the 8 root scripts + the bash `Dockerfile` (alpine:3.16) + `utils/pre-commit-hook.sh` + `utils/qa-fixtures/`; operator stripped the `Bash(./snapdir*)` allow + `Edit(snapdir*)`/`Edit(utils/qa-fixtures/**)` deny rules from `.claude/settings.json` (the classifier hard-walled the PM from self-modifying permissions — exactly why this was a human_checkpoint). Byte-contract now guarded by `compat_golden.rs` + `manifest-format.sha.lock`. **Unblocks `debash-ci`, `debash-gatesmith`, `no-bash-references-remain`, `docker-modernize`.** Next eligible by id-asc: `debash-ci` (its dep remove-bash-oracle is now ✅). Carry-forward: `remove-bash-test-harnesses` must scrub the now-dead `oracle()` skip helpers + delete `tests/interop/run.sh` + `tests/integration/*.sh`.

`deps-verify` ✅ PASSED (git cc1c626, **operator sign-off** 2026-06-03) — **Group B (latest deps + 3-day cooldown) COMPLETE.** Two proofs on the upgraded tree: CI release dry-run **26861289316 = success** (all 6 unix targets incl. both musl static-link builds, 0 Windows, publish/Docker skipped) + a MinIO S3 round-trip on the new rustls 0.23/hyper 1.x/ring connector (byte-identical, snapshot-id stable; the symlink-id quirk is a pre-existing snapdir dereference semantic, not a deps regression). aws-lc/openssl-sys=0, crate-age ≥3d. **Next: Group A de-bash — `remove-bash-oracle` (human_checkpoint: operator must lift the `Edit(snapdir*)` / `Edit(utils/qa-fixtures/**)` deny + the `./snapdir*` allow rules in `.claude/settings.json`).**

`deps-upgrade-general` ✅ PASSED (git 5d1a13d) — re-ran on the deny-clean post-tls-sdk tree: bumped md-5 0.11 (67d) / sha2 0.11 (69d) / criterion 0.8 (118d) via targeted lock update (bitflags/log held aged); build/test/clippy/deny all green, crate-age ≥3d, aws-lc/openssl-sys absent. **Group B (latest deps + 3-day cooldown) is now complete except `deps-verify`** (human_checkpoint — CI release dry-run musl-static + MinIO round-trip on the upgraded tree).

`deps-upgrade-tls-sdk` ✅ PASSED (git b2d0676) — **the hard gate.** Collapsed the two TLS islands into the **rustls 0.23 / hyper 1.x / ring** island, deleting the OLD island the AWS S3 connector pulled (rustls 0.21, hyper-rustls 0.24, hyper 0.14, tokio-rustls 0.24, rustls-webpki 0.101, rustls-pemfile — all gone). S3 connector rewritten onto `aws-smithy-http-client` (rustls-ring) + hyper-1.x, **native roots kept** (decision #2); GCS unpinned (`google-cloud-storage`/`-gax` `=x`→`1`); `deny.toml` allows CDLA-Permissive-2.0; `rustls-native-certs` pinned 0.8.4→0.8.3 (cooldown). Verified: build+test -p snapdir-stores green, **aws-lc/openssl-sys absent**, `cargo deny check` clean (advisories/bans/licenses/sources ok — clears RUSTSEC-2026-0098/0099 + 2025-0134 + the CDLA license), crate-age ≥3 days. **This unblocks `deps-upgrade-general`** (deny now clean) — re-run the parked bumps (md-5 0.11, sha2 0.11, criterion 0.8, rustls-native-certs 0.8.3) from `handoffs/deps-upgrade-general-handoff.md`.

**Re-sequenced (GATE-FIX 2026-06-03):** `deps-upgrade-general` → `depends_on: [deps-cooldown-automation, deps-upgrade-tls-sdk]` (both now ✅) — eligible again; its `cargo deny check` was the only blocker and tls-sdk cleared it.

`deps-cooldown-automation` ✅ PASSED (git 89f2468) — Dependabot cooldown (default-days:3) + `utils/ci/check-crate-age.sh` (every dep ≥3 days old) + supply-chain.yml. **The check caught `rustls-native-certs 0.8.4` (<3 days); the pin to 0.8.3 is parked in the general handoff.** `debash-stores-comments` ✅ (git 56df3cd). `compat-golden-tests` ✅ (git ea4dcbc, owner GATE-OWNER-FIX'd tests→core) — `crates/snapdir-core/tests/compat_golden.rs` pins the frozen byte-format with 16 embedded-constant Rust tests. **The contract anchor** — `remove-bash-oracle` + `remove-bash-test-harnesses` unblocked. `adr-records` ✅ (git 9464f77, 26 MADR ADRs in `docs/adr/`).

**Eligible now:** `deps-upgrade-tls-sdk` (stores) · `remove-bash-test-harnesses` (tests) · `debash-ci`? (no — deps on remove-bash-oracle) · `remove-bash-oracle` (generic, **human_checkpoint** — operator lifts the `Edit(snapdir*)` / `Edit(utils/qa-fixtures/**)` deny rules in `.claude/settings.json`).

The Bash→Rust port (Phases 0–10) is **complete & verified, 67/67**. The operator opened **Phase 11** (plan: `~/.claude/plans/can-we-do-the-foamy-pike.md`) — 15 new gates to: **(A)** remove the legacy Bash oracle + all references (full cut; the byte-contract is re-anchored as pure-Rust golden-constant compat tests + `manifest-format.sha.lock`), **(B)** upgrade all deps to latest with a **≥3-day cooldown** (Dependabot/Renovate + a crates.io `created_at` check; unify rustls 0.23 / hyper-rustls 0.27 / hyper 1.x / latest AWS SDK / unpin google-cloud-storage, keep ring + native-certs), **(C)** modernize the Docker image (scratch + musl + CA roots; self-contained root Dockerfile), **(D)** author `docs/adr/` (~25 ADRs), **(E)** finalize README/docs.

Decisions (operator, AskUserQuestion 2026-06-03): full-cut de-bash w/ Rust golden tests · **keep native-certs** (scratch+CA-certs, no stores TLS change) · **full-latest** deps · just delete the bash (no archive). Methodology shift: removing the oracle retires the live oracle-differential model.

**Eligible now** (no unmet deps): `compat-golden-tests` (tests — land FIRST, the contract anchor) · `deps-cooldown-automation` (ci) · `debash-stores-comments` (stores) · `adr-records` (docs). `remove-bash-oracle` + `deps-verify` + `phase11-complete` are **human_checkpoints** (operator lifts the `Edit(snapdir*)` deny rules; confirms the dry-run; final sign-off).

## ✅ Phase 10 complete — 67/67 prior gates green (2026-06-03)

`remote-interop-b2` ✅ PASSED (git cfad28f) — the final scoped live harness run exited 0: **S3(MinIO) + GCS(real) full A/B/C differential byte-identical green; zero-external-dependency lane green; B2 (real sandbox) scoped green** (Rust round-trip vs real B2 + Rust↔Bash snapshot-id agreement; the legacy bash-oracle cold-fetch-from-B2 loudly documented as a known limitation). `backends ran: s3 gcs b2`. The Bash→Rust port is feature-complete and verified: frozen manifest/merkle/excludes format byte-identical to the oracle; redb catalog; native in-process S3/B2/GCS stores (zero external CLI dependency, proven by the PATH-sanitized lane); 14 CLI subcommands wired; coverage floor; release dry-run (6 unix targets, Windows dropped); Rust-only docs + README; cargo Makefile. The human owns project end (stop ralph).

(Historical projection below retained for the record.)
Active phase: **5 (final gate)** — **67/67 gates passed**. The live B2 run proved the Rust port works against real B2 (`snapdir-tests`, us-west-001): **S3(MinIO)+GCS(real)+zero-dep byte-identical green; B2 Lane A (Rust push→fetch→pull→verify) ALL GREEN live; Rust↔Bash agree on the B2 snapshot id (format/key compat)**. The only failures were in the **FROZEN BASH ORACLE's cold-cache fetch-from-B2 path** (legacy b2-CLI orchestration, multiple latent bugs: line-395 --store fixed, line-169 ensure-no-errors --store confirmed-needed, plus a deeper silent fetch failure) — *not* the shipped Rust binary nor the interop format. **Operator decision (AskUserQuestion 2026-06-03): scope the B2 gate to Rust-side + format/id compat** (full bidirectional byte-identical interop already proven on S3+GCS); the legacy bash-oracle cold-fetch-from-B2 is a documented known limitation of the retired tool. `remote-interop-b2` **GATE-BUMPed** accordingly (new scoped pass_criteria; fc reset 2→0) and now depends on new gate **`b2-lane-scope-rust-format`** (tests) which adjusts only the B2 lane to assert the proven subset + loud-document the legacy limitation (S3/GCS keep full differential). The B2 creds/sandbox are RESOLVED (operator created bucket `snapdir-tests` + key in `us-west-001`), and the **2026-06-03 live B2 investigation proved the Rust side is correct**: Lane A Rust↔Rust B2 push/fetch/pull/verify ALL pass, and Bash derives the identical snapshot id from a Rust-pushed B2 store. The remaining work is encoded as gates so the loop validates it properly (no hand-hacking, no false pass):
- **`b2-harness-isolation-and-cli`** (tests) ✅ **PASSED** (git 310e690) — applied two tests-lane fixes to `remote_stores_live.sh`: (a) `unset SNAPDIR_S3_STORE_ENDPOINT_URL` before the B2 lane (the S3/MinIO endpoint had leaked into the Rust B2 adapter via `cli.rs store_for_adapter` and misrouted the B2 PUT to MinIO → `NoSuchBucket` → silent skip); (b) provisions a pinned **b2 CLI v3** for the bash-oracle side (system `b2` is v4.7, which dropped the oracle's `upload-file`/`download-file-by-name`) via a `uvx --from 'b2<4' --with docutils==0.18.1 b2` shim. --self-check green; zero-dep PATH-sanitizer still hides b2.
- **`b2-oracle-fetch-store-arg`** (stores, pending, HUMAN CHECKPOINT) — FROZEN-ORACLE one-liner (operator-approved verbatim): `snapdir-b2-store:395` parallel object-fetch must pass `--store "${_SNAPDIR_B2_STORE_STORE}"` (mirroring the manifest fetch on line 204); the worker requires it (line 472). **Requires the operator to lift the `Edit(snapdir-b2-store)` deny** in `.claude/settings.json` so the stores lane can apply it in a tick (agents cannot self-modify permissions) — then restore it.
- **`remote-interop-b2`** (tests, pending, HUMAN CHECKPOINT) — now `depends_on` both gates above. Its `pass_criteria` already does proper validation (harness must print `b2 (real sandbox): all Bash<->Rust differential lanes passed`); once the two prereqs land it should go green → **PROJECT COMPLETE (65/65)**.

NEXT-TICK PLAN: next eligible pick is **`remote-interop-b2`** (tests, human_checkpoint, scoped pass_criteria, fc=0) — the PM re-runs the live harness (`source ~/.config/snapdir/test-creds.sh && bash tests/integration/remote_stores_live.sh`). With the B2 lane scoped (Rust round-trip + Rust↔Bash id agreement; legacy cold-fetch documented), the run should print `b2 (real sandbox, scoped: Rust + format compat)` and exit 0 → with S3+GCS+zero-dep green → **67/67 PROJECT COMPLETE**. The teammate already saw a full live run pass (s3 gcs b2). Needs `uvx` (present) + B2 creds sourced (in test-creds); no operator action.
**release-dryrun re-run FAILED (fc=1) — and again the dry-run caught a real issue.** The `[profile.dist]` fix worked: 6/7 targets built (x86_64/aarch64 linux gnu+musl + x86_64/aarch64 macOS) + gen-assets; publish jobs correctly skipped. But **x86_64-pc-windows-msvc failed** — snapdir is **unix-only** (manifest = unix octal perms + symlinks; `core/walk.rs:46` + `cli.rs:16` use `std::os::unix`). **Operator decided: drop Windows.** GATE-ADDed `packaging-drop-windows` (release.yml matrix + dist-workspace.toml) + `ci-drop-windows` (ci.yaml test matrix `windows-latest`, which is also red). After both land, the PM re-runs the gh dispatch dry-run (now 6 unix targets) → expected green → escalate `release-dryrun` sign-off. The frozen oracle scripts + manifest format stay unix-only by design.
FINDING (non-blocking, unowned root file): the repo `Makefile` still has a Retype-docs-site target that `cp docs/images/favicon.ico` (now deleted). Stale; the public docs are README + docs/rust-port/. No lane owns the Makefile — flag for a future cleanup (could extend ci or generic).

**Release dry-run FAILED — root cause + fix gate.** Operator ran `gh workflow run release.yml --ref rust-port -f dry_run=true`: gen-assets passed but ALL 8 build jobs failed because `release.yml` uses `--profile dist` while `[profile.dist]` is missing from the ROOT `Cargo.toml` (it only lives in `packaging/dist-workspace.toml`). cargo needs the profile at the workspace root → `error: profile 'dist' is not defined`. Fixed by **`ci-dist-profile`** (ci lane adds `[profile.dist]` to root Cargo.toml). After it lands the PM re-runs the dry-run (`gh workflow run … && gh run watch`) and escalates the real result for the `release-dryrun` sign-off.

**Rust-only public docs (operator).** GATE-ADDED `docs-remove-bash-legacy` (delete root bash-era docs `docs/index|install|guide|authoring-stores|understanding-manifests|*-readme.md` + `docs/retype.yml` + `docs/api/**`; rewrite CONTRIBUTING for Rust; KEEP `docs/rust-port/**` incl. migration.md as transitional) and `readme-rewrite` (rewrite README Rust-only/AI-slop-free/discoverable; machine checks + operator human_confirm). The `docs` lane was **extended** to own `README.md`, `CONTRIBUTING.md`, root `docs/` (LANE-SCOPE, PM_PROMPT.md updated). README discoverability guidance is in `/Users/bermi/.claude/plans/can-we-do-the-foamy-pike.md` §D.

**verify --purge resolution.** Operator chose to REMOVE the inert flag → `cli-verify-purge-reject` (cli) makes `verify` reject `--purge` with a clear error (no purge path; --purge is verify-cache's). A premature PM hand-edit was reverted; the cli lane does it via the gate.

Open finding (future, no gate): the `Store` trait lacks object copy/delete → `fetch` double-copies (a store-API extension).
⚠ **FEATURE-COMPLETENESS GAP (operator-flagged at migration-guide sign-off).** 7 of 14 subcommands were never CLI-wired despite their library logic existing: `stage`/`verify-cache`/`flush-cache` (→ `snapdir_core::cache`), `locations`/`ancestors`/`revisions` (→ `snapdir_catalog`), `defaults`. The original ledger gated manifest/id + push/fetch/pull/checkout/verify + remote routing but MISSED these. GATE-ADDED `cli-cache-commands` + `cli-catalog-commands` + `cli-defaults` (cli, P6) to wire them, `migration-guide-refresh` (docs, P9) to update the guide's wired-vs-stub table afterward, and made `release-dryrun` depend on all four — **release cannot sign off until the CLI is feature-complete**. Each cli gate's pass_criteria guards against a vacuous 0-test filter via regex `running [1-9]`.
Last passed: `migration-guide` @ 2026-06-01T14:32:56Z — **operator-signed-off HUMAN CHECKPOINT** ("the guide is ok"). The guide honestly documents the current state; the operator's feature-completeness concern is tracked by the new CLI gates above. Contract frozen (locks 4/4 OK each tick).
`cargo-llvm-cov` 0.8.7 + `llvm-tools-preview` installed locally (PM). No nightly on host (cargo-fuzz can't build locally; CI cron does).
Last passed: `rustdoc-doctests` @ 2026-06-01T14:25:07Z — `docs/rust-port/manifest-spec.md` (faithful frozen-format spec; snapshot-id correctly = b3sum of `#`-stripped manifest text, not root checksum) + `CHANGELOG.md` (Keep a Changelog, 0.5.0); doctests green (core 2 + stores 1). PM also fixed a stale `PM_PROMPT.md` "snapshot ID = root dir checksum" line (DESC-CORRECTION). Contract frozen (locks 4/4 OK each tick).
GATE-OWNER-FIX pattern (this phase): gates verified by `cargo test -p <crate>` but nominally owned by `tests` are corrected to the crate's lane — `cli-trycmd → cli`, `proptest-roundtrip → core`. `fuzz-parser` (`tests/fuzz/...`, `cargo fuzz`) genuinely IS the `tests` lane (top-level `tests/`).
CROSS-LANE follow-up (cli): (a) wire `verify-cache`/`flush-cache` to `snapdir_core::cache` + call `check_snapshot_integrity` in `checkout`/`verify` (resolve `${XDG_CACHE_HOME:-$HOME/.cache}/snapdir`); (b) wire the `catalog` subcommands to `snapdir_catalog::Catalog`.

**Remote-interop is now PM-auto-verified, not a human rubber-stamp.** `remote-interop` runs `bash tests/integration/remote_stores_live.sh` every tick: MinIO S3 Bash↔Rust cross-tool (byte-identical) + a zero-external-dependency lane (Rust round-trip with `aws`/`b2`/`gcloud` removed from PATH). `remote-interop-gcs` proves the same against the **real** `gs://snapdir-integration-testing` bucket (ADC). Both green. `gcs-store-notfound-fix` repaired a real GcsStore bug — `key_exists`/`get_bytes` only treated HTTP 404 as absent, but `google-cloud-storage` v1.12 reports a missing object as service-level `Code::NotFound` (`http_status_code()==None`), so skip-if-present aborted **every** real GCS push; fixed via an `is_not_found()` helper.

**B2 (`remote-interop-b2`)** — endpoint/region prereq RESOLVED 2026-06-03 (bucket `snapdir-tests`, key + endpoint in `us-west-001`; creds verified). No local emulator serves both B2-native + S3 APIs, so it uses the real sandbox. Now gated on the two new dependency gates (see top): the harness endpoint-isolation + v3-b2 provision, and the frozen-oracle `--store` one-liner. The 2026-06-03 investigation (full forensics in journal) confirmed the Rust B2 store itself is correct.

**Lesson (gate-design principle):** a `human_confirm` must be paired with a real machine check wherever feasible — `remote-interop` was the only *hollow* gate (echo + lone human_confirm) and nearly produced a false pass. Remaining human gates legitimately need external systems: `ci-matrix-green` (GitHub Actions), `coverage-gate` (Codecov), `release-dryrun` (tag/CI); `perf-gate`/`migration-guide` already pair human_confirm with a real `exit_code` check.

Live-verification env (for the PM/loop): GCS needs only ambient ADC (`gcloud auth application-default login` as `bermi@bermilabs.com`, project `snapdir-development`) + the bucket path (hardcoded in the gate). S3 is hermetic (docker MinIO, no creds). B2 needs `source ~/.config/snapdir/test-creds.sh` + the endpoint fix above.
TLS pattern established for remote stores: `default-features=false` + ring rustls — keeps aws-lc-rs out. Re-run `grep -i aws-lc Cargo.lock` (must be empty) after each remote-store gate.
Open (non-blocking) findings to revisit later:
- `Store` trait lacks object copy/delete → fetch double-copies; `verify --purge` can't remove corrupt objects yet (verify-cache/cache-id or a store-API extension).
- FROZEN oracle macOS bug: `snapdir` L2163-2170 `_snapdir_absolute_path` can't checkout nested dirs on macOS (no `realpath -m`); Rust is correct. Bash-side macOS limitation only — not ours to fix (oracle is frozen).
- FROZEN oracle space-path bug: `snapdir` L1276 `IFS=' ' read -r -a line_parts` then `line_parts[4]` truncates space-bearing paths on Bash **push** (reproduces vs `file://`; Rust pushes spaces fine). Interop harness lane-C uses a no-space corpus for the Bash-push direction. Oracle is frozen — not ours to fix.

> **🔒 FROZEN INTERFACES — re-verify EVERY tick (READ STATE step):**
> `shasum -a 256 -c .gatesmith/golden-fixtures.sha.lock .gatesmith/manifest-format.sha.lock`
> Both must report OK. A mismatch = CRITICAL ALERT → `AskUserQuestion`, do nothing else.
> Locks cover: `utils/qa-fixtures/expected-guide-commands.txt` (golden corpus) and the
> format-defining core source (`manifest.rs`/`merkle.rs`/`excludes.rs`). Any change to
> line format / ordering / checksum algorithm / sharding / exclude sets now needs a
> human-approved frozen-interface mutation (`## Proposal` → AskUserQuestion).

> **⚠ CONTRACT DISCREPANCY (escalated to human @ 2026-05-31T01:55:16Z).** The oracle (`snapdir` L259/762/436/776) derives the snapshot ID as `manifest | grep -v '^#' | b3sum --no-names` — BLAKE3 of the **full manifest text** (incl. trailing newline), NOT the root directory checksum. PLAN.md's frozen-contract line "Root dir checksum = snapshot ID", the `dir-merkle` gate description, and `merkle.rs` doc comments + the `snapshot_id_equals_root_directory_checksum` test all encode the doc bug. The `directory_checksum` function is correct (it computes the `D ./` line's CHECKSUM field); only the "= snapshot id" labeling is wrong. golden-b3's tests use the correct derivation (ids c678a299…/8af03a1b…) + a guard test. **Must be corrected before freeze-contract** so the frozen contract and keystone interop gate key on the real snapshot ID.

## Phase summary

- Phase 0 (Bootstrap): 1/1 passed ✅
- Phase 1 (Scaffolding + CI): 6/6 passed ✅
- Phase 2 (Core manifest/hashing + FREEZE): 7/7 passed ✅ 🔒 FROZEN
- Phase 3 (Interop keystone, HARD): 4/4 passed ✅ 🔑 KEYSTONE PROVEN
- Phase 4 (Store trait + FileStore): 5/5 passed ✅
- Phase 5 (Remote stores): 8/9 passed (S3+GCS interop PM-verified; only `remote-interop-b2` — operator-deferred to post-release-candidate — remains)
- Phase 6 (Caching + redb catalog + CLI wiring): 9/9 passed ✅ (…`cli-catalog-logging-parity` `cli-verify-purge-reject`) — CLI feature-complete, catalog logging at oracle parity, verify --purge rejected
- Phase 7 (Performance): 4/4 passed ✅ (`bench-scaffold` `bench-compile` `perf-harness` `perf-gate` — Rust 33.6×/2.69× faster, output byte-identical, operator-signed-off)
- Phase 8 (Testing/fuzzing): 5/5 passed ✅ (`cli-trycmd` `proptest-roundtrip` `coverage-gate` 75%-floor `ci-coverage-floor` `fuzz-parser`)
- Phase 9 (Documentation): 6/6 passed ✅ (rustdoc-doctests, migration-guide-draft/-guide/-refresh, `docs-remove-bash-legacy`, `readme-rewrite` — Rust-only public docs + operator-signed-off README)
- Phase 10 (Packaging/release): 8/8 passed ✅ (`release-config` `cli-completions-man` `release-dry-run-mode` `ci-dist-profile` `ci-drop-windows` `packaging-drop-windows` `release-dryrun` `repo-makefile-rust` — release signed off, Windows dropped, cargo Makefile)

## Recent milestones

- 2026-05-31 — Phase 0 complete: plan vendored, gatesmith ledger + lane templates authored; bootstrap verified green.
- 2026-05-31 — `scaffold-workspace`: Cargo workspace (core/catalog/stores/cli) + pinned toolchain 1.96.0 builds clean (`cargo build --workspace --locked`); ring TLS stance, no aws-lc-rs.
- 2026-05-31 — `ci-config`: ci.yaml (lint/deny/test-matrix incl. musl static + coverage + semver), deny.toml (aws-lc-rs banned), _typos.toml authored; actionlint clean.
- 2026-05-31 — `clippy-pedantic-clean`: workspace pedantic lints (warn, prio -1) wired + all crates opt in; `cargo clippy --workspace --all-targets --all-features -- -D warnings` exit 0.
- 2026-05-31 — `cli-skeleton`: clap v4 derive `snapdir` binary exposes all 14 subcommands + global options (stubbed), pinned to the `./snapdir` oracle; help-surface regex passes.
- 2026-05-31 — `fmt-clean`: `cargo fmt --all --check` clean across the workspace (rustfmt.toml stable-compatible).
- 2026-05-31 — `ci-matrix-green` (human checkpoint): operator confirmed the full GitHub Actions matrix (Linux/macOS/Windows × MSRV/stable/beta + musl static) green on `rust-port`. **Phase 1 complete.**
- 2026-05-31 — `manifest-format`: `snapdir-core` manifest line model (`Manifest`/`ManifestEntry`) — Display `TYPE PERM CHECKSUM SIZE PATH`, sort -k5, `#`-comment/empty-line stripping, `./` vs `--absolute`; 15 unit tests, pinned to `./snapdir-manifest`.
- 2026-05-31 — `dir-merkle`: `directory_checksum` = sort -u + concat(no separator) + rehash of child checksums via in-process `blake3` crate (no b3sum shell-out); `Hasher` trait seam for `--checksum-bin`; 7 checksum tests. (Note: its "root checksum = snapshot id" claim is the doc bug now under escalation.)
- 2026-05-31 — `golden-b3`: 8 `golden_b3sum` tests reproduce the frozen ids byte-for-byte in-process (empty af1349b9, foo 49dc870d, root D-line dba5865c/4a0732cf, snapshot ids c678a299/8af03a1b). **Surfaced the snapshot-ID contract discrepancy (escalated).**
- 2026-05-31 — `snapshot-id-core-fix` (remediation): added `snapdir_core::snapshot_id(manifest, hasher)` = BLAKE3 of `Display` text + trailing `\n` (oracle-exact), reproducing c678a299…/8af03a1b…; relabeled `merkle.rs` docs + replaced the misleading test. Contract discrepancy resolved in core.
- 2026-05-31 — `golden-multi`: `Md5Hasher`/`Sha256Hasher`/`Blake3KeyedHasher` (`md-5`/`sha2`/`blake3 derive_key`, no shell-out) + `excludes.rs` (`%system%`/`%common%` sets verbatim from oracle, regex matcher, `FollowMode`); 19 tests. (verification_cmd GATE-BUMP-fixed from a hollow 0-test filter.)
- 2026-05-31 — `snapshot-id-doc-fix`: corrected PLAN.md frozen-contract (snapshot ID = b3sum of `#`-stripped manifest text, not the root dir checksum). Snapshot-ID discrepancy fully resolved (core + docs) ahead of the freeze.
- 2026-05-31 — `freeze-contract` (human checkpoint): operator approved the FREEZE. Manifest format + dir-merkle + snapshot-id + checksum modes + excludes + golden fixtures LOCKED via `.gatesmith/*.sha.lock`. **🔒 Phase 2 complete; the contract is now immutable without human approval.**
- 2026-05-31 — `interop-harness`: `tests/interop/run.sh` differential harness built (deterministic corpus, byte-identical Bash↔Rust diff across all checksum/keyed/no-follow modes); `--self-check` green. Flagged that interop-diff needs the core walk + CLI wiring first → added `core-walk` + `cli-manifest-wire` prereq gates.
- 2026-05-31 — `core-walk`: in-process FS walk (`src/walk.rs`) → frozen-format Manifest; 10 tests diff byte-for-byte vs the live `./snapdir-manifest` (b3/md5/sha256, symlink follow/no-follow, excludes). Matched the oracle's lstat-perms/target-checksum symlink rule. Frozen files untouched.
- 2026-05-31 — `cli-manifest-wire`: `snapdir manifest`/`id` wired to `snapdir-core` (thin layer; flags→WalkOptions/Hasher; keyed via `SNAPDIR_MANIFEST_CONTEXT`); 7 integration tests byte-identical vs the live oracle. `snapdir id` is checksum-mode-independent (always b3sum), matching the oracle.
- 2026-05-31 — `interop-diff` 🔑 **KEYSTONE** (human checkpoint): full `tests/interop/run.sh` → 15/15 corpus cases byte-identical Bash↔Rust (manifests + snapshot IDs, all checksum/keyed/no-follow modes); operator signed off. **Byte-for-byte interoperability proven; Phase 3 complete.**
- 2026-05-31 — `store-trait`: `snapdir-core::store` — `Store` trait (`get_manifest`/`fetch_files`/`push`, sync/object-safe) + sharded path helpers confirmed vs oracle (`snapdir` L1387/1399); 8 tests. Frozen files untouched.
- 2026-05-31 — `file-store`: `FileStore` (`file://`) impl of the `Store` trait — sharded `.objects`/`.manifests`, push (objects-before-manifest, skip-if-present), fetch (temp + verify BLAKE3 + retry≤5 + atomic rename); 10 tests. Mirrors `./snapdir-file-store`.
- 2026-05-31 — `external-store-shim`: `router.rs` (scheme→adapter, `gs`→`gcs` hardcoded, mirrors `snapdir` L1328-1370) + `shim.rs` `ExternalStore` (emit-command contract for third-party `snapdir-*-store` binaries; built-ins stay in-process); 10+5 tests w/ a mock store. → added `cli-store-wire` prereq.
- 2026-05-31 — `cli-store-wire`: `snapdir push/fetch/checkout/pull/verify` wired to FileStore+core (router-resolved `file://`); checkout restores perms → dest re-manifests to identical id; 2 `store_roundtrip` integration tests.
- 2026-05-31 — `file-store-roundtrip`: `tests/integration/file_store_roundtrip.sh` (15 assertions) — Rust e2e push/fetch/checkout/pull/verify + cross-tool Rust↔Bash both read each other's `file://` stores byte-identically. **Phase 4 complete.** (Surfaced a frozen-oracle macOS nested-checkout bug — not ours.)
- 2026-05-31 — `s3-store`: `S3Store` via `aws-sdk-s3` with **ring-only** rustls (aws-lc-rs kept out of Cargo.lock); `s3://bucket/prefix` + core sharded keys; push/fetch discipline; AWS cred chain via `aws-config`; sync↔async bridge via owned tokio rt; 12 tests (live gated behind env).
- 2026-05-31 — `b2-store`: `B2Store` = thin wrapper over `S3Store` at Backblaze's S3-compatible custom endpoint (`b2://` parse == `s3://` per oracle); no new deps; 12 tests.
- 2026-05-31 — `gcs-store`: `GcsStore` via `google-cloud-storage =1.12.0`, ring-only (eliminated google-cloud-auth's aws-lc-rs defaults + installed ring CryptoProvider); `gs://` parse matches oracle; ADC auth; 13 tests. aws-lc/openssl/native-tls all absent. → added `remote-stores-harness` prereq.
- 2026-05-31 — `remote-stores-harness`: `tests/integration/remote_stores.sh` (per-backend Rust roundtrip + Bash↔Rust cross-tool; emulator-free `--self-check` w/ skip-not-fail). Surfaced CLI `resolve_store` only wires `file://` → added `cli-remote-store-wire` prereq.
- 2026-05-31 — `cli-remote-store-wire`: CLI `resolve_store` now routes `file/s3/b2/gs` to their stores + `ExternalStore` shim for other schemes (via `snapdir_stores::resolve_adapter`); 4 creds-free routing tests. Remote push/fetch now reachable.
- 2026-06-01 — `remote-interop` **GATE-BUMP** (operator-approved): the hollow `echo`+`human_confirm` rubber-stamp was replaced with a real PM-run differential harness `tests/integration/remote_stores_live.sh` — MinIO S3 Bash↔Rust cross-tool (byte-identical) + a **zero-external-dependency** lane (Rust round-trip with `aws`/`b2`/`gcloud` removed from PATH). Hermetic, runs every tick. Caught (and prevented) a near-false-pass; now PM-auto-verified. (git fd17c22)
- 2026-06-01 — `gcs-store-notfound-fix` (stores): repaired a real GcsStore bug surfaced by the live harness — `key_exists`/`get_bytes` only treated HTTP 404 as absent; `google-cloud-storage` v1.12 reports a missing object as service-level `Code::NotFound` (`http_status_code()==None`), so skip-if-present aborted **every** real GCS push before upload. Fixed via `is_not_found()` (HTTP 404 OR service NotFound), mirroring S3; 3 regression tests; live GCS round-trip now passes. (git f08cb84)
- 2026-06-01 — `remote-interop-gcs` (tests): GCS Bash↔Rust cross-tool against the **real** bucket `gs://snapdir-integration-testing` (ADC) — Rust round-trip + Rust-push→Bash(`gcloud`)-fetch + Bash-push→Rust-fetch all byte-identical, same snapshot id `e52681b9…` every direction. **Phase 5 remote interop proven for S3+GCS.** B2 deferred to `remote-interop-b2` (post-release-candidate). (git fd17c22)
- 2026-06-01 — `cache-id` (core): new library-pure `snapdir-core::cache` — `check_snapshot_integrity` (mirrors `_snapdir_check_integrity`: manifest-present + every file object verified vs its content-address), `verify_cache(purge)` (mirrors `verify-cache`: scan `.objects/*/*/*/*`, recompute blake3, compare to path-encoded expected checksum, purge corrupt), `flush_cache`; reuses the frozen sharded helpers + in-process blake3, no `$HOME`/env/IO; 10 tests, tamper case cross-checked vs live `./snapdir verify-cache --purge`. Frozen files untouched. (git 3763bd3)
- 2026-06-01 — `catalog-redb` (catalog): redb-backed catalog (`redb =4.1.0`, pure-Rust, no sqlite/aws-lc) replacing the stub — fixed range scans (no SQL planner): `loc_head` → `locations` + save's previous_id (O(1)), `by_location` reverse-range → `revisions` DESC, `by_id` reverse-range → `ancestors` DESC; `created_at YYYY-MM-DD HH:MM:SS.SSS` (lexical=chronological) + monotonic seq; injectable `Clock`; `save` no-ops when head==id; field sets match the oracle SQL `json_object`. 8 tests. (git b63125f)
- 2026-06-01 — `catalog-compat` (catalog): JSON-line serialization (serde/serde_json) matches the oracle's sqlite `json_object` **byte-for-byte** — compact, exact key order, `revisions` without `location`, `previous_id` bare `null`; proven by a live-`sqlite3` golden test that drives the frozen `./snapdir-sqlite3-catalog` and neutralizes `NOW()` by parsing the oracle's own `created_at`. 6 `json_compat` tests. **Catalog JSON shapes CLI-compat FROZEN.** (git f7025b1)
- 2026-06-01 — `catalog-rebuild` (catalog): `Catalog::rebuild(location, RebuildEntry{id,created_at})` — store-agnostic (no `snapdir-stores` dep), clock-free: sorts the store's recovered entries by `created_at`, clears the location across all 4 redb tables, replays through `save`'s insert path to reconstruct `previous_id` + head==id dedup; touches only that location; idempotent. Recoverability boundary documented (store yields id-set + `created_at`; `previous_id` re-derived by chronological order). 6 rebuild tests incl. byte-identical round-trip. **Phase 6 complete.** (git d72913c)
- 2026-06-01 — `bench-scaffold` (ci): registered the top-level `benches` workspace member (`snapdir-benches`, criterion 0.7 no-aws-lc) + minimal real `hot_paths` `[[bench]]`, fixing `bench-compile`'s vacuous-pass risk (`cargo build --benches` was building zero targets). (git 26b89d5)
- 2026-06-01 — `bench-compile` (bench): 3 criterion hot-path groups — `hash/blake3/{64B,4K,64K,1M}` (Throughput::Bytes), `walk/{many_small,few_large}` via `walk()`+`Blake3Hasher`, `manifest/{emit,parse}/{1k,10k}`; `tempfile` corpora, deterministic, self-cleaning; smoke-run e.g. blake3/1M ~1.43 GiB/s. Benches only measure (output bytes unchanged). (git 06ff564)
- 2026-06-01 — `perf-harness` (bench): `benches/compare.sh` — Rust-vs-Bash `manifest` comparison that asserts **byte-identical output first** (hard-fail on drift) then times both (hyperfine if present, else a portable median-of-N `EPOCHREALTIME` fallback — hyperfine is absent here) over many-small + few-large corpora; jq JSON report for `perf-gate`; `--self-check`. Full run: **~33× (many-small), ~2.7× (few-large), output byte-identical**; perf win is pure in-process walk+BLAKE3 (no core change needed). (git ac688e5)
- 2026-06-01 — `perf-gate` (bench, **human checkpoint**): operator signed off after the PM ran `bash benches/compare.sh` — byte-identical output both corpora, **33.6× many-small / 2.69× few-large**. **Phase 7 complete.** (git ac688e5)
- 2026-06-01 — `cli-trycmd` (cli; owner-fixed from `tests`): 29 `trycmd` surface snapshots (help/version, all 14 subcommand `--help`, parse + missing-arg errors, the 7 stubs' real "not implemented yet") + 6 `assert_cmd`/`assert_fs` e2e (`id`/`manifest` byte-equal the oracle, `push→fetch→checkout→verify` file:// round-trip). Honest wired-vs-stub coverage map; no oracle divergence. (git 9218c33)
- 2026-06-01 — `proptest-roundtrip` (core; owner-fixed from `tests`): `tests/proptest_roundtrip.rs` — 3 proptests ×1024 cases proving manifest `parse_line(Display)==entry`, fields verbatim (paths keep spaces), `Manifest from_entries→Display→parse` modulo `sort -k5`; strategy constraints derived from real `parse_line`/`Display`. Frozen files untouched; no parse/emit asymmetry. (git 3690282)
- 2026-06-01 — `coverage-gate` (**GATE-BUMP**, operator-approved): hollow `echo`+`human_confirm` (CI `--fail-under-lines 0`) → real `cargo llvm-cov --fail-under-lines 75` machine check. PM installed cargo-llvm-cov 0.8.7, measured **79.43% workspace line coverage** (core/catalog/cli high; s3/gcs/b2 low — live-gated). Floor 75 set (operator value); proven real (90→exit1, 75→exit0). CI parity → `ci-coverage-floor`. (git f7db332)
- 2026-06-01 — `ci-coverage-floor` (ci): `ci.yaml` coverage job `--fail-under-lines 0 → 75` so GitHub CI enforces the operator-approved floor matching the `coverage-gate`; actionlint clean. (git c68ce6f)
- 2026-06-01 — `fuzz-parser` (tests): `tests/fuzz/` cargo-fuzz target `manifest_parser.rs` (libfuzzer feeds arbitrary bytes to `ManifestEntry::parse_line` + `Manifest::parse`; no-panic + parse→Display→parse stability), isolated via a nested `[workspace]` so the root workspace is unaffected (`snapdir-fuzz` not a root member). cargo-fuzz/nightly absent → presence-only (CI cron runs it). **Phase 8 complete.** (git 08b8d7c)
- 2026-06-01 — `rustdoc-doctests` (docs): `docs/rust-port/manifest-spec.md` (faithful frozen-format spec; snapshot ID = b3sum of `#`-stripped manifest text, NOT root dir checksum) + `CHANGELOG.md` (Keep a Changelog, 0.5.0). Doctests green (core 2 + stores 1). PM enforced the artifact presence beyond the doctest-only verification, and fixed a stale `PM_PROMPT.md` snapshot-id line. (git 45cd32f)
- 2026-06-01 — `migration-guide-draft` (docs; PM-added prereq): `docs/rust-port/migration.md` — tool→subcommand mapping, honest 14-subcommand wired-vs-stub table, per-backend auth tables (GCS ADC/`GOOGLE_APPLICATION_CREDENTIALS`, AWS chain, B2 S3-compat), SQLite→redb catalog note, byte-for-byte interop, doc-bug fixes (`--linked`/`ensure-no-errors`). Pinned to oracle scripts. Sets up the `migration-guide` human sign-off. (git 12da788)
- 2026-06-01 — `migration-guide` (docs, **human checkpoint**): operator signed off the guide. The honest wired-vs-stub table surfaced a **feature-completeness gap** → PM GATE-ADDed `cli-cache-commands`/`cli-catalog-commands`/`cli-defaults` (wire the 7 stub subcommands to existing core/catalog logic) + `migration-guide-refresh`, gating `release-dryrun` on all four. (git 12da788)
- 2026-06-01 — `cli-cache-commands` (cli): wired `stage` (= push to a `FileStore` rooted at the XDG cache dir; id == `snapdir id`), `verify-cache [--purge]` (→ `cache::verify_cache`, oracle exit semantics), `flush-cache` (→ `cache::flush_cache`) — all reusing existing libs, no new core code. 5 `cache_commands` assert_cmd tests; removed the 3 obsolete "not implemented" stub snapshots. **3/7 stub subcommands now wired.** (git 4399351)
- 2026-06-01 — `cli-catalog-commands` (cli): wired `locations`/`ancestors`/`revisions` → `snapdir_catalog` + the frozen JSON serializers; `catalog_db_path` resolves `--catalog`/`SNAPDIR_CATALOG`; wired `push` → `Catalog::log("push", id, store, SystemClock)` (oracle `_snapdir_log_event push` @ L359) so queries return real data. 4 `catalog_commands` tests incl. a live `sqlite3` oracle cross-check. **6/7 stub subcommands now wired** (only `defaults` left). (git 2032e2c)
- 2026-06-01 — `cli-defaults` (cli): wired `defaults` (last stub) reproducing oracle `snapdir_defaults` (L1083) — 3 groups under `sort -u`: manifest non-option defaults, `SNAPDIR*` env vars reformatted `--opt=value` (excl. `*VERSION*`), `SNAPDIR_BIN_PATH=`. 6 deterministic `defaults_command` tests + a `./snapdir` cross-check. **ALL 14 SUBCOMMANDS WIRED — CLI FEATURE-COMPLETE; Phase 6 complete.** (git f0db30b)
- 2026-06-01 — `migration-guide-refresh` (docs): flipped all 7 previously-stub rows in `migration.md` to WIRED (accurate, cross-checked vs `cli.rs`; kept the honest catalog push-only-logging caveat). Guide now matches the feature-complete CLI. **Phase 9 complete.** (git 7a66567)
- 2026-06-01 — `release-config` (packaging): `packaging/dist-workspace.toml` (cargo-dist, 7 targets incl. musl static) + Rust `release.yml` (gen-assets/build[+cross]/release/docker scratch-musl→GHCR/crates-io) + `packaging/Dockerfile` FROM scratch — replacing the Bash-era Docker+release-drafter. actionlint clean. Surfaced that the cli has no completions/man hook → GATE-ADDed `cli-completions-man`. (git cec279a)
- 2026-06-01 — `cli-completions-man` (cli): hidden `snapdir completions <shell>` (clap_complete) + `snapdir man` (clap_mangen) generation subcommands (stdout), so the release pipeline bundles completions + man into every archive; `hide=true` keeps the 14-surface help unchanged. 4 `completions_man` tests; PM smoke-verified real bash completion + roff man. Release artifact set now complete. (git 4efaac8)
- 2026-06-01 — `release-dry-run-mode` (packaging): operator asked how to dry-run; found `release.yml` published (incl. irreversible crates.io) on any `v*` tag with no safe dry-run. Added `workflow_dispatch`+`dry_run`; gated the 3 publish jobs on a real `v*` tag push so a manual run builds+smokes all targets but NEVER publishes. actionlint clean. Makes `release-dryrun` actually runnable. (git 4b3b6a9)
- 2026-06-01 — `cli-catalog-logging-parity` (cli): closed the last oracle-divergence — wired catalog logging on `manifest` (L212) + `stage` (L826) to match `push` (L359), reusing the same plumbing (no-op without a catalog). Oracle-faithful: `id` does NOT log; `manifest`/`id` stdout byte-unchanged. 7 tests. Catalog logging now at full parity. (git 666a299)

## Remote-store test credentials (operator)

Before the `remote-interop` gate (and any live remote-store run), source the creds:

    source ~/.config/snapdir/test-creds.sh

Then run: `bash tests/integration/remote_stores.sh` (S3/B2/GCS round-trips +
Bash<->Rust cross-tool). Creds live in `~/.config/snapdir/test-creds.sh` (chmod 600,
outside the repo). They are TEST-bucket keys; rotate after use. To scrub the keys from
Claude transcripts/logs after a session ends: `bash ~/.config/snapdir/scrub-test-creds-from-logs.sh`.
